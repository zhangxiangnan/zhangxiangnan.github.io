<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>张向南的博客</title>
  <icon>https://www.gravatar.com/avatar/8b49612074cca29633fcf2c5a21f5185</icon>
  <subtitle>progress everyday</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://zhangxiangnan.com/"/>
  <updated>2019-02-27T05:39:45.000Z</updated>
  <id>http://zhangxiangnan.com/</id>
  
  <author>
    <name>zhangxiangnan</name>
    <email>1194256997@qq.com</email>
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>lambda表达式入门</title>
    <link href="http://zhangxiangnan.com/2018/02/05/java/lambda%E8%A1%A8%E8%BE%BE%E5%BC%8F%E5%85%A5%E9%97%A8/"/>
    <id>http://zhangxiangnan.com/2018/02/05/java/lambda表达式入门/</id>
    <published>2018-02-05T11:47:07.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>lambda表达式入门学习</p><a id="more"></a><h4 id="lambda出现的背景"><a href="#lambda出现的背景" class="headerlink" title="lambda出现的背景"></a>lambda出现的背景</h4><p>如果匿名类的实现起来非常简单,如只有一个方法的接口,那么匿名类的语法使用起来看着会不清晰。</p><p>这些情形,看起来像是把功能当做参数传递到其他方法,如当点击按钮时应该执行的操作。Lambda表达式让我们能够把功能/函数/方法当做参数,或者代码当做数据来传递。</p><p>尽管匿名类比非匿名类简洁,但是对于只有一个方法的类,匿名类仍然有些烦琐。Lambda表达式让我们可以更简洁地表达单方法类的实例对象。</p><h4 id="lambda表达式语法"><a href="#lambda表达式语法" class="headerlink" title="lambda表达式语法"></a>lambda表达式语法</h4><p>lambda表达式如(p,q) -&gt; System.out.print(“x”), 包含如下几部分:</p><ul><li>圆括号里逗号分隔的一个或多个形参列表,注意:<ul><li>形参的数据数据类型可以省略不写</li><li>当只有一个形参时,圆括号也可以省略不写,如p -&gt; System.out.print(“x”)</li></ul></li><li>箭头标记</li><li><p>一个单独表达式或者代码块,如:</p><pre><code>p.getGender() == Person.Sex.MALE    &amp;&amp; p.getAge() &gt;= 18    &amp;&amp; p.getAge() &lt;= 25</code></pre><p>如果是一个单独的表达式,jvm计算表达式的值并返回;此外,也可以使用return语句:</p><pre><code>p -&gt; {    return p.getGender() == Person.Sex.MALE        &amp;&amp; p.getAge() &gt;= 18        &amp;&amp; p.getAge() &lt;= 25;}</code></pre><p> return语句不是一个表达式。在lambda表达式里,必须使用{}把return的语句块包起来,但若{}包的语句块中的方法为void方法,则可以省略{},如:</p><pre><code>email -&gt; System.out.println(email)</code></pre><p> lambda表达式看着很像一个方法声明,可以将lambda表达式认为是匿名的方法调用。</p></li></ul><p>lambda表达式也可以有多个形参,如:</p><pre><code>public class Calculator {    interface IntegerMath {        int operation(int a, int b);       }    public int operateBinary(int a, int b, IntegerMath op) {        return op.operation(a, b);    }    public static void main(String... args) {        Calculator myApp = new Calculator();        IntegerMath addition = (a, b) -&gt; a + b;        IntegerMath subtraction = (a, b) -&gt; a - b;        System.out.println(&quot;40 + 2 = &quot; + myApp.operateBinary(40, 2, addition));        System.out.println(&quot;20 - 10 = &quot; + myApp.operateBinary(20, 10, subtraction));        }}</code></pre><h4 id="lambda表达式理想用例"><a href="#lambda表达式理想用例" class="headerlink" title="lambda表达式理想用例"></a>lambda表达式理想用例</h4><h5 id="创建找出符合某一个特征的人员的搜索方法-找出年龄大于指定值的人"><a href="#创建找出符合某一个特征的人员的搜索方法-找出年龄大于指定值的人" class="headerlink" title="创建找出符合某一个特征的人员的搜索方法(找出年龄大于指定值的人?)"></a>创建找出符合某一个特征的人员的搜索方法(找出年龄大于指定值的人?)</h5><pre><code>public static void printPersonsOlderThan(List&lt;Person&gt; roster, int age) {    for (Person p : roster) {        if (p.getAge() &gt;= age) {            p.printPerson();        }    }}</code></pre><p>   思考: 不支持泛型,改变Person类型就需要重写方法&amp;只支持大于某个年龄,想要找出小于某个年龄的就..</p><h5 id="创建更通用的搜索方法"><a href="#创建更通用的搜索方法" class="headerlink" title="创建更通用的搜索方法"></a>创建更通用的搜索方法</h5><pre><code>public static void printPersonsWithinAgeRange(    List&lt;Person&gt; roster, int low, int high) {    for (Person p : roster) {        if (low &lt;= p.getAge() &amp;&amp; p.getAge() &lt; high) {            p.printPerson();        }    }}</code></pre><p>思考:支持小于某个年龄,但是假如想找出某个指定性别、或者指定性别结合年龄条件? 假如Person类增加了关系、地域等属性呢?每种属性定义一个搜索方法也可以,但会散乱,<br>可以将搜索条件单独隔离到其他方法里。</p><h5 id="在局部类中指定搜索条件的代码"><a href="#在局部类中指定搜索条件的代码" class="headerlink" title="在局部类中指定搜索条件的代码"></a>在局部类中指定搜索条件的代码</h5><pre><code>public static void printPersons(    List&lt;Person&gt; roster, CheckPerson tester) {    for (Person p : roster) {        if (tester.test(p)) {            p.printPerson();        }    }}interface CheckPerson {    boolean test(Person p);}class CheckPersonSelectiveService implements CheckPerson {    public boolean test(Person p) {        return p.gender == Person.Sex.MALE &amp;&amp;            p.getAge() &gt;= 18 &amp;&amp;            p.getAge() &lt;= 25;    }}</code></pre><p>  调用:</p><pre><code>printPersons(    roster, new CheckPersonEligibleForSelectiveService());</code></pre><p>  思考:改变person类型不需要重写printPersons方法,但引入一个接口及每一种搜索一个局部实现类,可以将局部实现类改为匿名类。</p><h5 id="匿名类中指定搜索条件代码"><a href="#匿名类中指定搜索条件代码" class="headerlink" title="匿名类中指定搜索条件代码"></a>匿名类中指定搜索条件代码</h5><pre><code>printPersons(    roster,    new CheckPerson() {        public boolean test(Person p) {            return p.getGender() == Person.Sex.MALE                &amp;&amp; p.getAge() &gt;= 18                &amp;&amp; p.getAge() &lt;= 25;        }    });</code></pre><p>  思考:改为了匿名类,省去局部类的定义,但是匿名类仍然较烦琐,由于CheckPerson接口只有一个方法,则可以考虑使用lambda表达式替换</p><h5 id="使用lambda表达式替换搜索代码"><a href="#使用lambda表达式替换搜索代码" class="headerlink" title="使用lambda表达式替换搜索代码"></a>使用lambda表达式替换搜索代码</h5><p>CheckPerson是一个函数接口,函数接口是仅有一个抽象方法的任何接口(可能包含多个静态方法或默认方法)。由于函数接口仅有一个方法,因为实现该接口时可以忽略方法名称,如下:</p><pre><code>printPersons(    roster,    (Person p) -&gt; p.getGender() == Person.Sex.MALE        &amp;&amp; p.getAge() &gt;= 18        &amp;&amp; p.getAge() &lt;= 25);</code></pre><p>思考:可以使用标准函数接口来进一步减少代码</p><h5 id="使用lambda表达式的标准函数接口"><a href="#使用lambda表达式的标准函数接口" class="headerlink" title="使用lambda表达式的标准函数接口"></a>使用lambda表达式的标准函数接口</h5><p>由于CheckPerson接口仅有一个方法,返回bool值,很简单,不必要在程序中自己定义,所以JDK在java.util.function中提供了若干标准函数接口,可以使用Predicate<t>替换CheckPerson:</t></p><pre><code>interface Predicate&lt;T&gt; {    boolean test(T t);}</code></pre><p>  这是一个泛型接口,替换CheckPerson,如下:</p><pre><code>public static void printPersonsWithPredicate(    List&lt;Person&gt; roster, Predicate&lt;Person&gt; tester) {    for (Person p : roster) {        if (tester.test(p)) {            p.printPerson();        }    }}</code></pre><p>  经过匿名类替换+使用lambda表达式,调用方式如下:</p><pre><code>printPersonsWithPredicate(    roster,    p -&gt; p.getGender() == Person.Sex.MALE        &amp;&amp; p.getAge() &gt;= 18        &amp;&amp; p.getAge() &lt;= 25);</code></pre><h5 id="整个程序中使用lambda表达式"><a href="#整个程序中使用lambda表达式" class="headerlink" title="整个程序中使用lambda表达式"></a>整个程序中使用lambda表达式</h5><p> 再考虑这个方法:</p><pre><code>public static void printPersonsWithPredicate(    List&lt;Person&gt; roster, Predicate&lt;Person&gt; tester) {    for (Person p : roster) {        if (tester.test(p)) {            p.printPerson();        }    }}</code></pre><p> p.printPerson()是一个void方法,对p操作,假如不是执行printPerson操作而是执行其他类似void操作,那么就需要修改printPersonsWithPredicate方法,考虑将这个操作使用<br>  lambda表达式,JDK提供了Consumer<t>标准函数接口,可改进如下:</t></p><pre><code>public static void processPersons(    List&lt;Person&gt; roster,    Predicate&lt;Person&gt; tester,    Consumer&lt;Person&gt; block) {        for (Person p : roster) {            if (tester.test(p)) {                block.accept(p);            }        }}</code></pre><p> 调用:</p><pre><code>processPersons(     roster,     p -&gt; p.getGender() == Person.Sex.MALE         &amp;&amp; p.getAge() &gt;= 18         &amp;&amp; p.getAge() &lt;= 25,     p -&gt; p.printPerson());</code></pre><p> 假如想验证人员身份或获取联系信息呢?需要一个函数接口,其有一个抽象方法,返回一个值,The Function&lt;T,R&gt;标准接口有一个R apply(T t)方法,应用到如下示例:</p><pre><code>public static void processPersonsWithFunction(    List&lt;Person&gt; roster,    Predicate&lt;Person&gt; tester,    Function&lt;Person, String&gt; mapper,    Consumer&lt;String&gt; block) {    for (Person p : roster) {        if (tester.test(p)) {            String data = mapper.apply(p);            block.accept(data);        }    }}</code></pre><p>调用(获取每一个人的邮箱信息,并打印):</p><pre><code>processPersonsWithFunction(    roster,    p -&gt; p.getGender() == Person.Sex.MALE        &amp;&amp; p.getAge() &gt;= 18        &amp;&amp; p.getAge() &lt;= 25,    p -&gt; p.getEmailAddress(),    email -&gt; System.out.println(email));</code></pre><h5 id="使用泛型使扩展性更强"><a href="#使用泛型使扩展性更强" class="headerlink" title="使用泛型使扩展性更强"></a>使用泛型使扩展性更强</h5><p>processPersonsWithFunction方法的泛型版本如下:</p><pre><code>public static &lt;X, Y&gt; void processElements(    Iterable&lt;X&gt; source,    Predicate&lt;X&gt; tester,    Function &lt;X, Y&gt; mapper,    Consumer&lt;Y&gt; block) {    for (X p : source) {        if (tester.test(p)) {            Y data = mapper.apply(p);            block.accept(data);        }    }}</code></pre><p>  调用:</p><pre><code>processElements(    roster,    p -&gt; p.getGender() == Person.Sex.MALE        &amp;&amp; p.getAge() &gt;= 18        &amp;&amp; p.getAge() &lt;= 25,    p -&gt; p.getEmailAddress(),    email -&gt; System.out.println(email));</code></pre><h4 id="聚合操作"><a href="#聚合操作" class="headerlink" title="聚合操作"></a>聚合操作</h4><p>如下聚合操作:</p><pre><code>roster    .stream()    .filter(        p -&gt; p.getGender() == Person.Sex.MALE            &amp;&amp; p.getAge() &gt;= 18            &amp;&amp; p.getAge() &lt;= 25)    .map(p -&gt; p.getEmailAddress())    .forEach(email -&gt; System.out.println(email));</code></pre><p> filter、map、foreach都是聚合操作,聚会操作从stream流中获取元素,而不是从集合中。</p><p> 流是一系列元素,跟集合不同,流不是存储元素的数据结构,流从数据源如集合中通过管道/流水线传输元素。<br> 管道/流水线是一系列流操作,一系列聚合操作,如filter-map-foreach。聚合操作通常接受lambda表达式作为参数,来自定义行为。</p><p>管道/流水线包含以下几部分:</p><ul><li>数据源<br>可以是一个集合、数组、一个生成器函数、I/O channel等</li><li>0步或多步中间操作<br>如filter就是中间操作,生成一个符合过滤条件的元素组成的新流</li><li>一个结束操作<br>如forEach生成一个非流的结果,如void、基本类型数据、一个集合。<h5 id="聚合操作与迭代的区别"><a href="#聚合操作与迭代的区别" class="headerlink" title="聚合操作与迭代的区别"></a>聚合操作与迭代的区别</h5>聚合操作如forEach,与迭代器看起来类似,但是有一个根本区别:</li><li>聚合操作使用内部迭代<br>聚合操作使用内部迭代,只需要提供迭代集合,内部迭代可以是顺序迭代,也可以是并行迭代(并行计算,合并结果);迭代器是外部迭代,需要提供迭代集合与迭代方式,是顺序迭代方式。</li><li>聚合操作从流获取数据来处理<br>聚合操作从流获取数据,非集合,所以聚合操作也称为流操作。</li><li><p>支持传递代码/行为为参数(lambada表达式作为参数)</p><h4 id="归约操作"><a href="#归约操作" class="headerlink" title="归约操作"></a>归约操作</h4><p>   double average = roster</p><pre><code>.stream().filter(p -&gt; p.getGender() == Person.Sex.MALE).mapToInt(Person::getAge).average().getAsDouble();</code></pre></li></ul><p>JDK有很多结束操作,如average、sum、min、max、count,这些操作通过组合流的元素内容来返回一个值,这些操作叫做归约操作(reduction operations)。<br>JDK也有返回一个集合的归约操作,很多归约操作都是执行一个指定的任务,如计算平均值,元素分组。JDK提供了通用的归约操作,Stream.reduce&amp;Stream.collect</p><h5 id="Stream-reduce"><a href="#Stream-reduce" class="headerlink" title="Stream.reduce"></a>Stream.reduce</h5><p>Stream.reduce是通用的归约操作,如下例则使用了Stream.sum 归约操作:</p><pre><code>Integer totalAge = roster    .stream()    .mapToInt(Person::getAge)    .sum();</code></pre><p>使用Stream.reduce实现相同的逻辑如下:</p><pre><code>Integer totalAgeReduce = roster   .stream()   .map(Person::getAge)   .reduce(       0,       (a, b) -&gt; a + b);</code></pre><p>reduce操作有2个参数:</p><pre><code>- 第一个为标识元素    是归约的初始值和当流中无元素时的默认返回值- 累加器函数    累加器函数有2个参数,归约的部分结果值和流中的下一个要计算的元素</code></pre><p>reduce操作每次都返回一个新值,累加器每次处理完一个元素后也是返回一个新值,所以如果想要归约流的元素为一个更复杂的对象,如集合,由于每次会返回一个新的集合,性能低下,可以用Stream.collect</p><h5 id="Stream-collect"><a href="#Stream-collect" class="headerlink" title="Stream.collect"></a>Stream.collect</h5><p>reduce方法每处理一个元素会新建一个值,collect方法则是修改、转换一个已经存在的值。考虑如何计算流中元素的平均值?需要两份数据,一个是所有元素的个数,一个是其和,和reduce方法以及其他所有<br>归约方法一样,collect也只返回一个值,可以创建一个新的数据类型,包含这两个计数,如下:</p><pre><code>class Averager implements IntConsumer {    private int total = 0;    private int count = 0;    public double average() {        return count &gt; 0 ? ((double) total)/count : 0;    }    public void accept(int i) { total += i; count++; }    public void combine(Averager other) {        total += other.total;        count += other.count;    }}Averager averageCollect = roster.stream()    .filter(p -&gt; p.getGender() == Person.Sex.MALE)    .map(Person::getAge)    .collect(Averager::new, Averager::accept, Averager::combine);System.out.println(&quot;Average age of male members: &quot; +    averageCollect.average());</code></pre><p>该例collect方法有三个参数:</p><pre><code>- supplier    结果容器创建函数,工厂方法,为collect操作创建一个新实例,创建结果容器的实例,本例则是创建Averager的实例。- accumulator    累加器函数合并一个流元素到结果容器中,本例中,即修改Averager的2个计数- combiner    合并函数将两个结果容器的内容进行合并,本例中,为对两个Averager容器中的2个计数进行合并相加。</code></pre><p>注意:</p><pre><code>- supplier是一个lambda表达式或者方法引用,而非reduce操作中的identity元素是一个值。- accumulator累加器和combiner合并函数不返回值- 可以结合并行流使用collect操作</code></pre><p>尽管JDK提供了average操作来计算平均值,当我们需要计算流中元素的几个值时就可以使用collect操作+自定义类。<br>collect操作最适合集合,如下:</p><pre><code>List&lt;String&gt; namesOfMaleMembersCollect = roster    .stream()    .filter(p -&gt; p.getGender() == Person.Sex.MALE)    .map(p -&gt; p.getName())    .collect(Collectors.toList());</code></pre><p>该例collect只需要一个Collector类型参数,该类封装了collect操作所需的三个参数(supplier、accumulator、combiner),Collectors类包含许多有用的操作,如累加元素到集合,根据不同条件分类元素等。<br>Collectors.toList返回的Collector会累加流元素到一个新list。</p><p>按性别分类:</p><pre><code>Map&lt;Person.Sex, List&lt;Person&gt;&gt; byGender =    roster        .stream()        .collect(            Collectors.groupingBy(Person::getGender));</code></pre><p>groupingBy方法根据指定的分类函数(getGener)来分类,返回的map的key为getGender的返回值。</p><p>获取集合中的成员姓名并按性别分类:</p><pre><code>Map&lt;Person.Sex, List&lt;String&gt;&gt; namesByGender =    roster        .stream()        .collect(            Collectors.groupingBy(                Person::getGender,                                      Collectors.mapping(                    Person::getName,                    Collectors.toList())));</code></pre><p>本例的groupingBy方法有2个参数,一个分类函数,一个Collector实例,Collector参数称为下游收集器,该收集器应用到另一个收集器的结果。</p><p>因此,groupingBy操作使你可以应用collect方法到groupingBy操作创建的List结果值上。</p><p>该例中,collector mapping将mapping函数应用到流中的每一个元素上,返回的结果流由会员名称组成;包含一个或多个下流收集器的流水线如本例称作多级归约。</p><p>如下例,获取每一种性别的会员的年龄总和:</p><pre><code>Map&lt;Person.Sex, Integer&gt; totalAgeByGender =    roster        .stream()        .collect(            Collectors.groupingBy(                Person::getGender,                                      Collectors.reducing(                    0,                    Person::getAge,                    Integer::sum)));</code></pre><p>Collectors.reducing归约操作有三个参数:</p><ul><li>identity:<br> 如Stream.reduce操作一样,identity是归约的初始值和默认值(如果流中没有元素),上例中,identity为0</li><li>mapper:<br>归约操作将mapper函数应用到所有流元素,本例是获取每个成员的年龄</li><li>operation:<br>操作函数用来归约映射的值,本例是对年龄求和</li></ul><p>下例是获取每种性别的会员的平均年龄:</p><pre><code>Map&lt;Person.Sex, Double&gt; averageAgeByGender = roster    .stream()    .collect(        Collectors.groupingBy(            Person::getGender,                                  Collectors.averagingInt(Person::getAge)));</code></pre><h4 id="并行"><a href="#并行" class="headerlink" title="并行"></a>并行</h4><p>并行计算涉及到将一个问题分为多个子问题,同时解决子问题(并行中,每一个子问题在一个独立线程中运行),然后合并各个子问题的结果。<br>JavaSE提供了fork/join框架,可以让我们很方便地实现并行计算,然而用这个框架的话,必须指定如何划分问题,通过聚合操作就可以进行问题划分和结果合并。</p><p>对于使用集合的情形,并行计算的一个难点就是操作集合时的线程安全问题,虽然集合框架的同步方法是线程安全的,但是会导致线程竞争,达不到并行的目的。<br>聚合操作以及并行流使我们能够实现提供的非线程安全性集合的并行性,在操作集合时不需要修改集合。<br>注意,并行并不一定比串行快,当有足够的数据和处理器核心时可能并行更有优势。所以,即使聚合操作让我们更容易实现并行计算,但是也要考虑是否适合并行。</p><h5 id="并行执行流操作"><a href="#并行执行流操作" class="headerlink" title="并行执行流操作"></a>并行执行流操作</h5><p>流可以串行执行,也可以并行执行,当并行执行流时,jvm将流分为多个子流,聚合操作并行的迭代处理这些流,并把结果合并。<br>当创建一个流时,默认是串行流,除非使用Collection.parallelStream或者BaseStream.parallel指定为并行流。如下例,并行计算男士的平均年龄:</p><pre><code>double average = roster    .parallelStream()    .filter(p -&gt; p.getGender() == Person.Sex.MALE)    .mapToInt(Person::getAge)    .average()    .getAsDouble();</code></pre><h5 id="并发归约"><a href="#并发归约" class="headerlink" title="并发归约"></a>并发归约</h5><p>再考虑如下例子:</p><pre><code>Map&lt;Person.Sex, List&lt;Person&gt;&gt; byGender =    roster        .stream()        .collect(            Collectors.groupingBy(Person::getGender));</code></pre><p>对应的并行代码:</p><pre><code>ConcurrentMap&lt;Person.Sex, List&lt;Person&gt;&gt; byGender =    roster        .parallelStream()        .collect(            Collectors.groupingByConcurrent(Person::getGender));</code></pre><p>上例称作并发归约,jvm在以下条件都为真时会为包含collect操作的指定流水线执行并发归约:</p><ul><li>流是并行流</li><li>collect操作的collector参数的Collector.Characteristics为并发模式,通过Collector.characteristics方法设置</li><li>无论流式无序,或者collector设置了Collector.Characteristics.UNORDERED;确保流式无序的,可以通过BaseStream.unordered操作</li></ul><h5 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h5><p>pipeline处理流中元素的顺序依赖于流是串行执行还是并行执行,以及流的数据源本身和中间操作,如下例:</p><pre><code>Integer[] intArray = {1, 2, 3, 4, 5, 6, 7, 8 };List&lt;Integer&gt; listOfIntegers =    new ArrayList&lt;&gt;(Arrays.asList(intArray));System.out.println(&quot;listOfIntegers:&quot;);listOfIntegers    .stream()    .forEach(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);System.out.println(&quot;listOfIntegers sorted in reverse order:&quot;);Comparator&lt;Integer&gt; normal = Integer::compare;Comparator&lt;Integer&gt; reversed = normal.reversed();Collections.sort(listOfIntegers, reversed);  listOfIntegers    .stream()    .forEach(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);System.out.println(&quot;Parallel stream&quot;);listOfIntegers    .parallelStream()    .forEach(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);System.out.println(&quot;Another parallel stream:&quot;);listOfIntegers    .parallelStream()    .forEach(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);System.out.println(&quot;With forEachOrdered:&quot;);listOfIntegers    .parallelStream()    .forEachOrdered(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);listOfIntegers:1 2 3 4 5 6 7 8listOfIntegers sorted in reverse order:8 7 6 5 4 3 2 1Parallel stream:3 4 1 6 2 5 7 8Another parallel stream:6 3 1 5 7 8 4 2With forEachOrdered:8 7 6 5 4 3 2 1</code></pre><h4 id="副作用"><a href="#副作用" class="headerlink" title="副作用"></a>副作用</h4><p>一个方法或一个表达式除了返回值及生成一个值,还有一个副作用就是会修改计算机的状态,如可变的reductions操作(collect操作)及System.out.println。<br>JDK对某些副作用处理的很好,特别地,collect方法就是设计针对并行安全有副作用的场景下来执行最常见的stream操作。类似forEach、peek操作可以消除副作用;lambda表达式即使返回void,但如果内部调用了System.out.println,<br>也会产生副作用。</p><h5 id="延迟计算-惰性计算"><a href="#延迟计算-惰性计算" class="headerlink" title="延迟计算/惰性计算"></a>延迟计算/惰性计算</h5><p>所有中间操作都是延迟执行的,表达式、方法或算法如果仅仅在需要的时候才计算其值,则就是延迟计算。中间操作是延迟计算因为当终结操作开始时,才会处理流中的数据。延迟处理流的机制让java编译器及运行时<br>能够优化处理流的方式。比如filter-mapToInt-average流水线操作average可以先从mapToInt操作创建的流中获取几个数,mapToInt则是从filter操作获取元素,average操作则重复着这个流程直至获取所有的数据,然后计算平均值。</p><h5 id="干扰"><a href="#干扰" class="headerlink" title="干扰"></a>干扰</h5><p>流操作中的lambda表达式不应该有干扰。干扰发生在一个流水线处理流时,流的数据源被修改,如下例:</p><pre><code>try {    List&lt;String&gt; listOfStrings =        new ArrayList&lt;&gt;(Arrays.asList(&quot;one&quot;, &quot;two&quot;));    String concatenatedString = listOfStrings        .stream()        // 不要在处理流时,对流的数据源进行修改,会发生干扰,导致报错        .peek(s -&gt; listOfStrings.add(&quot;three&quot;))        .reduce((a, b) -&gt; a + &quot; &quot; + b)        .get();    System.out.println(&quot;Concatenated string: &quot; + concatenatedString);} catch (Exception e) {    System.out.println(&quot;Exception caught: &quot; + e.toString());}</code></pre><h5 id="有状态的lambda表达式"><a href="#有状态的lambda表达式" class="headerlink" title="有状态的lambda表达式"></a>有状态的lambda表达式</h5><p>在流操作中避免使用有状态的lambda表达式当做参数,有状态的lambda表达式是指其结果依赖于在流水线执行过程中可能改变的任何状态,如下例:</p><pre><code>List&lt;Integer&gt; serialStorage = new ArrayList&lt;&gt;();System.out.println(&quot;Serial stream:&quot;);listOfIntegers    .stream()    // 不要这么做,其使用了有状态的lambda表达式    .map(e -&gt; { serialStorage.add(e); return e; })    .forEachOrdered(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);serialStorage    .stream()    .forEachOrdered(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);System.out.println(&quot;Parallel stream:&quot;);List&lt;Integer&gt; parallelStorage = Collections.synchronizedList(    new ArrayList&lt;&gt;());listOfIntegers    .parallelStream()    // 使用了有状态的lambda表达式    .map(e -&gt; { parallelStorage.add(e); return e; })    .forEachOrdered(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);parallelStorage    .stream()    .forEachOrdered(e -&gt; System.out.print(e + &quot; &quot;));System.out.println(&quot;&quot;);</code></pre><p>lambda表达式 e -&gt; { parallelStorage.add(e); return e; } 是有状态的lambda表达式,因为每次代码运行其结果都会不同:</p><pre><code>Serial stream:8 7 6 5 4 3 2 18 7 6 5 4 3 2 1Parallel stream:8 7 6 5 4 3 2 11 3 6 2 4 5 8 7</code></pre><p>若想要可预测性及确定性结果,要确保流操作中的lambda表达式参数是无状态的。</p><p>注意:ArrayList是非线程安全的,即多个线程不能同时访问指定集合,否则会导致不可预料结果,如下:</p><pre><code>Parallel stream:8 7 6 5 4 3 2 1null 3 5 4 7 8 1 2</code></pre><h4 id="访问外部范围的局部变量"><a href="#访问外部范围的局部变量" class="headerlink" title="访问外部范围的局部变量"></a>访问外部范围的局部变量</h4><p> 和局部和匿名类类似,lambda表达式也能够捕捉变量,也可以访问外部范围的局部变量,但是不会覆盖外部范围的局部变量。</p><p> 这是因为lambda表达式从词法上被隔离,不会从超类继承任何变量,也不会引入新的变量作用范围,<br> lambda表达式里声明的变量被封闭在lambda表达式里, 如下例:</p><pre><code>import java.util.function.Consumer;public class LambdaScopeTest {    public int x = 0;    class FirstLevel {        public int x = 1;        void methodInFirstLevel(int x) {            Consumer&lt;Integer&gt; myConsumer = (y) -&gt;            {                System.out.println(&quot;x = &quot; + x); // Statement A                System.out.println(&quot;y = &quot; + y);                System.out.println(&quot;this.x = &quot; + this.x);                System.out.println(&quot;LambdaScopeTest.this.x = &quot; + LambdaScopeTest.this.x);            };            myConsumer.accept(x);        }    }    public static void main(String... args) {        LambdaScopeTest st = new LambdaScopeTest();        LambdaScopeTest.FirstLevel fl = st.new FirstLevel();        fl.methodInFirstLevel(23);    }}x = 23y = 23this.x = 1LambdaScopeTest.this.x = 0</code></pre><p>假如把lambda表达式声明替换为如下:</p><pre><code>Consumer&lt;Integer&gt; myConsumer = (x) -&gt; {}</code></pre><p>则会报x已定义的错,这是因为lambda表达式不会引入新的变量作用范围,因此在lambda表达式内部可以直接访问外部范围的字段、方法、局部变量,比如可以直接访问methodInFirstLevel方法形参x,但是要访问FirstLevel<br>的字段x,则需要使用this.</p><h4 id="目标类型"><a href="#目标类型" class="headerlink" title="目标类型"></a>目标类型</h4><p>lambda表达式的类型如何确定呢?是通过目标类型,即如public static void printPersons(List<person> roster, CheckPerson tester)需要CheckPerson类型,则lambda表达式就是该类型(方法声明需要的参数类型)。</person></p><p>java编译器根据lambda表达式使用的上下文或场景里找到目标类型当做lambda表达式的类型,也就是lambda只能用在java编译器能确定目标类型的场景里:</p><pre><code>- 变量声明- 赋值- return 语句- 数组初始化- 方法或者构造函数参数- lambda表达式体- ? : 三元表达式- 转换语句</code></pre><p>对于方法参数,java编译器确定lambda表达式类型通过2个特征:重载解析&amp;类型参数推导</p><h4 id="序列化"><a href="#序列化" class="headerlink" title="序列化"></a>序列化</h4><p>如同非静态内部类一样,强烈建议禁止lambda表达式的序列化。</p><h4 id="方法引用"><a href="#方法引用" class="headerlink" title="方法引用"></a>方法引用</h4><p>我们使用lambda表达式来创建匿名方法时,有时lambda表达式啥也没做只是调用了一个已经存在的方法,针对这些情形,通过方法名称来引用已经存在的方法更简洁直观,方法引用可以做到这点。</p><pre><code>Arrays.sort(rosterAsArray, Person::compareByAge);</code></pre><h5 id="方法引用的种类"><a href="#方法引用的种类" class="headerlink" title="方法引用的种类"></a>方法引用的种类</h5><ul><li>引用静态方法<br>  ContainingClass::staticMethodName</li><li>引用非静态实例方法<br>containingObject::instanceMethodName</li><li>引用某个类型的任意对象的实例方法<br>  ContainingType::methodName,如String::compareToIgnoreCase</li><li>引用构造方法<br>  ClassName::new</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;lambda表达式入门学习&lt;/p&gt;
    
    </summary>
    
      <category term="lambda" scheme="http://zhangxiangnan.com/categories/lambda/"/>
    
    
      <category term="lambda" scheme="http://zhangxiangnan.com/tags/lambda/"/>
    
  </entry>
  
  <entry>
    <title>静态内部类&amp;非静态内部类&amp;局部类&amp;匿名类</title>
    <link href="http://zhangxiangnan.com/2018/01/18/java/%E9%9D%99%E6%80%81%E5%86%85%E9%83%A8%E7%B1%BB-%E9%9D%9E%E9%9D%99%E6%80%81%E5%86%85%E9%83%A8%E7%B1%BB-%E5%B1%80%E9%83%A8%E7%B1%BB-%E5%8C%BF%E5%90%8D%E7%B1%BB/"/>
    <id>http://zhangxiangnan.com/2018/01/18/java/静态内部类-非静态内部类-局部类-匿名类/</id>
    <published>2018-01-18T11:22:06.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>  静态内部类&amp;非静态内部类&amp;局部类&amp;匿名类学习笔记</p>  <a id="more"></a><h4 id="内部类-嵌套类"><a href="#内部类-嵌套类" class="headerlink" title="内部类/嵌套类"></a>内部类/嵌套类</h4><h5 id="内部类的定义"><a href="#内部类的定义" class="headerlink" title="内部类的定义"></a>内部类的定义</h5><p>Java语言支持在一个类中定义其他类,叫做内部类,分为2种,若用static修饰,则是静态内部类,否则为非静态内部类,如:</p><pre><code>class OuterClass {    static class StaticNestedClass {    }    class InnerClass {    }}</code></pre><p>注意:</p><pre><code>- 内部类是封闭类/外部类的一个成员,因此内部类可以被声明为private、public、protected、包级private(默认),而外部类只能用public、或包级private修饰。- 非静态内部类可以访问外部类的成员,即时是私有成员,静态内部类则不能访问外部类的成员。</code></pre><h5 id="为什么用内部类"><a href="#为什么用内部类" class="headerlink" title="为什么用内部类"></a>为什么用内部类</h5><ul><li>逻辑分类<br> 如果一个类只会在另一个类中使用,那么将其嵌入到那个类中就很合理,也有利于包结构的精简</li><li>有助于封装<br> 如果有A、B两个类,A的成员为私有,且B只会配合A使用,此时,B如果定义为单独的类就无法访问A的成员,但是如果把A嵌入到B中,则可以访问A的私有成员,且B也可以和外部隔离(因为B只会在A中使用)。</li><li>更高地可读性、可维护性<br> 小的类嵌入到其他类中,可读性更强</li></ul><h5 id="静态内部类"><a href="#静态内部类" class="headerlink" title="静态内部类"></a>静态内部类</h5><p>类似于方法和变量,静态内部类和外部类关联,如静态类方法一样,静态内部类不能直接引用外部类中定义的实例变量或方法,只能通过对象来间接引用。</p><pre><code>注意:静态内部类和外部类的实例对象成员交互时和其他正常类一样,实际上静态内部类和其他正常类一样。</code></pre><p>使用外部类名称来访问静态内部类,如:</p><pre><code>OuterClass.StaticNestedClass</code></pre><p>创建静态内部类的对象实例语法如下:</p><pre><code>OuterClass.StaticNestedClass nestedObject =     new OuterClass.StaticNestedClass();</code></pre><h5 id="非静态内部类"><a href="#非静态内部类" class="headerlink" title="非静态内部类"></a>非静态内部类</h5><p>类似于实例方法和实例变量,非静态内部类和外部类的实例相关联,可以直接访问外部类实例的方法和字段,同时非静态内部类本身不能定义任何静态成员。</p><pre><code>class OuterClass {    class InnerClass {    }}</code></pre><p>InnerClass的实例只能存在于OuterClass的实例中,同时能直接访问外部类实例的方法和字段。<br>要实例化内部InnerClass类,必须首先实例化OuterClass外部类,然后使用以下语法在外部对象中创建内部类的对象:</p><pre><code>OuterClass.InnerClass innerObject = outerObject.new InnerClass();</code></pre><p>有两种特殊的非静态内部类,局部类&amp;匿名类</p><h5 id="Shadowing"><a href="#Shadowing" class="headerlink" title="Shadowing"></a>Shadowing</h5><p>如果在一个特定范围(如非静态内部类或者方法声明)中,一个类型的声明(如一个成员变量或者参数名称)与外部范围里的另一个声明名称一样的话,那么该类型声明会覆盖外部范围的声明。<br>想要访问被覆盖的声明,仅仅用名称是不够的,如下:</p><pre><code>public class ShadowTest {    public int x = 0;    class FirstLevel {        public int x = 1;        void methodInFirstLevel(int x) {            System.out.println(&quot;x = &quot; + x);            System.out.println(&quot;this.x = &quot; + this.x);            System.out.println(&quot;ShadowTest.this.x = &quot; + ShadowTest.this.x);        }    }    public static void main(String... args) {        ShadowTest st = new ShadowTest();        ShadowTest.FirstLevel fl = st.new FirstLevel();        fl.methodInFirstLevel(23);    }}</code></pre><p>结果:</p><pre><code>x = 23this.x = 1ShadowTest.this.x = 0</code></pre><h5 id="序列化"><a href="#序列化" class="headerlink" title="序列化"></a>序列化</h5><p>强烈禁止非静态内部类、包括局部类及匿名类的序列化,这是因为编译器编译这些类时,会创建合成的构造方法,合成构造方法让java编译器能够实现新的java语言特征,而不需要改变jvm,<br>但是由于不同的java编译器实现中合成的构造方法不同,所以序列化与反序列化时若使用的jre实现不同会遇到兼容性问题。</p><h4 id="局部类"><a href="#局部类" class="headerlink" title="局部类"></a>局部类</h4><p>局部类是定义在代码块中的类,代码块指{}之间的零个或多个语句,经常是在方法内部定义局部类</p><h5 id="定义局部类"><a href="#定义局部类" class="headerlink" title="定义局部类"></a>定义局部类</h5><p>本地类/局部类是指在代码块中定义的类,代码块指{}括起来的0个或多个语句的组合体,经常是在方法中定义局部类.<br>可以在任何代码块中定义局部类,如可以在方法内部、for循环中、if语句中定局部类。如:</p><pre><code>public class LocalCalssTest {    private int name ;    public static void main(String[] args) {        class LocalClass {          private int age;        }        int age = new LocalClass().age;        System.out.println(age);    }}</code></pre><h5 id="访问外部类-封装类的成员"><a href="#访问外部类-封装类的成员" class="headerlink" title="访问外部类/封装类的成员"></a>访问外部类/封装类的成员</h5><p>局部类可以访问外部类/封装类的成员,上个例子中,LocalClass就可以访问name属性。此外,局部类也可以访问定义为final的局部变量或参数.jdk8后final或等效final(初始化后无更改指令)的都可以访问。<br>jdk8开始,如果在方法中定义局部类,那么该局部类可以访问方法的参数。</p><h5 id="覆盖"><a href="#覆盖" class="headerlink" title="覆盖"></a>覆盖</h5><p>局部类中一个类型或变量的声明会覆盖外部类中相同名字类型或变量的声明。</p><h5 id="局部类类似于非静态内部类"><a href="#局部类类似于非静态内部类" class="headerlink" title="局部类类似于非静态内部类"></a>局部类类似于非静态内部类</h5><p>2者类似因为都不能定义或声明任何静态成员,静态方法里定义的局部类也仅只能访问外部类的静态成员。</p><p>代码块中不能定义接口,因为接口本质上是静态的,所以如下编译不通过:</p><pre><code>public void greetInEnglish() {    interface HelloThere {       public void greet();    }    class EnglishHelloThere implements HelloThere {        public void greet() {        }    }}</code></pre><p>局部类中也不能定义静态的方法、静态成员,如下编译不通过:</p><pre><code>public void sayGoodbyeInEnglish() {    class EnglishGoodbye {        public static void sayGoodbye() {        }    }}</code></pre><p>局部类可以有是常量变量的静态成员</p><pre><code>- 常量变量指基本类型的变量,或声明为final的对象类型并且是使用编译期常量表达式初始化- 编译期常量表达式指如string的&quot;&quot;初始化形式,或者表达式的值在编译器可以计算得到。</code></pre><h4 id="匿名类"><a href="#匿名类" class="headerlink" title="匿名类"></a>匿名类</h4><p>匿名类使代码更加简洁,可以同时进行类的声明和实例化,类似于局部类除了没名字,用于只使用局部类一次的情形。</p><h5 id="声明匿名类"><a href="#声明匿名类" class="headerlink" title="声明匿名类"></a>声明匿名类</h5><p>局部类是有类声明的,匿名类是通过表达式来定义的,如下:</p><pre><code>public class HelloWorldAnonymousClasses {    interface HelloWorld {        public void greet();    }    public void sayHello() {        class EnglishGreeting implements HelloWorld {            public void greet() {            }        }        HelloWorld englishGreeting = new EnglishGreeting();        HelloWorld frenchGreeting = new HelloWorld() {            public void greet() {                greetSomeone(&quot;tout le monde&quot;);            }        };    }            }</code></pre><h5 id="匿名类的语法"><a href="#匿名类的语法" class="headerlink" title="匿名类的语法"></a>匿名类的语法</h5><p>匿名类是一个表达式,匿名类表达式的语法和构造函数的调用类似,只是在代码块中进行类定义:</p><pre><code>HelloWorld frenchGreeting = new HelloWorld() {    public void greet() {    }};</code></pre><p>匿名类表达式包含如下几部分:</p><pre><code>- new操作符- 接口或抽象类的名称- 圆括号把传递给构造函数的参数括起来,类似正常类实例的创建,接口无参数。- 类声明的主体,允许定义方法,不能使用语句。</code></pre><p>因为匿名类定义是一个表达式,表达式必须是语句的一部分,所以以分号结束。</p><h5 id="声明及访问变量说明"><a href="#声明及访问变量说明" class="headerlink" title="声明及访问变量说明"></a>声明及访问变量说明</h5><p>匿名类可以访问外部域的局部变量:</p><ul><li>匿名类可以访问外部类的成员</li><li>匿名类不能访问外部范围中定义的非final或非等效final的局部变量</li><li>和嵌套类类型,匿名类中类型的声明如变量会覆盖外部范围中有相同名称的定义</li></ul><p>匿名类中的成员定义和局部类一样也有约束:</p><ul><li>匿名类中不能声明静态的成员(非final)、静态方法</li><li>匿名类可以声明是常量变量的静态成员。</li></ul><p>匿名类中可以声明以下部分:</p><ul><li>字段</li><li>额外的方法(未实现父类抽象方法或接口方法)</li><li>局部/本地类</li><li>实例初始化</li></ul><p>匿名类中不能定义构造函数</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;  静态内部类&amp;amp;非静态内部类&amp;amp;局部类&amp;amp;匿名类学习笔记&lt;/p&gt;
    
    </summary>
    
      <category term="java" scheme="http://zhangxiangnan.com/categories/java/"/>
    
    
      <category term="generics" scheme="http://zhangxiangnan.com/tags/generics/"/>
    
      <category term="java" scheme="http://zhangxiangnan.com/tags/java/"/>
    
      <category term="javadoc" scheme="http://zhangxiangnan.com/tags/javadoc/"/>
    
  </entry>
  
  <entry>
    <title>java注解详解</title>
    <link href="http://zhangxiangnan.com/2018/01/05/java/java%E6%B3%A8%E8%A7%A3%E8%AF%A6%E8%A7%A3/"/>
    <id>http://zhangxiangnan.com/2018/01/05/java/java注解详解/</id>
    <published>2018-01-05T11:16:57.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>java注解学习</p><a id="more"></a><p>注解是元数据的一种形式,提供程序相关的数据,但不是程序本身的一部分,注解不会直接操作他们所标注的代码。</p><p>注解有很多用途:</p><pre><code>- 编译器所需的信息,如编译器可以用注解来检测错误或禁止警告- 编译时或发布时处理,如软件工具可以处理注解信息来生成代码、xml文件等- 运行时处理,某些注解在运行时也可以检测到</code></pre><h4 id="注解基本点"><a href="#注解基本点" class="headerlink" title="注解基本点"></a>注解基本点</h4><h5 id="注解的形式"><a href="#注解的形式" class="headerlink" title="注解的形式"></a>注解的形式</h5><p>最简单的:</p><pre><code>@Entity</code></pre><p>@符号后的信息对编译器表明这是一个注解。</p><p>以下注解的名称是Override:</p><pre><code>@Overridevoid mySuperMethod() {  }</code></pre><p>注解也可以包含元素,元素可以有名称,也可以没有名称,他们也有对应的值:</p><pre><code>@Author(   name = &quot;Benjamin Franklin&quot;,   date = &quot;3/27/2003&quot;)class MyClass() { }@SuppressWarnings(value = &quot;unchecked&quot;)void myMethod() { }</code></pre><p>如果只有一个命为value的元素,那么名称可以省略,如:</p><pre><code>@SuppressWarnings(&quot;unchecked&quot;)void myMethod() {}</code></pre><p>如果注解没有元素,那么圆括号可以省略,如@Override注解的例子。</p><p>在相同声明的位置可以使用多个注解:</p><pre><code>@Author(name = &quot;Jane Doe&quot;)@EBookclass MyClass { }</code></pre><p>如果注解有相同的类型,称为可重复注解(&gt;=Jdk8):</p><pre><code>@Author(name = &quot;Jane Doe&quot;)@Author(name = &quot;John Smith&quot;)class MyClass { }</code></pre><p>注解可以使用java.lang或java.lang.annotation包下定义的注解,也可以使用自定义注解。</p><h5 id="注解可以用在哪儿"><a href="#注解可以用在哪儿" class="headerlink" title="注解可以用在哪儿"></a>注解可以用在哪儿</h5><p>注解可以用在声明的地方:</p><pre><code>- 类/接口(含注解类型)/枚举声明- 方法声明- 字段(含枚举常量)声明- 形参声明- 构造函数声明- 本地变量声明- 注解类型声明- 包声明- 类型参数声明- 类型的使用</code></pre><p>使用时注解约定占一行。</p><h5 id="类型注解"><a href="#类型注解" class="headerlink" title="类型注解"></a>类型注解</h5><p>Java8中支持类型使用的注解,称为类型注解,如:<br>实例化对象的表达式:</p><pre><code>new @Interned MyObject();</code></pre><p>类型强转:</p><pre><code>myString = (@NonNull String) str;</code></pre><p>implements子句:</p><pre><code>class UnmodifiableList&lt;T&gt; implements    @Readonly List&lt;@Readonly T&gt; {  }</code></pre><p>异常声明:</p><pre><code>void monitorTemperature() throws    @Critical TemperatureException {  }</code></pre><h4 id="定义注解类型"><a href="#定义注解类型" class="headerlink" title="定义注解类型"></a>定义注解类型</h4><p>注解可以替换注释为代码,设想:</p><pre><code>public class Generation3List extends Generation2List {   // Author: John Doe   // Date: 3/17/2002   // Current revision: 6   // Last modified: 4/12/2004   // By: Jane Doe   // Reviewers: Alice, Bill, Cindy   // class code goes here}</code></pre><p>将这些注释信息如何转化为一个注解?首先必须定一个注解类型,如下:</p><pre><code>@interface ClassPreamble {   String author();   String date();   int currentRevision() default 1;   String lastModified() default &quot;N/A&quot;;   String lastModifiedBy() default &quot;N/A&quot;;   // Note use of array   String[] reviewers();}</code></pre><p>注解类型的定义有点像接口的声明,但是在interface关键字前加@符号。注解是接口的一种形式,其元素的声明像方法,可以指定默认值。</p><pre><code>@ClassPreamble (   author = &quot;John Doe&quot;,   date = &quot;3/17/2002&quot;,   currentRevision = 6,   lastModified = &quot;4/12/2004&quot;,   lastModifiedBy = &quot;Jane Doe&quot;,   // Note array notation   reviewers = {&quot;Alice&quot;, &quot;Bob&quot;, &quot;Cindy&quot;})public class Generation3List extends Generation2List {}</code></pre><p>注意:如果想在Javadoc生成的文档中包含@ClassPreamble中描述的信息,需要在其注解的定义上加@Documented注解。</p><pre><code>@Documented@interface ClassPreamble {}</code></pre><h4 id="已有注解类型"><a href="#已有注解类型" class="headerlink" title="已有注解类型"></a>已有注解类型</h4><p>Java预先定义了一些注解,有些被java编译器使用,有些应用到其他注解中。</p><h5 id="java语言使用的注解"><a href="#java语言使用的注解" class="headerlink" title="java语言使用的注解"></a>java语言使用的注解</h5><p>java.lang定义的注解为:@Deprecated, @Override, and @SuppressWarnings.</p><h6 id="Deprecated"><a href="#Deprecated" class="headerlink" title="@Deprecated"></a>@Deprecated</h6><p>该注解表明被标注的元素已过时,不该再被使用。使用有过时注解标注的方法、类、字段,编译器都会警告。如果元素过时,也应该使用Javadc的过时标记@deprecated来标注,两个注解一个大写,一个小写。</p><pre><code>// Javadoc comment follows /**  * @deprecated  * explanation of why it was deprecated  */ @Deprecated static void deprecatedMethod() { }</code></pre><h6 id="Override"><a href="#Override" class="headerlink" title="@Override"></a>@Override</h6><p>@Override注解通知编译器被标注元素覆盖了父类的元素:</p><pre><code>// 重写方法约定加覆盖注解@Overrideint overriddenMethod() { }</code></pre><p>虽然不强制使用覆盖标记,但是使用了有助于发现错误。</p><h6 id="SuppressWarnings"><a href="#SuppressWarnings" class="headerlink" title="@SuppressWarnings"></a>@SuppressWarnings</h6><p>@SuppressWarnings注解告诉编译器忽略可能产生的某种特定的警告,如下忽略了过时方法调用警告:</p><pre><code>@SuppressWarnings(&quot;deprecation&quot;) void useDeprecatedMethod() {     // deprecation warning     // - suppressed     objectOne.deprecatedMethod(); }</code></pre><p>每一种编译器警告都属于一个分类,java语言规范定义了2中分类:deprecation and unchecked. 想要忽略多种分类的警告,如下:</p><pre><code>@SuppressWarnings({&quot;unchecked&quot;, &quot;deprecation&quot;})</code></pre><h6 id="SafeVarargs"><a href="#SafeVarargs" class="headerlink" title="@SafeVarargs"></a>@SafeVarargs</h6><p>@SafeVarargs注解当使用在方法或构造函数上时,断言了代码不可能对可变参数执行潜在的不安全操作。当使用该注解后,跟可变参数使用相关的unchecked警告会被忽略。</p><h6 id="FunctionalInterface"><a href="#FunctionalInterface" class="headerlink" title="@FunctionalInterface"></a>@FunctionalInterface</h6><p>@FunctionalInterface在java8中引入,表明当前类型声明是一个函数接口。</p><h5 id="应用到其他注解的注解"><a href="#应用到其他注解的注解" class="headerlink" title="应用到其他注解的注解"></a>应用到其他注解的注解</h5><p>这类注解成为元注解, java.lang.annnotation中定义的几个元注解如下:</p><h6 id="Retention"><a href="#Retention" class="headerlink" title="@Retention"></a>@Retention</h6><p>@Retention注解指明了被标记注解如何存储:</p><pre><code>- RetentionPolicy.SOURCE, 被标记注解仅仅在源文件级别保留,编译时被忽略- RetentionPolicy.CLASS ,被标记注解在编译时被编译器保留,但是在JVM运行时被忽略- RetentionPolicy.RUNTIME ,被标记注解被jvm保留,在运行时环境中可以使用</code></pre><h6 id="Documented"><a href="#Documented" class="headerlink" title="@Documented"></a>@Documented</h6><p>@Documented注解表示被标注的注解在使用Javadoc工具生成文档时会被记录到文档中,默认注解是不被记录的。</p><h6 id="Target"><a href="#Target" class="headerlink" title="@Target"></a>@Target</h6><p>@Target注解标记其他注解来限制注解可以应用的java元素,如:</p><pre><code>- ElementType.CONSTRUCTOR 可应用到构造函数上- ElementType.FIELD 可应用到字段或属性上- ElementType.LOCAL_VARIABLE 可应用到本地变量上- ElementType.METHOD 可应用到方法级别的注解上- ElementType.PACKAGE 可应用到包声明上- ElementType.PARAMETER 可应用到方法的参数上- ElementType.TYPE 可应用到类的任何元素上</code></pre><h6 id="Inherited"><a href="#Inherited" class="headerlink" title="@Inherited"></a>@Inherited</h6><p>@Inherited表示注解可以从父类继承,默认不可以。 当用户查询注解类型时,若当前类没有该类型的注解,会接着查询父类的该注解。该注解只可用于类声明上。</p><h6 id="Repeatable"><a href="#Repeatable" class="headerlink" title="@Repeatable"></a>@Repeatable</h6><p>@Repeatable在java8引入,表示被标记注解可以多次应用到相同声明或类型使用</p><h4 id="类型注解-amp-可插拔类型系统"><a href="#类型注解-amp-可插拔类型系统" class="headerlink" title="类型注解&amp;可插拔类型系统"></a>类型注解&amp;可插拔类型系统</h4><p>Jdk8以前,注解只能应用在声明上,jdk8里注解可以应用到任何类型使用上,这意味着可以在你使用一个类型的任何地方使用。<br>类型注解引入是为了提升java程序的分析来提供更强的类型检查,java8不提供类型检查框架,但是它允许我们来使用多个可插拔类型检查框架来配合java编译器进行更强大的类型检查。</p><p>如,想要确保某一个特定的变量不能被赋值为null,来避免NPE异常的话,可以编写一个自定义插件来检查这一点,类似如下:</p><pre><code>@NonNull String str;</code></pre><p>可以加载多种插件来加强类型检查,减少错误。</p><h4 id="可重复注解"><a href="#可重复注解" class="headerlink" title="可重复注解"></a>可重复注解</h4><p>有些情形可能想要使用相同的注解多次,java8引入类型注解。<br>如想要定义多个时间点执行方法,如下:</p><pre><code>@Schedule(dayOfMonth=&quot;last&quot;)@Schedule(dayOfWeek=&quot;Fri&quot;, hour=&quot;23&quot;)public void doPeriodicCleanup() { }</code></pre><p>可重复注解可以在任何使用标准注解的地方使用,如也可在类声明上使用,如:</p><pre><code>@Alert(role=&quot;Manager&quot;)@Alert(role=&quot;Administrator&quot;)public class UnauthorizedAccessException extends SecurityException { }</code></pre><p>为了兼容,可重复注解保存在java编译器自动生成的注解容器里,需要以下两步声明:</p><ul><li><p>一是定义可重复注解:</p><pre><code>import java.lang.annotation.Repeatable;@Repeatable(Schedules.class)public @interface Schedule {  String dayOfMonth() default &quot;first&quot;;  String dayOfWeek() default &quot;Mon&quot;;  int hour() default 12;}</code></pre><p>  @Repeatable元注解圆括号里定义的是注解容器的类型,java编译器用来存储可重复注解。</p><p>  这个例子里,注解容器是Scheudles,所以可重复注解@Schedule存储在@Schedules注解里。</p></li><li><p>二是声明容器枚举类型</p><p>  容器枚举类型必须有一个数组类型的元素,数组的数据类型必须是可重复注解的类型,如:</p><pre><code>public @interface Schedules {    Schedule[] value();}</code></pre></li></ul><h5 id="重新获取注解"><a href="#重新获取注解" class="headerlink" title="重新获取注解"></a>重新获取注解</h5><p>反射API中有几个方法可用来获取注解信息,返回一个单独注解的方法,如AnnotatedElement.getAnnotation(Class<t>)的行为不变,因为仅仅返回请求类型的一个注解(如果存在),如果请求类型存在多个注解,可以先得到其容器注解。</t></p><p>java8也引入其他方法来遍历容器注解来一次获取多个注解,如AnnotatedElement.getAnnotationsByType(Class<t>)。</t></p><h5 id="设计考虑"><a href="#设计考虑" class="headerlink" title="设计考虑"></a>设计考虑</h5><p>当设计注解类型时,必须考虑注解类型的使用次数,因为有了可重复注解,可使用多次;也可以通过@Target元注解来限制注解的使用元素范围。</p><p>总之,要让使用者使用起来尽可能灵活、强大。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;java注解学习&lt;/p&gt;
    
    </summary>
    
      <category term="注解" scheme="http://zhangxiangnan.com/categories/%E6%B3%A8%E8%A7%A3/"/>
    
    
      <category term="注解" scheme="http://zhangxiangnan.com/tags/%E6%B3%A8%E8%A7%A3/"/>
    
  </entry>
  
  <entry>
    <title>JAVA泛型</title>
    <link href="http://zhangxiangnan.com/2017/12/18/java/java%E6%B3%9B%E5%9E%8B/"/>
    <id>http://zhangxiangnan.com/2017/12/18/java/java泛型/</id>
    <published>2017-12-18T12:46:15.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>Java中的泛型详解！<br><a id="more"></a></p><h4 id="为什么用泛型"><a href="#为什么用泛型" class="headerlink" title="为什么用泛型"></a>为什么用泛型</h4><p>泛型支持在定义类、接口、方法时将类型当作参数，称作类型参数/类型变量，和形参类似，形参是输入值不同，类型参数是输入类型不同。</p><ul><li>编译时更强的类型检测，避免更多运行时异常，减少bug</li><li>消除强转</li><li>实现支持不同类型集合的泛型算法，更安全、易懂</li></ul><h4 id="泛型类型"><a href="#泛型类型" class="headerlink" title="泛型类型"></a>泛型类型</h4><p>泛型是类型参数化的泛型类或接口。<br>不用泛型时(可放各种类型，所以运行时强转时容易异常)：</p><pre><code>public class Box {  private Object object;  public void set(Object object) { this.object = object; }  public Object get() { return object; }}</code></pre><p>泛型类的定义：</p><pre><code>class name&lt;T1, T2, ..., Tn&gt; { }</code></pre><p>例子：</p><pre><code>public class Box&lt;T&gt; {    // T stands for &quot;Type&quot;    private T t;    public void set(T t) { this.t = t; }    public T get() { return t; }}  </code></pre><p>类型变量T可以在类中任何地方使用，类型变量可以是任何非基本类型的类型：任意类类型、接口类型、以至另一个类型变量。</p><h5 id="类型参数命名规范"><a href="#类型参数命名规范" class="headerlink" title="类型参数命名规范"></a>类型参数命名规范</h5><p>约定俗成，类型参数单字母、大写，常用的如下：</p><ul><li>E 元素 (java集合框架常用）</li><li>K Key</li><li>N Number</li><li>T Type</li><li>V Value</li><li>S,U,V等</li></ul><h5 id="泛型调用及实例化"><a href="#泛型调用及实例化" class="headerlink" title="泛型调用及实例化"></a>泛型调用及实例化</h5><p>泛型类型的调用(也称参数化类型)：Box<integer> integerBox;<br>实例化：</integer></p><pre><code>Box&lt;Integer&gt; integerBox = new Box&lt;Integer&gt;();Box&lt;Integer&gt; integerBox = new Box&lt;&gt;();//&gt;=jdk7</code></pre><h5 id="参数化类型"><a href="#参数化类型" class="headerlink" title="参数化类型"></a>参数化类型</h5><p>类型参数也可以是参数化类型：</p><pre><code>OrderedPair&lt;String, Box&lt;Integer&gt;&gt; p = new OrderedPair&lt;&gt;(&quot;primes&quot;, new Box&lt;Integer&gt;(...));</code></pre><h4 id="泛型方法"><a href="#泛型方法" class="headerlink" title="泛型方法"></a>泛型方法</h4><p>泛型方法指引入类型参数的方法，在方法的返回类型之前声明，使用&lt;&gt;，可以有多个类型参数，适用于静态、非静态、构造方法。</p><pre><code>public class Util {    public static &lt;K, V&gt; boolean compare(Pair&lt;K, V&gt; p1, Pair&lt;K, V&gt; p2) {        return p1.getKey().equals(p2.getKey()) &amp;&amp;               p1.getValue().equals(p2.getValue());    }}public class Pair&lt;K, V&gt; {private K key;private V value;public Pair(K key, V value) {    this.key = key;    this.value = value;}public void setKey(K key) { this.key = key; }public void setValue(V value) { this.value = value; }public K getKey()   { return key; }public V getValue() { return value; }</code></pre><p>  }</p><pre><code>Pair&lt;Integer, String&gt; p1 = new Pair&lt;&gt;(1, &quot;apple&quot;);Pair&lt;Integer, String&gt; p2 = new Pair&lt;&gt;(2, &quot;pear&quot;);boolean same = Util.&lt;Integer, String&gt;compare(p1, p2);boolean same = Util.compare(p1, p2);//类型推导</code></pre><h4 id="有界（受限）类型参数"><a href="#有界（受限）类型参数" class="headerlink" title="有界（受限）类型参数"></a>有界（受限）类型参数</h4><p>  有些情况想限制类型参数的参数类型为某个类型或及其子类型，用extends表示.<br>  extends此处表示类的继承和接口的实现。</p><pre><code>public &lt;U extends Number&gt; void inspect(U u){      System.out.println(&quot;T: &quot; + t.getClass().getName());      System.out.println(&quot;U: &quot; + u.getClass().getName());}</code></pre><p>有界类型参数可以调用界限类型里定义的方法，如：</p><pre><code>public class NaturalNumber&lt;T extends Integer&gt; {  private T n;  public NaturalNumber(T n)  { this.n = n; }  public boolean isEven() {      return n.intValue() % 2 == 0;  }  // ...}</code></pre><p>界限可以有多个，用&amp;连接，多个界限中最多只能有一个类；如果有一个类，多个接口，则该类必须在最左，如<t extends b1 & b2 b3> （B1为类）</t></p><p>有界类型参数是泛型算法实现的关键，如：</p><pre><code>public interface Comparable&lt;T&gt; {  public int compareTo(T o);}public static &lt;T extends Comparable&lt;T&gt;&gt; int countGreaterThan(T[] anArray, T elem) {    int count = 0;    for (T e : anArray)        if (e.compareTo(elem) &gt; 0)            ++count;    return count;}</code></pre><h4 id="泛型-amp-继承-amp-子类型"><a href="#泛型-amp-继承-amp-子类型" class="headerlink" title="泛型&amp;继承&amp;子类型"></a>泛型&amp;继承&amp;子类型</h4><pre><code>Box&lt;Number&gt; box = new Box&lt;Number&gt;();box.add(new Integer(10));   // OKbox.add(new Double(10.1));  // OKpublic void boxTest(Box&lt;Number&gt; n) { }boxTest(new Box(10))//error,Box&lt;Integer&gt;、Box&lt;Double&gt;都不是Box&lt;Number&gt;的子类型。</code></pre><p>A继承于B，但是不代表Class<a>继承于Class<b></b></a></p><p>泛型的继承可以通过extends或implements来实现：</p><pre><code>ArrayList&lt;String&gt; -&gt; List&lt;String&gt; -&gt; Collection&lt;String&gt;</code></pre><h4 id="类型推导"><a href="#类型推导" class="headerlink" title="类型推导"></a>类型推导</h4><p>Java编译器从方法调用传入的类型以及对应的方法声明的参数类型来推断出使方法调用最合理的参数类型。</p><pre><code>static &lt;T&gt; T pick(T a1, T a2) { return a2; }Serializable s = pick(&quot;d&quot;, new ArrayList&lt;String&gt;())</code></pre><p>有了类型推导，在泛型方法调用、实例化泛型类、泛型类/非泛型类的泛型构造方法调用都可以省略。<br>类型推导只通过调用参数、目标类型、返回值类型来推导，而不是用程序后续的结果类型。</p><h4 id="通配符"><a href="#通配符" class="headerlink" title="通配符"></a>通配符</h4><p>泛型中，问号标记“？”叫通配符，表示一种未知类型，通配符可以用在以下情形：参数的类型、字段、本地变量、返回值；不用于泛型方法调用的类型参数、泛型类的创建、父类型。</p><h5 id="上限通配符"><a href="#上限通配符" class="headerlink" title="上限通配符"></a>上限通配符</h5><p>上限通配符用来放宽变量的限制，比如想定义一个方法，适用于 List<integer>, List<double>, and List<number>类型。</number></double></integer></p><pre><code>public static void process(List&lt;? extends Number&gt;) {}</code></pre><p>List<number>限制所有类型只能为Number，但List&lt;? extends Number&gt;可匹配Number及Number子类型，并且每个元素都可以调用Number类里的方法。</number></p><h5 id="无界通配符"><a href="#无界通配符" class="headerlink" title="无界通配符"></a>无界通配符</h5><p>使用?定义，如List&lt;?&gt;，称作未知类型的list，适用场合：</p><ul><li>如果在写一个可以使用Object类中的功能实现的方法</li><li><p>当代码在泛型中使用不依赖类型参数的方法，如List.size List.clear，实际上Class&lt;?&gt;最常用，因为Class<t>里的多数方法不依俩T。</t></p><p>// 不适用于List<integer>、List<string>等<br>public static void printList(List<object> list) {<br>  for (Object elem : list)</object></string></integer></p><pre><code>System.out.println(elem + &quot; &quot;);</code></pre><p>  System.out.println();<br>}</p><p>// 打印，适合任务类型<br>public static void printList(List&lt;?&gt; list) {<br>  for (Object elem: list)</p><pre><code>System.out.print(elem + &quot; &quot;);</code></pre><p>  System.out.println();<br>}</p></li></ul><p>List&lt;?&gt;与List<object>不一样，List<object>可以往里面添加Object、及Object的任务子类型，但是List&lt;?&gt;声明的变量只能往里添加null。</object></object></p><h5 id="下界通配符"><a href="#下界通配符" class="headerlink" title="下界通配符"></a>下界通配符</h5><p>下届通配符用 ? super Foo 表示只能是Foo或Foo的父类型，不能同时声明下届和上界。</p><p>List<integer>和List&lt;? super Integer&gt;不一样，List<integer>匹配Integer的列表，但是List&lt;? super Integer&gt;匹配Integer及Integer的父类型。</integer></integer></p><h5 id="通配符和子类型"><a href="#通配符和子类型" class="headerlink" title="通配符和子类型"></a>通配符和子类型</h5><p>A extends B，但是Class<a>和Class<b>没有继承关系，如何让两者有关系？</b></a></p><pre><code>List&lt;? extends Integer&gt; intList = new ArrayList&lt;&gt;();List&lt;? extends Number&gt;  numList = intList;  // OK. List&lt;? extends Integer&gt;是List&lt;? extends Number&gt;的子类型</code></pre><p>List&lt;?&gt;是List<integer>和List<number>的父类型。</number></integer></p><p>List<integer> -&gt; List&lt;? extends Integer&gt; -&gt; List&lt;? extends Number&gt; -&gt; List&lt;?&gt;</integer></p><p>List<number> -&gt; List&lt;? super Number&gt; -&gt; List&lt;? super Integer&gt; -&gt; List&lt;?&gt;</number></p><p>List<number> -&gt; List&lt;? extends Number&gt;</number></p><p>List<integer> -&gt; List&lt;? super Integer&gt;</integer></p><h5 id="通配符捕获和辅助方法"><a href="#通配符捕获和辅助方法" class="headerlink" title="通配符捕获和辅助方法"></a>通配符捕获和辅助方法</h5><p>有些情况下，编译器可以捕获到通配符的类型，这种叫通配符捕捉。</p><p>  public class WildcardError {</p><pre><code>void foo(List&lt;?&gt; i) {    i.set(0, i.get(0));//报包含capture of ?的错误  }}</code></pre><p>添加辅助方法来解决：</p><pre><code>public class WildcardFixed {  void foo(List&lt;?&gt; i) {      fooHelper(i);  }  // 创建辅助方法以便可以通过类型推导来进行通配符类型捕获  private &lt;T&gt; void fooHelper(List&lt;T&gt; l) {      l.set(0, l.get(0));  }}</code></pre><h5 id="通配符的使用场合"><a href="#通配符的使用场合" class="headerlink" title="通配符的使用场合"></a>通配符的使用场合</h5><p>何时使用上界通配符、何时使用下界通配符？首先通配符主要用在方法的形式参数声明上，应避免使用在方法返回类型上（调用者需要处理通配符）</p><ul><li>输入变量(In)就是一个提供数据给代码使用的变量，如拷贝方法copy(src, dst)，src就是输入变量，因为src提供了用来拷贝的数据（producer）；</li><li>输出变量(Out)就是一个保存数据以便在其他地方使用的变量，如copy方法的dst变量，数据拷贝到了dst接受了拷贝的数据(consumer)</li><li>又用于输入且用于输出的变量</li></ul><p>使用指南（方法的参数声明）：</p><ul><li>输入变量使用上界通配符，extends，此时变量只读</li><li>输出变量使用下界通配符，super，此时变量只可写</li><li>针对输入变量可以使用Object类里定义的方法访问时，使用无界通配符</li><li>当代码同时需要将变量当作输入、输出变量时，不使用通配符</li></ul><p>以上描述总结出PECS－Producer extends, consumer super.</p><p>List&lt;? extends …&gt;定义的变量可以认为只读，但是不是严格意义的只读，只是说不能修改已有元素、不能新增元素，但是仍可以执行如下：</p><ul><li>clear</li><li>插入null</li><li>迭代与删除</li><li>通过通配符捕捉来写入元素</li></ul><h4 id="类型擦除"><a href="#类型擦除" class="headerlink" title="类型擦除"></a>类型擦除</h4><p>Java引入泛型来加强编译器类型检查以及支持泛型编程，java使用类型擦除来实现泛型：</p><ul><li>替换泛型类型中所有的类型参数为他们的边界类型或者为Object（类型参数无界时）,因为生成的字节码都是普通的类、接口、方法</li><li>如果需要保持类型安全，插入类型转换指令</li><li>生成桥接方法来保持泛型类型继承中的多态性</li></ul><p>java针对参数化类型不会生成新的类，所以泛型不会产生运行时开销。</p><h5 id="泛型类型擦除"><a href="#泛型类型擦除" class="headerlink" title="泛型类型擦除"></a>泛型类型擦除</h5><p>类型擦除时，编译器擦除所有的类型参数，并每个都使用最左边界类型或者Object(无界类型)来替换 ，Node<t>中T是无界通配符。</t></p><h5 id="泛型方法擦除"><a href="#泛型方法擦除" class="headerlink" title="泛型方法擦除"></a>泛型方法擦除</h5><p>泛型方法擦除时，和类型擦除一样。</p><h5 id="桥接方法"><a href="#桥接方法" class="headerlink" title="桥接方法"></a>桥接方法</h5><p>如下：<br>    public class Node<t> {<br>    public T data;<br>    public Node(T data) { this.data = data; }<br>      public void setData(T data) {<br>          System.out.println(“Node.setData”);<br>          this.data = data;<br>      }<br>    }<br>    public class MyNode extends Node<integer> {<br>    public MyNode(Integer data) { super(data); }<br>      public void setData(Integer data) {<br>          System.out.println(“MyNode.setData”);<br>          super.setData(data);<br>      }<br>    }</integer></t></p><p>类型擦除后：</p><pre><code>public class Node {  public Object data;  public Node(Object data) { this.data = data; }  public void setData(Object data) {      System.out.println(&quot;Node.setData&quot;);      this.data = data;  }}public class MyNode extends Node {  public MyNode(Integer data) { super(data); }  public void setData(Integer data) {      System.out.println(&quot;MyNode.setData&quot;);      super.setData(data);  }}</code></pre><p>擦除后setData方法不能覆盖父类的setData方法，为了保持泛型类型在擦除后的多态特性，java编译器自动生成桥接方法如下：</p><pre><code>class MyNode extends Node {  //桥接方法    public void setData(Object data) {        setData((Integer) data);    }    public void setData(Integer data) {        System.out.println(&quot;MyNode.setData&quot;);        super.setData(data);    }}</code></pre><h4 id="泛型的限制"><a href="#泛型的限制" class="headerlink" title="泛型的限制"></a>泛型的限制</h4><h5 id="不能使用基本类型实例化泛型类型"><a href="#不能使用基本类型实例化泛型类型" class="headerlink" title="不能使用基本类型实例化泛型类型"></a>不能使用基本类型实例化泛型类型</h5><pre><code>Pair&lt;int, char&gt; p = new Pair&lt;&gt;(8, &apos;a&apos;);//errorPair&lt;Integer, Character&gt; p = new Pair&lt;&gt;(8, &apos;a&apos;);// 8，&apos;a&apos;会自动装箱</code></pre><h5 id="不能创建类型参数的实例"><a href="#不能创建类型参数的实例" class="headerlink" title="不能创建类型参数的实例"></a>不能创建类型参数的实例</h5><pre><code>public static &lt;E&gt; void append(List&lt;E&gt; list) {  E elem = new E();  // compile-time error  list.add(elem);}public static &lt;E&gt; void append(List&lt;E&gt; list, Class&lt;E&gt; cls) throws Exception {  E elem = cls.newInstance();   // 反射可以实现  list.add(elem);}</code></pre><h5 id="不能声明类型为类型参数的静态字段"><a href="#不能声明类型为类型参数的静态字段" class="headerlink" title="不能声明类型为类型参数的静态字段"></a>不能声明类型为类型参数的静态字段</h5><pre><code>public class MobileDevice&lt;T&gt; {  private static T os;//编译错误}</code></pre><p>因为静态字段是所有类的实例共享，多个实例实例化时传入不同的T类型，则无法确定os字段属于哪一个类型。</p><h5 id="针对参数化类型不能使用cast或instanceof"><a href="#针对参数化类型不能使用cast或instanceof" class="headerlink" title="针对参数化类型不能使用cast或instanceof"></a>针对参数化类型不能使用cast或instanceof</h5><pre><code>public static &lt;E&gt; void rtti(List&lt;E&gt; list) {  if (list instanceof ArrayList&lt;Integer&gt;) { //编译错误  }}public static void rtti(List&lt;?&gt; list) {  if (list instanceof ArrayList&lt;?&gt;) {  // 无界通配符OK; instanceof需要一个具体化的类型  }}List&lt;Integer&gt; li = new ArrayList&lt;&gt;();List&lt;Number&gt;  ln = (List&lt;Number&gt;) li;  //编译错误List&lt;String&gt; l1 = ...;ArrayList&lt;String&gt; l2 = (ArrayList&lt;String&gt;)l1;  // OK</code></pre><h5 id="不能创建参数化的数组类型"><a href="#不能创建参数化的数组类型" class="headerlink" title="不能创建参数化的数组类型"></a>不能创建参数化的数组类型</h5><pre><code>List&lt;Integer&gt;[] arrayOfLists = new List&lt;Integer&gt;[2];  //编译错误，数组声明时需要具体化的类型</code></pre><h5 id="不能创建、捕获、抛出序列化类型的对象"><a href="#不能创建、捕获、抛出序列化类型的对象" class="headerlink" title="不能创建、捕获、抛出序列化类型的对象"></a>不能创建、捕获、抛出序列化类型的对象</h5><pre><code>// 不能隐式地继承Throwableclass MathException&lt;T&gt; extends Exception {  }    // compile-time error// 不能显式地继承Throwableclass QueueFullException&lt;T&gt; extends Throwable { } // compile-time error// 不能创建类型参数的实例public static &lt;T extends Exception, J&gt; void execute(List&lt;J&gt; jobs) {  try {      for (J job : jobs)          // ...  } catch (T e) {   // compile-time error      // ...  }}可以在throws子句中使用类型参数class Parser&lt;T extends Exception&gt; {  public void parse(File file) throws T {     // OK  }}</code></pre><h5 id="形式类型参数擦除后类型一样的不能重载"><a href="#形式类型参数擦除后类型一样的不能重载" class="headerlink" title="形式类型参数擦除后类型一样的不能重载"></a>形式类型参数擦除后类型一样的不能重载</h5><pre><code>public class Example {  public void print(Set&lt;String&gt; strSet) { }  public void print(Set&lt;Integer&gt; intSet) { }}</code></pre><h4 id="非具体化类型"><a href="#非具体化类型" class="headerlink" title="非具体化类型"></a>非具体化类型</h4><p> 具体化类型是指运行时完全可以获取到其类型信息的类型，如基本类型、非泛型类型、裸（原始）类型、或者无界通配符的调用。<br> 非具体化类型是指编译时类型信息被擦除,如泛型类型的调用（无界通配符除外），非具化类型运行时没有足够的类型信息。如JVM运行时无法区分List<string>、List<integer>。非具化类型不能用于：instaceof表达式、或者作为数组的元素。</integer></string></p><h5 id="堆污染"><a href="#堆污染" class="headerlink" title="堆污染"></a>堆污染</h5><p>当参数化类型的变量引用非参数化类型的对象时，就会发生堆污染。在编译或运行时如果无法验证包含参数话类型当操作如转换、调用的正确性，就会报未经检查的警告，需要引起注意。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Java中的泛型详解！&lt;br&gt;
    
    </summary>
    
      <category term="java" scheme="http://zhangxiangnan.com/categories/java/"/>
    
    
      <category term="generics" scheme="http://zhangxiangnan.com/tags/generics/"/>
    
      <category term="java" scheme="http://zhangxiangnan.com/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>JAVA反射之字段&amp;方法&amp;构造函数</title>
    <link href="http://zhangxiangnan.com/2017/04/12/java/JAVA%E5%8F%8D%E5%B0%84%E4%B9%8B%E5%AD%97%E6%AE%B5&amp;%E6%96%B9%E6%B3%95&amp;%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E8%AF%A6%E8%A7%A3/"/>
    <id>http://zhangxiangnan.com/2017/04/12/java/JAVA反射之字段&amp;方法&amp;构造函数详解/</id>
    <published>2017-04-12T12:32:02.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="反射中字段、方法、构造函数的学习"><a href="#反射中字段、方法、构造函数的学习" class="headerlink" title="反射中字段、方法、构造函数的学习"></a>反射中字段、方法、构造函数的学习</h3><a id="more"></a><p>反射定义了一个接口java.lang.reflect.Member，java.lang.reflect.Field、java.lang.reflect.Method、java.lang.reflect.Constructor都实现了该接口。</p><p>注意:根据java7的语言规范，类的成员是类体可继承的组件，包括fields、methods、nested classes、interfaces及枚举类型。由于构造函数不能被继承，他们不是成员。这和java.lang.reflect.Member的实现类不同。</p><h3 id="字段"><a href="#字段" class="headerlink" title="字段"></a>字段</h3><p>字段有一个类型和值，java.lang.reflect.Field类提供了访问类型信息和设置、获取某个对象的某字段值的方法。</p><h4 id="获取字段类型"><a href="#获取字段类型" class="headerlink" title="获取字段类型"></a>获取字段类型</h4><p>一个字段要么是基本类型，要么是引用类型。有8中基本类型：boolean, byte, short, int, long, char, float, and double. 引用类型是直接或间接继承java.lang.Object类的子类，包括接口、数组及枚举类型。</p><p>如下是一个输出不同类型字段的类型及泛华类型的展示：</p><pre><code>  import java.lang.reflect.Field;  import java.util.List;  public class FieldSpy&lt;T&gt; {      public boolean[][] b = {{ false, false }, { true, true } };      public String name  = &quot;Alice&quot;;      public List&lt;Integer&gt; list;      public T val;      public static void main(String... args) {          try {              Class&lt;?&gt; c = Class.forName(args[0]);              Field f = c.getField(args[1]);              System.out.format(&quot;Type: %s%n&quot;, f.getType());              System.out.format(&quot;GenericType: %s%n&quot;, f.getGenericType());          } catch (ClassNotFoundException x) {              x.printStackTrace();          } catch (NoSuchFieldException x) {              x.printStackTrace();          }      }  }$ java FieldSpy FieldSpy bType: class [[ZGenericType: class [[Z$ java FieldSpy FieldSpy nameType: class java.lang.StringGenericType: class java.lang.String$ java FieldSpy FieldSpy listType: interface java.util.ListGenericType: java.util.List&lt;java.lang.Integer&gt;$ java FieldSpy FieldSpy valType: class java.lang.ObjectGenericType: T</code></pre><p>说明:字段b的类型时boolean类型的二维数组，类型名称的语法规则根据Class.getName()。<br>字段val的类型时java.lang.Object，因为泛型的信息会在编译期间擦除泛型的相关信息。T被类型变量的上层限制替代，这里是java.lang.Object.<br>Field.getGenericType()方法会在类文件中查找签名属性（如果存在）。如果签名属性不存在，会返回Field.getType()的值（没有因为引入泛型发生改变）。反射的其他名称为getGenericFoo的方法实现思路类似。</p><h4 id="获取-amp-解析字段的修饰符"><a href="#获取-amp-解析字段的修饰符" class="headerlink" title="获取&amp;解析字段的修饰符"></a>获取&amp;解析字段的修饰符</h4><p>字段声明时允许的几个修饰符：</p><ul><li>访问修饰符: public, protected, and private</li><li>管理运行时行为的修饰符： transient and volatile</li><li>限制到一个实例的修饰符: static</li><li>禁止修改值的修饰符：final</li><li><p>注解<br>Field.getModifiers()方法用来一个整数值，代表该字段声明的修饰符集（一个或多个修饰符），该整数值中位表示的修饰符在java.lang.reflect.Modifier定义。<br>下例展示了如何根据给定的修饰符查找字段，以及判断字段是否是合成（编译器生成的）及是否是枚举常量</p><p>import java.lang.reflect.Field;<br>import java.lang.reflect.Modifier;<br>import static java.lang.System.out;</p><p>enum Spy { BLACK , WHITE }</p><p>public class FieldModifierSpy {</p><pre><code>volatile int share;int instance;class Inner {}public static void main(String... args) {    try {        Class&lt;?&gt; c = Class.forName(args[0]);        int searchMods = 0x0;        for (int i = 1; i &lt; args.length; i++) {              searchMods |= modifierFromString(args[i]);        }        Field[] flds = c.getDeclaredFields();        out.format(&quot;Fields in Class &apos;%s&apos; containing modifiers:  %s%n&quot;,               c.getName(),               Modifier.toString(searchMods));        boolean found = false;        for (Field f : flds) {            int foundMods = f.getModifiers();            // Require all of the requested modifiers to be present            if ((foundMods &amp; searchMods) == searchMods) {                out.format(&quot;%-8s [ synthetic=%-5b enum_constant=%-5b ]%n&quot;,                       f.getName(), f.isSynthetic(),                       f.isEnumConstant());                found = true;            }        }        if (!found) {              out.format(&quot;No matching fields%n&quot;);        }        // production code should handle this exception more gracefully    } catch (ClassNotFoundException x) {        x.printStackTrace();    }}private static int modifierFromString(String s) {    int m = 0x0;    if (&quot;public&quot;.equals(s))           m |= Modifier.PUBLIC;    else if (&quot;protected&quot;.equals(s))   m |= Modifier.PROTECTED;    else if (&quot;private&quot;.equals(s))     m |= Modifier.PRIVATE;    else if (&quot;static&quot;.equals(s))      m |= Modifier.STATIC;    else if (&quot;final&quot;.equals(s))       m |= Modifier.FINAL;    else if (&quot;transient&quot;.equals(s))   m |= Modifier.TRANSIENT;    else if (&quot;volatile&quot;.equals(s))    m |= Modifier.VOLATILE;    return m;}</code></pre><p>}</p></li></ul><pre><code>$ java FieldModifierSpy FieldModifierSpy volatileFields in Class &apos;FieldModifierSpy&apos; containing modifiers:  volatileshare    [ synthetic=false enum_constant=false ]$ java FieldModifierSpy Spy publicFields in Class &apos;Spy&apos; containing modifiers:  publicBLACK    [ synthetic=false enum_constant=true  ]WHITE    [ synthetic=false enum_constant=true  ]$ java FieldModifierSpy FieldModifierSpy\$Inner finalFields in Class &apos;FieldModifierSpy$Inner&apos; containing modifiers:  finalthis$0   [ synthetic=true  enum_constant=false ]$ java FieldModifierSpy Spy private static finalFields in Class &apos;Spy&apos; containing modifiers:  private static final$VALUES  [ synthetic=true  enum_constant=false ]// 枚举类有private static final类型的合成字段$VALUES</code></pre><p>注意编译器会生成一些合成的运行时需要的字段，可使用Field.isSynthetic()来判断是否合成字段，合成的字段根据编译器不同不同。然而内部类引入this$0字段 (即嵌套类为非静态成员类）来持有最外层类的引用；枚举类引入$VALUES字段实现隐式地定义静态方法values().合成的类成员的名字未被指定，不同的编译器实现或不同版本中可能不同。Class.getDeclaredFields()方法会返回包含合成字段的数组，但是Class.getField()方法不会返回，因为合成字段通常不是public的。<br>因为Field字段实现了接口java.lang.reflect.AnnotatedElement，因此运行时能够获取到保留策略为java.lang.annotation.RetentionPolicy.RUNTIME的注解信息。</p><h4 id="设置-amp-获取字段值"><a href="#设置-amp-获取字段值" class="headerlink" title="设置&amp;获取字段值"></a>设置&amp;获取字段值</h4><p>给定某个类的一个实例，是能够用反射来设置类的字段的值。这通常仅在特殊情况下不能够以常规方式设置值。因为这么做破坏了类的设计意图，应该慎用。<br>示例：</p><pre><code>  import java.lang.reflect.Field;  import java.util.Arrays;  import static java.lang.System.out;  enum Tweedle { DEE, DUM }  public class Book {      public long chapters = 0;      public String[] characters = { &quot;Alice&quot;, &quot;White Rabbit&quot; };      public Tweedle twin = Tweedle.DEE;      public static void main(String... args) {          Book book = new Book();          String fmt = &quot;%6S:  %-12s = %s%n&quot;;          try {              Class&lt;?&gt; c = book.getClass();              Field chap = c.getDeclaredField(&quot;chapters&quot;);              out.format(fmt, &quot;before&quot;, &quot;chapters&quot;, book.chapters);                chap.setLong(book, 12);              out.format(fmt, &quot;after&quot;, &quot;chapters&quot;, chap.getLong(book));              Field chars = c.getDeclaredField(&quot;characters&quot;);              out.format(fmt, &quot;before&quot;, &quot;characters&quot;,                     Arrays.asList(book.characters));              String[] newChars = { &quot;Queen&quot;, &quot;King&quot; };              chars.set(book, newChars);              out.format(fmt, &quot;after&quot;, &quot;characters&quot;,                     Arrays.asList(book.characters));              Field t = c.getDeclaredField(&quot;twin&quot;);              out.format(fmt, &quot;before&quot;, &quot;twin&quot;, book.twin);              t.set(book, Tweedle.DUM);              out.format(fmt, &quot;after&quot;, &quot;twin&quot;, t.get(book));              // production code should handle these exceptions more gracefully          } catch (NoSuchFieldException x) {              x.printStackTrace();          } catch (IllegalAccessException x) {              x.printStackTrace();          }      }  }$ java BookBEFORE:  chapters     = 0 AFTER:  chapters     = 12BEFORE:  characters   = [Alice, White Rabbit] AFTER:  characters   = [Queen, King]BEFORE:  twin         = DEE AFTER:  twin         = DUM</code></pre><p>注意：通过反射设置字段的值有一定的性能开销，因为必须进行各种操作，比如访问权限验证。从运行时角度看，效果一样，并且操作就像在代码中直接改变值一样是原子性的。<br>反射的使用会导致丢失一些运行时优化，如下代码很可能被虚拟机优化，但是使用Field.set*()就不会进行优化。</p><pre><code>int x = 1;x = 2;x = 3;</code></pre><h4 id="常见代码错误"><a href="#常见代码错误" class="headerlink" title="常见代码错误"></a>常见代码错误</h4><h5 id="IllegalArgumentException由于不可转换类型（due-to-Inconvertible-Types）"><a href="#IllegalArgumentException由于不可转换类型（due-to-Inconvertible-Types）" class="headerlink" title="IllegalArgumentException由于不可转换类型（due to Inconvertible Types）"></a>IllegalArgumentException由于不可转换类型（due to Inconvertible Types）</h5><p>当使用反射给引用类型的整数赋值基本类型的数值时，就会报该错误。不是用反射的话，编译器会执行自动装箱操作，将基本类型装箱为引用类型，这样类型检查就没问题，但是用反射的话，类型检查只发生在运行时，没有机会去执行装箱操作。</p><pre><code>import java.lang.reflect.Field;public class FieldTrouble {    public Integer val;    public static void main(String... args) {    FieldTrouble ft = new FieldTrouble();    try {        Class&lt;?&gt; c = ft.getClass();        Field f = c.getDeclaredField(&quot;val&quot;);          f.setInt(ft, 42);               // IllegalArgumentException        // production code should handle these exceptions more gracefully    } catch (NoSuchFieldException x) {        x.printStackTrace();     } catch (IllegalAccessException x) {         x.printStackTrace();    }    }}$ java FieldTroubleException in thread &quot;main&quot; java.lang.IllegalArgumentException: Can not set  java.lang.Object field FieldTrouble.val to (long)42        at sun.reflect.UnsafeFieldAccessorImpl.throwSetIllegalArgumentException          (UnsafeFieldAccessorImpl.java:146)        at sun.reflect.UnsafeFieldAccessorImpl.throwSetIllegalArgumentException          (UnsafeFieldAccessorImpl.java:174)        at sun.reflect.UnsafeObjectFieldAccessorImpl.setLong          (UnsafeObjectFieldAccessorImpl.java:102)        at java.lang.reflect.Field.setLong(Field.java:831)        at FieldTrouble.main(FieldTrouble.java:11)</code></pre><p>解决办法：</p><pre><code>f.set(ft, new Integer(43));</code></pre><p>提示：当时使用反射设&amp;获取一个字段的值的时候，编译器没机会来执行装箱操作。编译器只能转换Class.isAssignableFrom()方法的规范描述的相关转换。如下：</p><pre><code>Integer.class.isAssignableFrom(int.class) == false// 反射时引用类型到基本类型不成功int.class.isAssignableFrom(Integer.class) == false// 反射时基本类型到引用类型不成功</code></pre><h5 id="NoSuchFieldException-for-Non-Public-Fields"><a href="#NoSuchFieldException-for-Non-Public-Fields" class="headerlink" title="NoSuchFieldException for Non-Public Fields"></a>NoSuchFieldException for Non-Public Fields</h5><pre><code>$ java FieldSpy java.lang.String countjava.lang.NoSuchFieldException: count        at java.lang.Class.getField(Class.java:1519)        at FieldSpy.main(FieldSpy.java:12)</code></pre><p>提示：Class.getField()及Class.getFields()方法返回class对象代表的类、枚举、接口的公共成员方法。想获取类声明的所有方法（不是继承），使用Class.getDeclaredFields()方法。</p><h5 id="IllegalAccessException-when-Modifying-Final-Fields"><a href="#IllegalAccessException-when-Modifying-Final-Fields" class="headerlink" title="IllegalAccessException when Modifying Final Fields"></a>IllegalAccessException when Modifying Final Fields</h5><p>如果尝试获取&amp;设置私有或其他无法访问的字段的值，或设置final字段的值（不论修饰符是什么）可能会抛出IllegalAccessException异常。</p><pre><code>  import java.lang.reflect.Field;  public class FieldTroubleToo {      public final boolean b = true;      public static void main(String... args) {      FieldTroubleToo ft = new FieldTroubleToo();      try {          Class&lt;?&gt; c = ft.getClass();          Field f = c.getDeclaredField(&quot;b&quot;);  //         f.setAccessible(true);  // solution          f.setBoolean(ft, Boolean.FALSE);   // IllegalAccessException          // production code should handle these exceptions more gracefully      } catch (NoSuchFieldException x) {          x.printStackTrace();      } catch (IllegalArgumentException x) {          x.printStackTrace();      } catch (IllegalAccessException x) {          x.printStackTrace();      }      }  }$ java FieldTroubleToojava.lang.IllegalAccessException: Can not set final boolean field  FieldTroubleToo.b to (boolean)false        at sun.reflect.UnsafeFieldAccessorImpl.          throwFinalFieldIllegalAccessException(UnsafeFieldAccessorImpl.java:55)        at sun.reflect.UnsafeFieldAccessorImpl.          throwFinalFieldIllegalAccessException(UnsafeFieldAccessorImpl.java:63)        at sun.reflect.UnsafeQualifiedBooleanFieldAccessorImpl.setBoolean          (UnsafeQualifiedBooleanFieldAccessorImpl.java:78)        at java.lang.reflect.Field.setBoolean(Field.java:686)        at FieldTroubleToo.main(FieldTroubleToo.java:12)</code></pre><p>提示：class初始化后，存在一个访问限制组织修改final字段值。Field声明为继承自AccessibleObject，提供了方法来抑制此检查。但这会产生副作用；如有时即使值已经被修改，但是程序的其他部分仍可能使用旧值。AccessibleObject.setAccessible()仅在安全上下文允许的情况下才能成功。</p><h3 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h3><p>方法拥有返回值、参数、及可能抛出异常。java.lang.reflect.Method类提供了获取参数和返回值的类型信息的方法，也经常用来执行指定对象的方法。</p><h4 id="获取方法类型信息"><a href="#获取方法类型信息" class="headerlink" title="获取方法类型信息"></a>获取方法类型信息</h4><p>一个字段要么是基本类型要么是引用类型，有8中基本类型：boolean、byte、short、int、long、char、float、double。一个引用类型指直接或间接继承java.lang.Object类包括接口、数组、枚举类型的任意对象。</p><pre><code>  import java.lang.reflect.Field;  import java.util.List;  public class FieldSpy&lt;T&gt; {      public boolean[][] b = {{ false, false }, { true, true } };      public String name  = &quot;Alice&quot;;      public List&lt;Integer&gt; list;      public T val;// 参数化类型      public static void main(String... args) {          try {              Class&lt;?&gt; c = Class.forName(args[0]);              Field f = c.getField(args[1]);              System.out.format(&quot;Type: %s%n&quot;, f.getType());              System.out.format(&quot;GenericType: %s%n&quot;, f.getGenericType());              // production code should handle these exceptions more gracefully          } catch (ClassNotFoundException x) {              x.printStackTrace();          } catch (NoSuchFieldException x) {              x.printStackTrace();          }      }  }$ java FieldSpy FieldSpy bType: class [[ZGenericType: class [[Z$ java FieldSpy FieldSpy nameType: class java.lang.StringGenericType: class java.lang.String$ java FieldSpy FieldSpy listType: interface java.util.ListGenericType: java.util.List&lt;java.lang.Integer&gt;$ java FieldSpy FieldSpy valType: class java.lang.ObjectGenericType: T</code></pre><p>字段b是二维boolean数组，其类型名称的语法在Class.getName()描述。<br>字段val的类型结果是继承自java.lang.Object，因为通过类型擦除实现泛型，在编译期间删除删除有关泛型的信息。所以T被类型变量的上界替换，该例是java.lang.Object.</p><p>Field.getGenericType()在类文件中查找签名属性（如果存在），如果不存在，会降级为Field.getType()（没有因为泛型导致被改变）。反射中其他有类似getGenericFoo方法来获取Foo的某个值的实现都类似。</p><h4 id="获取方法或构造函数参数的名称和其他信息"><a href="#获取方法或构造函数参数的名称和其他信息" class="headerlink" title="获取方法或构造函数参数的名称和其他信息"></a>获取方法或构造函数参数的名称和其他信息</h4><p>可以使用java.lang.reflect.Executable.getParameters方法来获取任何方法或构造函数的形式参数(Method和Constructor类继承了Executable，因此继承了Executable.getParameters方法)然而，.class文件不保存默认不保存形参名称。这是因为许多生成和使用类的工具不希望更大的静态或动态的包含参数名称的类文件占位。尤其，这些工具不得不处理更大的.class类文件，JVM也会使用更多的内存。另外，某些参数名称，如secret、password可能暴露安全敏感的方法信息。</p><p>为了在制定类文件中保存形参名称，这样就能够在反射时拿到这些形参的名称，可以使用-parameters配置javac编译器来编译源文件。</p><pre><code>import java.lang.reflect.`*`;import java.util.function.`*`;import static java.lang.System.out;public class MethodParameterSpy {  private static final String  fmt = &quot;%24s: %s%n&quot;;  &lt;E extends RuntimeException&gt; void genericThrow() throws E {}  public static void printClassConstructors(Class c) {    Constructor[] allConstructors = c.getConstructors();    out.format(fmt, &quot;Number of constructors&quot;, allConstructors.length);    for (Constructor currentConstructor : allConstructors) {        printConstructor(currentConstructor);    }      Constructor[] allDeclConst = c.getDeclaredConstructors();    out.format(fmt, &quot;Number of declared constructors&quot;,        allDeclConst.length);    for (Constructor currentDeclConst : allDeclConst) {        printConstructor(currentDeclConst);    }            }  public static void printClassMethods(Class c) {   Method[] allMethods = c.getDeclaredMethods();    out.format(fmt, &quot;Number of methods&quot;, allMethods.length);    for (Method m : allMethods) {        printMethod(m);    }          }  public static void printConstructor(Constructor c) {    out.format(&quot;%s%n&quot;, c.toGenericString());    Parameter[] params = c.getParameters();    out.format(fmt, &quot;Number of parameters&quot;, params.length);    for (int i = 0; i &lt; params.length; i++) {        printParameter(params[i]);    }  }  public static void printMethod(Method m) {    out.format(&quot;%s%n&quot;, m.toGenericString());    out.format(fmt, &quot;Return type&quot;, m.getReturnType());    out.format(fmt, &quot;Generic return type&quot;, m.getGenericReturnType());    Parameter[] params = m.getParameters();    for (int i = 0; i &lt; params.length; i++) {        printParameter(params[i]);    }  }  public static void printParameter(Parameter p) {    out.format(fmt, &quot;Parameter class&quot;, p.getType());    out.format(fmt, &quot;Parameter name&quot;, p.getName());    out.format(fmt, &quot;Modifiers&quot;, p.getModifiers());    out.format(fmt, &quot;Is implicit?&quot;, p.isImplicit());//    out.format(fmt, &quot;Is name present?&quot;, p.isNamePresent());//参数名字是否存在    out.format(fmt, &quot;Is synthetic?&quot;, p.isSynthetic());//是否自动生成  }  public static void main(String... args) {              try {          printClassConstructors(Class.forName(args[0]));          printClassMethods(Class.forName(args[0]));      } catch (ClassNotFoundException x) {          x.printStackTrace();      }  }}import java.util.`*`;public class ExampleMethods&lt;T&gt; {    public boolean simpleMethod(String stringParam, int intParam) {        System.out.println(&quot;String: &quot; + stringParam + &quot;, integer: &quot; + intParam);        return true;    }    public int varArgsMethod(String... manyStrings) {        return manyStrings.length;    }    public boolean methodWithList(List&lt;String&gt; listParam) {        return listParam.isEmpty();    }    public &lt;T&gt; void genericMethod(T[] a, Collection&lt;T&gt; c) {        System.out.println(&quot;Length of array: &quot; + a.length);        System.out.println(&quot;Size of collection: &quot; + c.size());    }}执行命令：java MethodParameterSpy ExampleMethods输出：Number of constructors: 1Constructor #1public ExampleMethods()Number of declared constructors: 1Declared constructor #1public ExampleMethods()Number of methods: 4Method #1public boolean ExampleMethods.simpleMethod(java.lang.String,int)           Return type: boolean   Generic return type: boolean       Parameter class: class java.lang.String        Parameter name: stringParam             Modifiers: 0          Is implicit?: false      Is name present?: true         Is synthetic?: false       Parameter class: int        Parameter name: intParam             Modifiers: 0          Is implicit?: false      Is name present?: true         Is synthetic?: falseMethod #2public int ExampleMethods.varArgsMethod(java.lang.String...)           Return type: int   Generic return type: int       Parameter class: class [Ljava.lang.String;        Parameter name: manyStrings             Modifiers: 0          Is implicit?: false      Is name present?: true         Is synthetic?: falseMethod #3public boolean ExampleMethods.methodWithList(java.util.List&lt;java.lang.String&gt;)           Return type: boolean   Generic return type: boolean       Parameter class: interface java.util.List        Parameter name: listParam             Modifiers: 0          Is implicit?: false      Is name present?: true         Is synthetic?: falseMethod #4public &lt;T&gt; void ExampleMethods.genericMethod(T[],java.util.Collection&lt;T&gt;)           Return type: void   Generic return type: void       Parameter class: class [Ljava.lang.Object;        Parameter name: a             Modifiers: 0          Is implicit?: false      Is name present?: true         Is synthetic?: false       Parameter class: interface java.util.Collection        Parameter name: c             Modifiers: 0          Is implicit?: false      Is name present?: true         Is synthetic?: false</code></pre><ul><li>getType: 返回参数的声明类型的Class类</li><li><p>getName: 返回参数的名字。如果名字存在，则返回.class类文件返回的名称，否则该方法返回自动生成的名称，形式如argN，N是定义该参数的方法的参数的索引。<br>  例如，假如没有指定-parameters编译配置来编译类文件，则如下输出：</p><pre><code>public boolean ExampleMethods.simpleMethod(java.lang.String,int)         Return type: boolean Generic return type: boolean     Parameter class: class java.lang.String      Parameter name: arg0           Modifiers: 0        Is implicit?: false    Is name present?: false       Is synthetic?: false     Parameter class: int      Parameter name: arg1           Modifiers: 0        Is implicit?: false    Is name present?: false       Is synthetic?: false</code></pre></li><li><p>getModifiers :返回形参具有的各种特征表示的整数，该数值是下列值的和，如果应用于形式参数：</p></li></ul><table><thead><tr><th>Value (in decimal)</th><th>Value (in hexadecimal)</th><th>Description</th></tr></thead><tbody><tr><td>16</td><td>0x0010</td><td>形参定义为final</td></tr><tr><td>4096</td><td>0x1000</td><td>形参是synthetic合成的. 或者可以调用方法isSynthetic.</td></tr><tr><td>32768</td><td>0x8000</td><td>形参在源码中声明为隐式的，或者可以调用isImplicit方法。</td></tr></tbody></table><ul><li><p>isImplicit: 返回源码中声明的参数是否是隐性的</p></li><li><p>isNamePresent: 根据.class类文件来决定形参是否有一个名字，有返回true。</p></li><li>isSynthetic: 如果源码中声明的参数既不是隐性的也不是明确在源码定义的则返回true。</li></ul><h5 id="隐性和合成参数"><a href="#隐性和合成参数" class="headerlink" title="隐性和合成参数"></a>隐性和合成参数</h5><p>  某些构造函数是如果没有被显示声明则会在源码中隐性声明。如ExampleMethods例子无构造函数，一个默认构造函数就会隐性声明。MethodParameterSpy例子打印的隐性声明构造函数如下：</p><pre><code>Number of declared constructors: 1public ExampleMethods()</code></pre><p>考虑如下片段：</p><pre><code>public class MethodParameterExamples {  public class InnerClass { }}</code></pre><p>InnerClass是一个非京台嵌套类或内部类。内部类的有个构造函数也是隐性声明的。然而，该隐性构造包含一个参数，当java编译器编译内部类时，会生成一个类似下面的：</p><pre><code>public class MethodParameterExamples {  public class InnerClass {      final MethodParameterExamples parent;      InnerClass(final MethodParameterExamples this$0) {          parent = this$0;      }  }}</code></pre><p>InnerClass构造包含一个参数，参数类型是包含内部类InnerClass的类，即MethodParameterExamples。参照如下输出:</p><pre><code>public MethodParameterExamples$InnerClass(MethodParameterExamples)       Parameter class: class MethodParameterExamples        Parameter name: this$0             Modifiers: 32784          Is implicit?: true      Is name present?: true         Is synthetic?: false</code></pre><p>因为InnerClass类的构造函数是隐性指定的，所以参数也是隐性的。<br>注意：<br>Java编译器为内部类的构造函数创建一个形式参数，以使编译器能够将创建表达式中的引用（表示直接包含的实例）传递给成员类的构造函数。值32784表示InnerClass构造函数的参数同时是finla（16）和隐性的（32768）。Java语言允许变量名称含有$符号，但是约定来说，在变量名称中不用$符号。Java编译器提供的构造函数如果他们不能对应到源码里明确地或隐含的构造函数，则就是是synthetic合成的，除非他们是类初始化方法。合成的构造函数根据不同的编译器实现而不同。考虑如下：</p><pre><code>public class MethodParameterExamples {  enum Colors {      RED, WHITE;  }}</code></pre><p>Java编译器针对该类会生成几个方法，兼容.class类文件结构，并提供enum构造预期的功能。如，Java编译器会创建一个类文件如下：</p><pre><code>final class Colors extends java.lang.Enum&lt;Colors&gt; {  public final static Colors RED = new Colors(&quot;RED&quot;, 0);  public final static Colors BLUE = new Colors(&quot;WHITE&quot;, 1);  private final static values = new Colors[]{ RED, BLUE };  private Colors(String name, int ordinal) {      super(name, ordinal);  }  public static Colors[] values(){      return values;  }  public static Colors valueOf(String name){      return (Colors)java.lang.Enum.valueOf(Colors.class, name);  }}</code></pre><p>java编译器创建了三个构造和方法，为该枚举构造： Colors(String name, int ordinal), Colors[] values(), and Colors valueOf(String name). 方法values和valueOf是隐性声明的. 因此他们的形参名称也是隐性的。</p><p>枚举的Colors(String name, int ordinal)构造函数是一个默认构造函数，隐性声明。然而，该构造的形参（name和ordinal）则是非隐性声明的，因为这些形参既不是明确的，也不是隐性的，是合成的。 (一个枚举构造的默认构造的形参不是隐性声明的，因为不同的编译器构造的形式要求不一样；另一个java编译器可能指定不同的形参。当编译器编译使用了枚举常量的表达式时，他们仅仅依赖枚举构造的公共静态字段，不依赖构造函数及常量初始化的过程。）<br>因此：</p><pre><code>enum Colors:Number of constructors: 0Number of declared constructors: 1Declared constructor #1private MethodParameterExamples$Colors()       Parameter class: class java.lang.String        Parameter name: $enum$name             Modifiers: 4096          Is implicit?: false      Is name present?: true         Is synthetic?: true       Parameter class: int        Parameter name: $enum$ordinal             Modifiers: 4096          Is implicit?: false      Is name present?: true         Is synthetic?: trueNumber of methods: 2Method #1public static MethodParameterExamples$Colors[]  MethodParameterExamples$Colors.values()           Return type: class [LMethodParameterExamples$Colors;   Generic return type: class [LMethodParameterExamples$Colors;Method #2public static MethodParameterExamples$Colors  MethodParameterExamples$Colors.valueOf(java.lang.String)           Return type: class MethodParameterExamples$Colors   Generic return type: class MethodParameterExamples$Colors       Parameter class: class java.lang.String        Parameter name: name             Modifiers: 32768          Is implicit?: true      Is name present?: true         Is synthetic?: false</code></pre><h4 id="获取-amp-解析方法修饰符"><a href="#获取-amp-解析方法修饰符" class="headerlink" title="获取&amp;解析方法修饰符"></a>获取&amp;解析方法修饰符</h4><p>有几个可能成为方法声明的几个修饰符：</p><ul><li>访问修饰符: public, protected, and private</li><li>限制到一个实例的修饰符： static</li><li>禁止改变值的修饰符：final</li><li>需要覆盖的修饰符： abstract</li><li>组织可重入的修饰符：synchronized</li><li>表示用另一种语言实现的修饰符: native</li><li>强制执行严格的浮点行为: strictfp</li><li>注解：Annotations</li></ul><p>MethodModifierSpy例子展示的指定方法的修饰符，及方法是否是编译器生成（synthetic），是否包含可变参数，是否是桥接方法（编译器生成来支持通用接口).</p><pre><code>  import java.lang.reflect.Method;  import java.lang.reflect.Modifier;  import static java.lang.System.out;  public class MethodModifierSpy {      private static int count;      private static synchronized void inc() { count++; }      private static synchronized int cnt() { return count; }      public static void main(String... args) {          try {              Class&lt;?&gt; c = Class.forName(args[0]);              Method[] allMethods = c.getDeclaredMethods();              for (Method m : allMethods) {              if (!m.getName().equals(args[1])) {                  continue;              }              out.format(&quot;%s%n&quot;, m.toGenericString());              out.format(&quot;  Modifiers:  %s%n&quot;,                     Modifier.toString(m.getModifiers()));              out.format(&quot;  [ synthetic=%-5b var_args=%-5b bridge=%-5b ]%n&quot;,                     m.isSynthetic(), m.isVarArgs(), m.isBridge());              inc();              }              out.format(&quot;%d matching overload%s found%n&quot;, cnt(),                     (cnt() == 1 ? &quot;&quot; : &quot;s&quot;));              // production code should handle this exception more gracefully          } catch (ClassNotFoundException x) {              x.printStackTrace();          }      }  }$ java MethodModifierSpy java.lang.Object waitpublic final void java.lang.Object.wait() throws java.lang.InterruptedException  Modifiers:  public final  [ synthetic=false var_args=false bridge=false ]public final void java.lang.Object.wait(long,int)  throws java.lang.InterruptedException  Modifiers:  public final  [ synthetic=false var_args=false bridge=false ]public final native void java.lang.Object.wait(long)  throws java.lang.InterruptedException  Modifiers:  public final native  [ synthetic=false var_args=false bridge=false ]3 matching overloads found$ java MethodModifierSpy java.lang.StrictMath toRadianspublic static double java.lang.StrictMath.toRadians(double)  Modifiers:  public static strictfp  [ synthetic=false var_args=false bridge=false ]1 matching overload found$ java MethodModifierSpy MethodModifierSpy incprivate synchronized void MethodModifierSpy.inc()  Modifiers: private synchronized  [ synthetic=false var_args=false bridge=false ]1 matching overload found$ java MethodModifierSpy java.lang.Class getConstructorpublic java.lang.reflect.Constructor&lt;T&gt; java.lang.Class.getConstructor  (java.lang.Class&lt;T&gt;[]) throws java.lang.NoSuchMethodException,  java.lang.SecurityException  Modifiers: public transient  [ synthetic=false var_args=true bridge=false ]1 matching overload found$ java MethodModifierSpy java.lang.String compareTopublic int java.lang.String.compareTo(java.lang.String)  Modifiers: public  [ synthetic=false var_args=false bridge=false ]public int java.lang.String.compareTo(java.lang.Object)  Modifiers: public volatile  [ synthetic=true  var_args=false bridge=true  ]  2 matching overloads found</code></pre><p>注意对Class.getConstructor()执行Method.isVarArgs()返回true，这因为该方法声明如下：</p><pre><code>public Constructor&lt;T&gt; getConstructor(Class&lt;?&gt;... parameterTypes)</code></pre><p>而非：</p><pre><code>public Constructor&lt;T&gt; getConstructor(Class&lt;?&gt; [] parameterTypes)</code></pre><p>注意String.compareTo()方法的输出有2个方法，一个是String.java声明的：</p><pre><code>public int compareTo(String anotherString);</code></pre><p>另一个是编译器生成的桥接方法或合成方法。这种情况是因为String事先了参数化泛型接口Comparable。在类型擦除时，继承的方法Comparable.compareTo()的参数类型从java.lang.Object变为java.lang.String。由于Comparable接口中的compareTo方法的参数化类型和String的方法在类型擦除后，不在匹配，不存在覆盖overriding。在所有的情形中，这会产生一个编译错误，因为接口没有被实现。桥接方法的作用就是避免这种问题。</p><p>Method实现了java.lang.reflect.AnnotatedElement，所以保留策略为运行时的注解java.lang.annotation.RetentionPolicy.RUNTIME都能够获取到.</p><h4 id="调用方法"><a href="#调用方法" class="headerlink" title="调用方法"></a>调用方法</h4><p>反射提供了执行类的方法的手段。通常，反射调用方法仅仅当在非反射代码里不可能转化类的实例到指定类型时才是必须的。通过java.lang.reflect.Method.invoke()可以执行方法的调用，第一个参数表示执行哪一个实例的指定方法（如果方法是静态的，第一个参数应该是null），第二个参数表示方法执行所需的参数。如果底层方法抛出异常，会被java.lang.reflect.InvocationTargetException包装。方法的原始异常可以通过异常链机制的InvocationTargetException.getCause()方法获取。</p><h5 id="找到-amp-执行指定声明的方法"><a href="#找到-amp-执行指定声明的方法" class="headerlink" title="找到&amp;执行指定声明的方法"></a>找到&amp;执行指定声明的方法</h5><pre><code>import java.lang.reflect.InvocationTargetException;import java.lang.reflect.Method;import java.lang.reflect.Type;import java.util.Locale;import static java.lang.System.out;import static java.lang.System.err;public class Deet&lt;T&gt; {  private boolean testDeet(Locale l) {      // getISO3Language() may throw a MissingResourceException      out.format(&quot;Locale = %s, ISO Language Code = %s%n&quot;, l.getDisplayName(), l.getISO3Language());      return true;  }  private int testFoo(Locale l) { return 0; }  private boolean testBar() { return true; }  public static void main(String... args) {    if (args.length != 4) {        err.format(&quot;Usage: java Deet &lt;classname&gt; &lt;langauge&gt; &lt;country&gt; &lt;variant&gt;%n&quot;);        return;    }    try {        Class&lt;?&gt; c = Class.forName(args[0]);        Object t = c.newInstance();        Method[] allMethods = c.getDeclaredMethods();        for (Method m : allMethods) {        String mname = m.getName();        if (!mname.startsWith(&quot;test&quot;)            || (m.getGenericReturnType() != boolean.class)) {            continue;        }         Type[] pType = m.getGenericParameterTypes();         if ((pType.length != 1)            || Locale.class.isAssignableFrom(pType[0].getClass())) {             continue;         }        out.format(&quot;invoking %s()%n&quot;, mname);        try {            m.setAccessible(true);            Object o = m.invoke(t, new Locale(args[1], args[2], args[3]));            out.format(&quot;%s() returned %b%n&quot;, mname, (Boolean) o);        // Handle any exceptions thrown by method to be invoked.        } catch (InvocationTargetException x) {            Throwable cause = x.getCause();            err.format(&quot;invocation of %s failed: %s%n&quot;,                   mname, cause.getMessage());        }        }        // production code should handle these exceptions more gracefully    } catch (ClassNotFoundException x) {        x.printStackTrace();    } catch (InstantiationException x) {        x.printStackTrace();    } catch (IllegalAccessException x) {        x.printStackTrace();    }  }}</code></pre><p>Deet invokes getDeclaredMethods() which will return all methods explicitly declared in the class. Also, Class.isAssignableFrom() is used to determine whether the parameters of the located method are compatible with the desired invocation. Technically the code could have tested whether the following statement is true since Locale is final:</p><p>Locale.class == pType[0].getClass()<br>However, Class.isAssignableFrom() is more general.</p><p>$ java Deet Deet ja JP JP<br>invoking testDeet()<br>Locale = Japanese (Japan,JP),<br>ISO Language Code = jpn<br>testDeet() returned true<br>$ java Deet Deet xx XX XX<br>invoking testDeet()<br>invocation of testDeet failed:<br>Couldn’t find 3-letter language code for xx<br>First, note that only testDeet() meets the declaration restrictions enforced by the code. Next, when testDeet() is passed an invalid argument it throws an unchecked java.util.MissingResourceException. In reflection, there is no distinction in the handling of checked versus unchecked exceptions. They are all wrapped in an InvocationTargetException</p><p>Invoking Methods with a Variable Number of Arguments</p><p>Method.invoke() may be used to pass a variable number of arguments to a method. The key concept to understand is that methods of variable arity are implemented as if the variable arguments are packed in an array.</p><p>The InvokeMain example illustrates how to invoke the main() entry point in any class and pass a set of arguments determined at runtime.</p><p>import java.lang.reflect.InvocationTargetException;<br>import java.lang.reflect.Method;<br>import java.util.Arrays;</p><p>public class InvokeMain {<br>    public static void main(String… args) {<br>    try {<br>        Class&lt;?&gt; c = Class.forName(args[0]);<br>        Class[] argTypes = new Class[] { String[].class };<br>        Method main = c.getDeclaredMethod(“main”, argTypes);<br>          String[] mainArgs = Arrays.copyOfRange(args, 1, args.length);<br>        System.out.format(“invoking %s.main()%n”, c.getName());<br>        main.invoke(null, (Object)mainArgs);</p><pre><code>    // production code should handle these exceptions more gracefully} catch (ClassNotFoundException x) {    x.printStackTrace();} catch (NoSuchMethodException x) {    x.printStackTrace();} catch (IllegalAccessException x) {    x.printStackTrace();} catch (InvocationTargetException x) {    x.printStackTrace();}}</code></pre><p>}<br>First, to find the main() method the code searches for a class with the name “main” with a single parameter that is an array of String Since main() is static, null is the first argument to Method.invoke(). The second argument is the array of arguments to be passed.</p><p>$ java InvokeMain Deet Deet ja JP JP<br>invoking Deet.main()<br>invoking testDeet()<br>Locale = Japanese (Japan,JP),<br>ISO Language Code = jpn<br>testDeet() returned true</p><h4 id="Troubleshooting-covers-common-errors-encountered-when-finding-or-invoking-methods"><a href="#Troubleshooting-covers-common-errors-encountered-when-finding-or-invoking-methods" class="headerlink" title="Troubleshooting covers common errors encountered when finding or invoking methods"></a>Troubleshooting covers common errors encountered when finding or invoking methods</h4><h3 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h3><p>The Reflection APIs for constructors are defined in java.lang.reflect.Constructor and are similar to those for methods, with two major exceptions: first, constructors have no return values; second, the invocation of a constructor creates a new instance of an object for a given class.</p><h4 id="Finding-Constructors-illustrates-how-to-retrieve-constructors-with-specific-parameters"><a href="#Finding-Constructors-illustrates-how-to-retrieve-constructors-with-specific-parameters" class="headerlink" title="Finding Constructors illustrates how to retrieve constructors with specific parameters"></a>Finding Constructors illustrates how to retrieve constructors with specific parameters</h4><h4 id="Retrieving-and-Parsing-Constructor-Modifiers-shows-how-to-obtain-the-modifiers-of-a-constructor-declaration-and-other-information-about-the-constructor"><a href="#Retrieving-and-Parsing-Constructor-Modifiers-shows-how-to-obtain-the-modifiers-of-a-constructor-declaration-and-other-information-about-the-constructor" class="headerlink" title="Retrieving and Parsing Constructor Modifiers shows how to obtain the modifiers of a constructor declaration and other information about the constructor"></a>Retrieving and Parsing Constructor Modifiers shows how to obtain the modifiers of a constructor declaration and other information about the constructor</h4><h4 id="Creating-New-Class-Instances-shows-how-to-instantiate-an-instance-of-an-object-by-invoking-its-constructor"><a href="#Creating-New-Class-Instances-shows-how-to-instantiate-an-instance-of-an-object-by-invoking-its-constructor" class="headerlink" title="Creating New Class Instances shows how to instantiate an instance of an object by invoking its constructor"></a>Creating New Class Instances shows how to instantiate an instance of an object by invoking its constructor</h4><h4 id="Troubleshooting-describes-common-errors-which-may-be-encountered-while-finding-or-invoking-constructors"><a href="#Troubleshooting-describes-common-errors-which-may-be-encountered-while-finding-or-invoking-constructors" class="headerlink" title="Troubleshooting describes common errors which may be encountered while finding or invoking constructors"></a>Troubleshooting describes common errors which may be encountered while finding or invoking constructors</h4>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;反射中字段、方法、构造函数的学习&quot;&gt;&lt;a href=&quot;#反射中字段、方法、构造函数的学习&quot; class=&quot;headerlink&quot; title=&quot;反射中字段、方法、构造函数的学习&quot;&gt;&lt;/a&gt;反射中字段、方法、构造函数的学习&lt;/h3&gt;
    
    </summary>
    
      <category term="java" scheme="http://zhangxiangnan.com/categories/java/"/>
    
    
      <category term="java" scheme="http://zhangxiangnan.com/tags/java/"/>
    
      <category term="reflect" scheme="http://zhangxiangnan.com/tags/reflect/"/>
    
  </entry>
  
  <entry>
    <title>JAVA反射介绍及Class详解</title>
    <link href="http://zhangxiangnan.com/2017/04/12/java/JAVA%E5%8F%8D%E5%B0%84%E4%BB%8B%E7%BB%8D%E5%8F%8AClass%E8%AF%A6%E8%A7%A3/"/>
    <id>http://zhangxiangnan.com/2017/04/12/java/JAVA反射介绍及Class详解/</id>
    <published>2017-04-12T12:14:01.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="反射的用途"><a href="#反射的用途" class="headerlink" title="反射的用途"></a>反射的用途</h3><p>  反射对于想要在运行时去检查或修改运行在Java虚拟机中的程序的运行时行为的场合，经常用到。<br>  <a id="more"></a></p><ul><li>可扩展的特征<br>应用程序可以通过使用可扩展对象的全名称创建实例来使用外部用户定义的类</li><li>类浏览器和可视化开发环境<br>一个类浏览器需要能够枚举出类的成员。可视化开发环境可以借助于使用反射提供的类型信息来帮助开发者写正确的代码。</li><li>调试器和测试工具<br>调试器能够检查类的基本成员.测试工具可以利用反射来得到类中定义的API，来确保测试用例的高度覆盖（检查单元测试覆盖率）。</li></ul><h3 id="反射的缺点"><a href="#反射的缺点" class="headerlink" title="反射的缺点"></a>反射的缺点</h3><p>反射很强大，但不能滥用。能不用反射就不用，不得不用反射时需要考虑以下几点：</p><ul><li>性能开销<br>因为反射设计动态解析的类型，所以不能执行某些Java虚拟机优化。因此反射操作的性能比非反射对象的性能更慢，应该在对性能敏感的程序中频繁调用的地方尽量避免使用反射。</li><li>安全限制<br>反射需要一个运行时权限，但运行在安全管理器下该权限可能不存在。对于必须在安全受限的上下文来说，很重要，如Applet</li><li>内部暴露<br>由于反射允许代码执行非反射代码中认为非法的操作，如访问私有字段和方法，此时使用反射能导致意外地副作用，导致代码功能失调、破坏可移植性。同时，反射代码破坏了抽象性，因此可能通过更新平台来改变行为（不懂）</li></ul><h3 id="Class"><a href="#Class" class="headerlink" title="Class"></a>Class</h3><p>每一个对象要么是引用类型要么是基本类型。引用类型全部继承自java.lang.Object。Classes、enums、arrays及interfaces都是引用类型。基本类型只包括8种：boolean、byte、short、long、char、int、float、double。引用类型例如java.lang.String，所有基本类型的包装类型如java.lang.Double，接口java.io.Serializable，及枚举javax.swing.SortOrder。</p><p>针对每一种对象类型，Java虚拟机实例化java.lang.Class的一份不可变实例。Class提供了方法来检查对象的运行时属性，包括成员和类型信息，同时Class也提供了创建新的classes及对象的功能。更重要的是，Class是所有反射API的入口。</p><h4 id="重新获取Class对象"><a href="#重新获取Class对象" class="headerlink" title="重新获取Class对象"></a>重新获取Class对象</h4><p>  所有反射操作的入口是java.lang.Class。除了java.lang.reflect.ReflectPermission类，java.lang.reflect包下的其他类都没有公共构造函数。要获得这些类，有必要调取Class的相应地方法。有几种方法来来获取Class，根据是代码否有权限访问一个对象，类的名称，类型或一个已存在的Class。</p><h5 id="Object-getClass"><a href="#Object-getClass" class="headerlink" title="Object.getClass()"></a>Object.getClass()</h5><p>  如果能取到类的某个实例对象，最简单的获取其Class对象的方式是调用Object.getClass()方法。当然，这种方式只适合于全部继承自Object类的引用类型。如：</p><pre><code>Class c = &quot;foo&quot;.getClass();</code></pre><p>  返回String类的Class对象</p><pre><code>Class c = System.console().getClass();</code></pre><p>  和虚拟机关联的有一个唯一的console对象，通过System.console()对象返回，getClass()返回的是java.io.Console类的Class对象。</p><pre><code>enum E { A, B }Class c = A.getClass();</code></pre><p>  A是枚举类型E的一个实例，getClass()方法返回枚举类型E对应的Class对象</p><pre><code>byte[] bytes = new byte[1024];Class c = bytes.getClass();</code></pre><p>  由于数组是Object对象，所以也能对数组的实例调用getClass()方法。返回的Class对应组件类型为byte的数组</p><pre><code>import java.util.HashSet;import java.util.Set;Set&lt;String&gt; s = new HashSet&lt;String&gt;();Class c = s.getClass();</code></pre><p>  该例中，java.util.Set是java.util.HashSet类型的实例对象实现的一个接口。getClass()返回的Class对应于java.util.HashSet。</p><h5 id="class语法"><a href="#class语法" class="headerlink" title=".class语法"></a>.class语法</h5><p>如果有类型但是没有实例对象，通过追加”.class”到类型的名称后面也可以获取到Class。这也是基本类型最简单的方式获取Class。</p><pre><code>  boolean b;  Class c = b.getClass();   // 编译报错  Class c = boolean.class;  // 正确注意boolean.getClass()会产生编译错误，因为boolean类型时基本类型，不能被引用。.class语法返回类型boolean对应的Class。  Class c = java.io.PrintStream.class;返回java.io.PrintStream类型对应的Class对象  Class c = int[][][].class;The .class也用来获取多维数组类型对应的Class</code></pre><h5 id="Class-forName"><a href="#Class-forName" class="headerlink" title="Class.forName()"></a>Class.forName()</h5><p>如果有一个类的全限定名称，使用静态方法Class.forName()能获取到对应的Class对象。该方式不能用来加载基本类型。</p><pre><code>Class c = Class.forName(&quot;com.duke.MyLocaleServiceProvider&quot;);</code></pre><p>该语句根据传入的全限定名来创建一个class对象</p><pre><code>Class cDoubleArray = Class.forName(&quot;[D&quot;);Class cStringArray = Class.forName(&quot;[[Ljava.lang.String;&quot;);</code></pre><p>如果是数组类型传入的名称，则按下面Class.getName的描述；变量cDoubleArray表示基本类型double数组对应的Class（和double[].class一样），变量cStringArray表示String的二维数组对应的Class（和String[][].class一样）。</p><h5 id="Class-getName-："><a href="#Class-getName-：" class="headerlink" title="Class.getName()："></a>Class.getName()：</h5><p>  返回Class对象代表的实体的字符串名称（类、接口、数组、基本类型或void）</p><ul><li>如果该class对象表示一个引用类型（非数组类型）就依据java语言规范，返回类的二进制名称。</li><li>如果该class对象表示一个基本类型或者void类型，返回对应基本类型或者void的java语言关键字对应的字符串</li><li>如果该class对象表示一个数组类，则名称由元素类型的名称前面追加字符”[“表示数组嵌套的深度，元素类型名称的编码如下：    </li></ul><p>元素类型|编码   </p><table><thead><tr><th>boolean</th><th>Z    </th></tr></thead><tbody><tr><td>byte</td><td>B</td></tr><tr><td>char</td><td>C</td></tr><tr><td>类或接口</td><td>类全限定名称</td></tr><tr><td>double</td><td>D</td></tr><tr><td>float</td><td>F</td></tr><tr><td>int</td><td>I</td></tr><tr><td>long</td><td>J</td></tr><tr><td>short</td><td>S</td></tr></tbody></table><p>类或接口名称就如上述说明的类的二进制名称</p><p>  示例:   </p><table><thead><tr><th>方法调用</th><th>结果    </th></tr></thead><tbody><tr><td>String.class.getName()</td><td>“java.lang.String”</td></tr><tr><td>byte.class.getName()</td><td>“byte”</td></tr><tr><td>(new Object[3]).getClass().getName()</td><td>“[Ljava.lang.Object;”</td></tr><tr><td>(new int[3][4][5][6][7][8][9]).getClass().getName()</td><td>“[[[[[[[I”</td></tr></tbody></table><h5 id="基本类型包装类的TYPE字段"><a href="#基本类型包装类的TYPE字段" class="headerlink" title="基本类型包装类的TYPE字段"></a>基本类型包装类的TYPE字段</h5><p>对于基本类型来说.Class语法获取其Class对象非常方便，也是首选方式；还有另一种方式来获取Class。每一个基本类型和void类型都有一个包装类在java.lang中，将基本类型装箱为包装类型。每一个包装类型有一个TYPE字段，即是基本类型对应的Class对象</p><p>Class c = Double.TYPE;// 相等于double.class<br>Class c = Void.TYPE;//相等于void.class.</p><h5 id="返回Classes的方法"><a href="#返回Classes的方法" class="headerlink" title="返回Classes的方法"></a>返回Classes的方法</h5><p>有几个反射API可以返回classes，但是只有当某个Class已经直接或间接得到才能访问那几个API。    </p><p><strong>Class.getSuperclass()</strong><br>  返回指定class的超类class</p><pre><code>Class c = javax.swing.JButton.class.getSuperclass();// 返回javax.swing.AbstractButton.    </code></pre><p><strong>Class.getClasses()</strong><br>返回指定class的所有的公共classes、接口及枚举成员，包含继承的成员。</p><pre><code>Class&lt;?&gt;[] c = Character.class.getClasses();//返回内部多个公共class成员   </code></pre><p><strong>Class.getDeclaredClasses()</strong><br>返回class里所有显示声明的类、接口、枚举（公共、私有等都包含）</p><pre><code>Class&lt;?&gt;[] c = Character.class.getDeclaredClasses();//返回显示声明的几个类</code></pre><p><strong>Class.getDeclaringClass()</strong><br>如果Class对象表示的接口或类是另一个类的成员，则返回声明该成员的类对应的Class对象；否则如果不是任何其他类的成员则返回null。如果该Class对象表示一个数组类型、基本类型、或void，则返回null。<br>匿名类的声明没有声明类但有一个封闭类    </p><pre><code>public class MyClass {    static Object o = new Object() {        public void m() {}    };    static Class&lt;c&gt; = o.getClass().getDeclaringClass();}</code></pre><p>o定义的匿名类的声明类为null</p><p><strong>Class.getEnclosingClass()</strong><br>返回class的直接封闭类   </p><pre><code>Class c = Thread.State.class().getEnclosingClass();//枚举类Thread.State的封闭类是Threadpublic class MyClass {    static Object o = new Object() {        public void m() {}    };    static Class&lt;c&gt; = o.getClass().getEnclosingClass();}o定义的匿名内部类的封闭类是Mycalss</code></pre><p><strong>java.lang.reflect.Field.getDeclaringClass()</strong><br>返回这些成员被声明的Class。    </p><pre><code>import java.lang.reflect.Field;Field f = System.class.getField(&quot;out&quot;);Class c = f.getDeclaringClass();//返回System,out字段在System类里定义</code></pre><p><strong>java.lang.reflect.Method.getDeclaringClass()</strong><br>返回这些成员被声明的Class。</p><p><strong>java.lang.reflect.Constructor.getDeclaringClass()</strong><br>返回这些成员被声明的Class。    </p><p><strong>Class.getGenericInterfaces()</strong></p><p>返回该对象表示的类或接口直接实现的接口的类型(Type类型)</p><ul><li>如果某个实现的接口是参数化类型，则返回的Type对象必须准确反映出源码中使用的实际类型参数，如果以前没有创建表示每个实现接口的参数化类型，则创建该类型。</li><li>如果该对象是一个类，则返回该类实现的所有的接口数组，顺序按照该对象表示的类实现接口时的顺序对应；对于数组来说，按照Cloneable、Serializable的顺序返回。</li><li>如果该对象是一个接口，返回该接口直接继承的所有接口，顺序按照和接口继承时声明的接口顺序一致。</li><li>如果该对象是类或接口，但是没有实现任何接口，返回空数组。</li><li>如果该对象是基本类型或void，返回空数组。</li></ul><p><strong>Class.getInterfaces()</strong></p><p>返回类型时Class类型</p><ul><li>如果该对象是一个类，则返回该类实现的所有的接口数组，顺序按照该对象表示的类实现接口时的顺序对应；对于数组来说，按照Cloneable、Serializable的顺序返回。</li><li>如果该对象是一个接口，返回该接口直接继承的所有接口，顺序按照和接口继承时声明的接口顺序一致。</li><li>如果该对象是类或接口，但是没有实现任何接口，返回空数组。</li><li>如果该对象是基本类型或void，返回空数组。</li></ul><p><strong>Class.getModifiers()</strong></p><p>返回类或接口的java语言修饰符，加密成一个整数。修饰符包含Java虚拟机为public、protected、private、final、static、abstract、interface的常量，需要使用Modifier的方法来解码</p><ul><li>如果是数组类型，public、private、protected修饰符是和数组的组件类型一致</li><li>如果是基本类型或者void，总是有public修饰符，总无protected及private修饰符。</li><li>如果是数组类型、基本类型或void，总有final修饰符，总是无interface修饰符。其他修饰符不能由此规则决定。</li><li>如果是数组类型，总有abstract修饰符</li></ul><h4 id="检查类的修饰符和类型"><a href="#检查类的修饰符和类型" class="headerlink" title="检查类的修饰符和类型"></a>检查类的修饰符和类型</h4><p>class可以定义一个或多个修饰符来决定运行时行为：</p><ul><li>访问修饰符：public, protected, and private</li><li>需要重写的修饰符：abstract</li><li>限制为某个实例的修饰符：: static</li><li>禁止值被修改的修饰符： final</li><li>强制执行严格的浮点行为的修饰符：strictfp</li><li>注解<br>并不是所有的修饰符可以用在所有类上，如接口不能用final修饰，enum不能用abstract修饰。java.lang.reflect.Modifier包含了所有可能的修饰符的声明，也包含了对Class.getModifiers()方法返回的修饰符标记的解码方法。</li></ul><p>如下例展示了如何获取类的声明的组件类型包括修饰符、泛型参数、实现的接口及继承的父类的路径。如果累实现了java.lang.reflect.AnnotatedElement接口，也能拿到运行时的注解信息<br>    import java.lang.annotation.Annotation;<br>    import java.lang.reflect.Modifier;<br>    import java.lang.reflect.Type;<br>    import java.lang.reflect.TypeVariable;<br>    import java.util.Arrays;<br>    import java.util.ArrayList;<br>    import java.util.List;<br>    import static java.lang.System.out;</p><pre><code>public class ClassDeclarationSpy {    public static void main(String... args) {        try {            Class&lt;?&gt; c = Class.forName(args[0]);//根据类名称得到class对象            out.format(&quot;Class:%n  %s%n%n&quot;, c.getCanonicalName());// 类的规范名称            out.format(&quot;Modifiers:%n  %s%n%n&quot;,                   Modifier.toString(c.getModifiers()));// 输出类的修饰符            out.format(&quot;Type Parameters:%n&quot;);            TypeVariable[] tv = c.getTypeParameters();// 获取类型参数            if (tv.length != 0) {                out.format(&quot;  &quot;);                for (TypeVariable t : tv)                    out.format(&quot;%s &quot;, t.getName());                out.format(&quot;%n%n&quot;);            } else {                  out.format(&quot;  -- No Type Parameters --%n%n&quot;);            }            out.format(&quot;Implemented Interfaces:%n&quot;);            Type[] intfs = c.getGenericInterfaces();// 实现接口            if (intfs.length != 0) {                for (Type intf : intfs)                    out.format(&quot;  %s%n&quot;, intf.toString());                out.format(&quot;%n&quot;);            } else {                  out.format(&quot;  -- No Implemented Interfaces --%n%n&quot;);            }            out.format(&quot;Inheritance Path:%n&quot;);// 继承路径            List&lt;Class&gt; l = new ArrayList&lt;Class&gt;();            printAncestor(c, l);            if (l.size() != 0) {                for (Class&lt;?&gt; cl : l)                    out.format(&quot;  %s%n&quot;, cl.getCanonicalName());                out.format(&quot;%n&quot;);            } else {                  out.format(&quot;  -- No Super Classes --%n%n&quot;);            }            out.format(&quot;Annotations:%n&quot;);            Annotation[] ann = c.getAnnotations();// 注解            if (ann.length != 0) {                for (Annotation a : ann)                    out.format(&quot;  %s%n&quot;, a.toString());                out.format(&quot;%n&quot;);            } else {                  out.format(&quot;  -- No Annotations --%n%n&quot;);            }            // production code should handle this exception more gracefully        } catch (ClassNotFoundException x) {            x.printStackTrace();        }    }    private static void printAncestor(Class&lt;?&gt; c, List&lt;Class&gt; l) {        Class&lt;?&gt; ancestor = c.getSuperclass();// 实现的父类         if (ancestor != null) {            l.add(ancestor);            printAncestor(ancestor, l);         }    }}</code></pre><p>几个不同输入参数下的结果示例如下:</p><pre><code>$ java ClassDeclarationSpy java.util.concurrent.ConcurrentNavigableMapClass:  java.util.concurrent.ConcurrentNavigableMapModifiers:  public abstract interfaceType Parameters:  K VImplemented Interfaces:  java.util.concurrent.ConcurrentMap&lt;K, V&gt;  java.util.NavigableMap&lt;K, V&gt;Inheritance Path:  -- No Super Classes --Annotations:  -- No Annotations --This is the actual declaration for java.util.concurrent.ConcurrentNavigableMap in the source code:public interface ConcurrentNavigableMap&lt;K,V&gt;    extends ConcurrentMap&lt;K,V&gt;, NavigableMap&lt;K,V&gt;注意由于上述例子参数是接口，隐含abstract修饰符。编译器为每个接口添加该修饰符。同样，该接口有2个泛型参数，K、V。K、V具体的额外信息可通过TypeVariable的方法得到。上述说明接口也可以实现接口。$ java ClassDeclarationSpy &quot;[Ljava.lang.String;&quot;Class:  java.lang.String[]Modifiers:  public abstract finalType Parameters:  -- No Type Parameters --Implemented Interfaces:  interface java.lang.Cloneable  interface java.io.SerializableInheritance Path:  java.lang.ObjectAnnotations:  -- No Annotations --由于数组是运行时对象，所有的类型信息都由java虚拟机提供。特别的，数组实现了Cloneable、java.io.Serializable接口，并且数组的直接父类总是Object类。$ java ClassDeclarationSpy java.io.InterruptedIOExceptionClass:  java.io.InterruptedIOExceptionModifiers:  publicType Parameters:  -- No Type Parameters --Implemented Interfaces:  -- No Implemented Interfaces --Inheritance Path:  java.io.IOException  java.lang.Exception  java.lang.Throwable  java.lang.ObjectAnnotations:  -- No Annotations --$ java ClassDeclarationSpy java.security.IdentityClass:  java.security.IdentityModifiers:  public abstractType Parameters:  -- No Type Parameters --Implemented Interfaces:  interface java.security.Principal  interface java.io.SerializableInheritance Path:  java.lang.ObjectAnnotations:  @java.lang.Deprecated()java.security.Identity被注解标记为过时的api，可以用来通过反射代码来监测过时API注意：并不是所有的注解都能通过反射拿到，需要注解的保留策略的类型为Runtime类型（ java.lang.annotation.RetentionPolicy of RUNTIME）的注解才能在运行时拿到。</code></pre><h4 id="获取Class的成员"><a href="#获取Class的成员" class="headerlink" title="获取Class的成员"></a>获取Class的成员</h4><p>Class里提供了2类方法来访问字段、方法、构造函数：枚举所有成员的方法及查找某个指定成员的方法。还有不同的方法来访问类本身直接声明的成员及查找实现的接口或实现类继承下来的成员。</p><p>定义字段的Class方法  </p><table><thead><tr><th>API</th><th>是否枚举成员</th><th>是否包括继承的成员</th><th>是否返回私有成员</th></tr></thead><tbody><tr><td>getDeclaredField()</td><td>no</td><td>no</td><td>yes</td></tr><tr><td>getField()</td><td>no</td><td>yes</td><td>no</td></tr><tr><td>getDeclaredFields()</td><td>yes</td><td>no</td><td>yes</td></tr><tr><td>getFields()</td><td>yes</td><td>yes</td><td>no</td></tr></tbody></table><p>定位方法的Class方法</p><table><thead><tr><th>API</th><th>是否枚举成员</th><th>是否包括继承的成员</th><th>是否返回私有成员</th></tr></thead><tbody><tr><td>getDeclaredMethod()</td><td>no</td><td>no</td><td>yes</td></tr><tr><td>getMethod()</td><td>no</td><td>yes</td><td>no</td></tr><tr><td>getDeclaredMethods()</td><td>yes</td><td>no</td><td>yes</td></tr><tr><td>getMethods()</td><td>yes</td><td>yes</td><td>no</td></tr></tbody></table><p>定位构造函数的方法</p><table><thead><tr><th>API</th><th>是否枚举成员</th><th>是否包括继承的成员</th><th>是否返回私有成员</th></tr></thead><tbody><tr><td>getDeclaredConstructor()</td><td>no</td><td>N/A1</td><td>yes</td></tr><tr><td>getConstructor()</td><td>no</td><td>N/A1</td><td>no</td></tr><tr><td>getDeclaredConstructors()</td><td>yes</td><td>N/A1</td><td>yes</td></tr><tr><td>getConstructors()</td><td>yes</td><td>N/A1</td><td>no</td></tr></tbody></table><p>构造函数不能被继承</p><pre><code>import java.lang.reflect.Constructor;import java.lang.reflect.Field;import java.lang.reflect.Method;import java.lang.reflect.Member;import static java.lang.System.out;enum ClassMember { CONSTRUCTOR, FIELD, METHOD, CLASS, ALL }public class ClassSpy {    public static void main(String... args) {        try {            Class&lt;?&gt; c = Class.forName(args[0]);            out.format(&quot;Class:%n  %s%n%n&quot;, c.getCanonicalName());            Package p = c.getPackage();            out.format(&quot;Package:%n  %s%n%n&quot;,                   (p != null ? p.getName() : &quot;-- No Package --&quot;));            for (int i = 1; i &lt; args.length; i++) {                switch (ClassMember.valueOf(args[i])) {                  case CONSTRUCTOR:                      printMembers(c.getConstructors(), &quot;Constructor&quot;);                      break;                  case FIELD:                      printMembers(c.getFields(), &quot;Fields&quot;);                      break;                  case METHOD:                      printMembers(c.getMethods(), &quot;Methods&quot;);                      break;                  case CLASS:                      printClasses(c);                      break;                  case ALL:                      printMembers(c.getConstructors(), &quot;Constuctors&quot;);                      printMembers(c.getFields(), &quot;Fields&quot;);                      printMembers(c.getMethods(), &quot;Methods&quot;);                      printClasses(c);                      break;                  default:                      assert false;                }            }        } catch (ClassNotFoundException x) {            x.printStackTrace();        }    }    private static void printMembers(Member[] mbrs, String s) {        out.format(&quot;%s:%n&quot;, s);        for (Member mbr : mbrs) {            if (mbr instanceof Field)                out.format(&quot;  %s%n&quot;, ((Field)mbr).toGenericString());            else if (mbr instanceof Constructor)                out.format(&quot;  %s%n&quot;, ((Constructor)mbr).toGenericString());// 返回泛型化String            else if (mbr instanceof Method)                out.format(&quot;  %s%n&quot;, ((Method)mbr).toGenericString());         }        if (mbrs.length == 0)            out.format(&quot;  -- No %s --%n&quot;, s);        out.format(&quot;%n&quot;);    }    private static void printClasses(Class&lt;?&gt; c) {        out.format(&quot;Classes:%n&quot;);        Class&lt;?&gt;[] clss = c.getClasses();        for (Class&lt;?&gt; cls : clss)            out.format(&quot;  %s%n&quot;, cls.getCanonicalName());        if (clss.length == 0)            out.format(&quot;  -- No member interfaces, classes, or enums --%n&quot;);        out.format(&quot;%n&quot;);    }}</code></pre><p>以下是几个示例：</p><pre><code>$ java ClassSpy java.lang.ClassCastException CONSTRUCTORClass:  java.lang.ClassCastExceptionPackage:  java.langConstructor:  public java.lang.ClassCastException()  public java.lang.ClassCastException(java.lang.String)// 构造函数不能被继承$ java ClassSpy java.nio.channels.ReadableByteChannel METHODClass:  java.nio.channels.ReadableByteChannelPackage:  java.nio.channelsMethods:  public abstract int java.nio.channels.ReadableByteChannel.read    (java.nio.ByteBuffer) throws java.io.IOException  // 从实现接口继承的方法  public abstract void java.nio.channels.Channel.close() throws    java.io.IOException  public abstract boolean java.nio.channels.Channel.isOpen()$ java ClassSpy ClassMember FIELD METHODClass:  ClassMemberPackage:  -- No Package --Fields:  // 可以根据 java.lang.reflect.Field.isEnumConstant()来区分枚举字段  public static final ClassMember ClassMember.CONSTRUCTOR  public static final ClassMember ClassMember.FIELD  public static final ClassMember ClassMember.METHOD  public static final ClassMember ClassMember.CLASS  public static final ClassMember ClassMember.ALLMethods:  // 方法名都带有类名，这样可以区分如toString()方法是Enum类的，而不是继承自Object的  public static ClassMember ClassMember.valueOf(java.lang.String)  public static ClassMember[] ClassMember.values()  public final int java.lang.Enum.hashCode()  public final int java.lang.Enum.compareTo(E)  public int java.lang.Enum.compareTo(java.lang.Object)  public final java.lang.String java.lang.Enum.name()  public final boolean java.lang.Enum.equals(java.lang.Object)  public java.lang.String java.lang.Enum.toString()  public static &lt;T&gt; T java.lang.Enum.valueOf    (java.lang.Class&lt;T&gt;,java.lang.String)  public final java.lang.Class&lt;E&gt; java.lang.Enum.getDeclaringClass()  public final int java.lang.Enum.ordinal()  public final native java.lang.Class&lt;?&gt; java.lang.Object.getClass()  public final native void java.lang.Object.wait(long) throws    java.lang.InterruptedException  public final void java.lang.Object.wait(long,int) throws    java.lang.InterruptedException  public final void java.lang.Object.wait() hrows java.lang.InterruptedException  public final native void java.lang.Object.notify()  public final native void java.lang.Object.notifyAll()// 获取字段的声明类if (mbr instanceof Field) {    Field f = (Field)mbr;    out.format(&quot;  %s%n&quot;, f.toGenericString());    out.format(&quot;  -- declared in: %s%n&quot;, f.getDeclaringClass());}</code></pre><h4 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h4><h5 id="编译警告"><a href="#编译警告" class="headerlink" title="编译警告"></a>编译警告</h5><p>可能发生的警告为：”Note: … uses unchecked or unsafe operations”<br>当调用一个方法时，参数值的类型经过校验，很可能经过转化。如下例getMethod()方法引起常见的未收校验的转换警告：</p><pre><code>import java.lang.reflect.Method;public class ClassWarning {    void m() {    try {        Class c = ClassWarning.class;        Method m = c.getMethod(&quot;m&quot;);  // warning        // production code should handle this exception more gracefully    } catch (NoSuchMethodException x) {            x.printStackTrace();        }    }}$ javac ClassWarning.javaNote: ClassWarning.java uses unchecked or unsafe operations.Note: Recompile with -Xlint:unchecked for details.$ javac -Xlint:unchecked ClassWarning.javaClassWarning.java:6: warning: [unchecked] unchecked call to getMethod  (String,Class&lt;?&gt;...) as a member of the raw type ClassMethod m = c.getMethod(&quot;m&quot;);  // warning                      ^</code></pre><p>因为c被声明为原始类型（不带有类型参数），但是getMethod()方法是一个参数化类型，所以发生未经转换的警告<br>有2中解决方法，更倾向于修改c的声明，使其带有合适的泛型类型，该例的声明应该为：</p><pre><code>Class&lt;?&gt; c = warn.getClass();</code></pre><p>或者可以使用@SuppressWarnings来抑制警告</p><pre><code>Class c = ClassWarning.class;@SuppressWarnings(&quot;unchecked&quot;)Method m = c.getMethod(&quot;m&quot;);  </code></pre><p>建议：通常的原则是,警告不应该被忽略， 因为警告可能表明一个bug。参数化声明应该合理使用。如果参数化声明不可能（如和厂商的类库代码交互），则可以使用@SuppressWarnings.</p><h5 id="构造函数不可访问时构造异常"><a href="#构造函数不可访问时构造异常" class="headerlink" title="构造函数不可访问时构造异常"></a>构造函数不可访问时构造异常</h5><p>如果在类的无参构造函数不可访问时，调用Class.newInstance()方法会抛出InstantiationException异常。</p><pre><code>class Cls {    private Cls() {}}public class ClassTrouble {    public static void main(String... args) {    try {        Class&lt;?&gt; c = Class.forName(&quot;Cls&quot;);        c.newInstance();  // InstantiationException    } catch (InstantiationException x) {        x.printStackTrace();    } catch (IllegalAccessException x) {        x.printStackTrace();    } catch (ClassNotFoundException x) {        x.printStackTrace();    }    }}$ java ClassTroublejava.lang.IllegalAccessException: Class ClassTrouble can not access a member of  class Cls with modifiers &quot;private&quot;        at sun.reflect.Reflection.ensureMemberAccess(Reflection.java:65)        at java.lang.Class.newInstance0(Class.java:349)        at java.lang.Class.newInstance(Class.java:308)        at ClassTrouble.main(ClassTrouble.java:9)</code></pre><p>Class.newInstance()的行为和new关键字很类似，会因为new关键失败的相同原因失败。反射里通用的解决方案是利用java.lang.reflect.AccessibleObject类提供的抑制访问控制校验的功能；然而由于java.lang.Class没继承AccessibleObject，所以不会有效。唯一的方案是使使用Constructor.newInstance()方法的类继承AccessibleObject。<br>建议：通常，更倾向于使用Constructor.newInstance()</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;反射的用途&quot;&gt;&lt;a href=&quot;#反射的用途&quot; class=&quot;headerlink&quot; title=&quot;反射的用途&quot;&gt;&lt;/a&gt;反射的用途&lt;/h3&gt;&lt;p&gt;  反射对于想要在运行时去检查或修改运行在Java虚拟机中的程序的运行时行为的场合，经常用到。&lt;br&gt;
    
    </summary>
    
      <category term="java" scheme="http://zhangxiangnan.com/categories/java/"/>
    
    
      <category term="java" scheme="http://zhangxiangnan.com/tags/java/"/>
    
      <category term="reflect" scheme="http://zhangxiangnan.com/tags/reflect/"/>
    
  </entry>
  
  <entry>
    <title>排查CPU使用率始终100%问题</title>
    <link href="http://zhangxiangnan.com/2017/02/24/problems/%E6%8E%92%E6%9F%A5CPU%20load%E8%BF%87%E9%AB%98%E7%9A%84%E7%BA%BF%E4%B8%8A%E9%97%AE%E9%A2%98/"/>
    <id>http://zhangxiangnan.com/2017/02/24/problems/排查CPU load过高的线上问题/</id>
    <published>2017-02-24T10:21:08.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>排查CPU使用率始终100%的线上问题，现象，排查过程，解决方案。<br><a id="more"></a></p><h3 id="现象"><a href="#现象" class="headerlink" title="现象"></a>现象</h3><p>  收到线上机器单个CPU平均load过高的报警，登陆到机器，命令行执行top命令显示如下：   </p><pre><code>[zhangxia@xxxx ~]$ toptop - 17:38:39 up 58 days,  6:38,  2 users,  load average: 0.66, 0.20, 0.19Tasks: 119 total,   2 running, 117 sleeping,   0 stopped,   0 zombieCpu0  : 38.3%us, 59.0%sy,  0.0%ni,  0.3%id,  0.0%wa,  0.0%hi,  2.3%si,  0.0%stCpu1  : 20.7%us, 47.4%sy,  0.0%ni, 31.6%id,  0.0%wa,  0.0%hi,  0.0%si,  0.3%stMem:   1922348k total,  1703128k used,   219220k free,     4300k buffersSwap:  2096440k total,        0k used,  2096440k free,   235564k cachedPID USER      PR  NI  VIRT  RES  SHR S %CPU %MEM    TIME+  COMMAND                                8288 zhangxia  20   0 2628m  29m  11m S 98.1  1.6   0:25.24 java          </code></pre><p>  可以发现进程号为8288的进程CPU使用率为98%，接近100%。</p><h3 id="解决过程"><a href="#解决过程" class="headerlink" title="解决过程"></a>解决过程</h3><h4 id="top找到cpu占用最高的进程号"><a href="#top找到cpu占用最高的进程号" class="headerlink" title="top找到cpu占用最高的进程号"></a>top找到cpu占用最高的进程号</h4><p>  已找到进程号为8288.</p><h4 id="查看已找到的进程中哪个线程CPU使用率最高"><a href="#查看已找到的进程中哪个线程CPU使用率最高" class="headerlink" title="查看已找到的进程中哪个线程CPU使用率最高"></a>查看已找到的进程中哪个线程CPU使用率最高</h4><p>  在top执行的视图里执行shift+H快捷键，提示“Show threads On”，此时展示的PID为线程ID，可以观察到线程id为19234的线程占用CPU最高。或者使用”Top -Hp 进程号“来观察。</p><pre><code>top - 17:47:40 up 58 days,  6:47,  2 users,  load average: 0.71, 0.23, 0.18Tasks: 526 total,   2 running, 524 sleeping,   0 stopped,   0 zombieCpu0  : 25.5%us, 44.4%sy,  0.0%ni,  0.0%id,  0.0%wa,  0.0%hi,  1.9%si, 28.2%stCpu1  : 19.9%us, 41.4%sy,  0.0%ni, 37.5%id,  0.0%wa,  0.0%hi,  0.0%si,  1.3%stMem:   1922348k total,  1707684k used,   214664k free,     5740k buffersSwap:  2096440k total,        0k used,  2096440k free,   237624k cachedPID USER      PR  NI  VIRT  RES  SHR S %CPU %MEM    TIME+  COMMAND                                19234 zhangxia  20   0 2628m  29m  11m R 97.3  1.6   0:26.50 java</code></pre><h5 id="jstack-进程号导出线程堆栈"><a href="#jstack-进程号导出线程堆栈" class="headerlink" title="jstack+进程号导出线程堆栈"></a>jstack+进程号导出线程堆栈</h5><p>  执行导出某个进程内的所有线程堆栈信息到文件：</p><pre><code>jstack 8288 &gt; stack.8288</code></pre><p>  堆栈信息导出到了当前文件夹，为stack.8288。<br>  内容如下：</p><pre><code>  2017-02-24 18:12:51Full thread dump Java HotSpot(TM) 64-Bit Server VM (25.45-b02 mixed mode):&quot;Attach Listener&quot; #9 daemon prio=9 os_prio=0 tid=0x00007f8abc001000 nid=0x4b48 waiting on condition [0x0000000000000000]   java.lang.Thread.State: RUNNABLE&quot;Finalizer&quot; #3 daemon prio=8 os_prio=0 tid=0x00007f8b0407e800 nid=0x4b2a in Object.wait() [0x00007f8af12f9000]   java.lang.Thread.State: WAITING (on object monitor)    at java.lang.Object.wait(Native Method)    - waiting on &lt;0x00000006c6c06e80&gt; (a java.lang.ref.ReferenceQueue$Lock)    at java.lang.ref.ReferenceQueue.remove(ReferenceQueue.java:143)    - locked &lt;0x00000006c6c06e80&gt; (a java.lang.ref.ReferenceQueue$Lock)    at java.lang.ref.ReferenceQueue.remove(ReferenceQueue.java:164)    at java.lang.ref.Finalizer$FinalizerThread.run(Finalizer.java:209)&quot;Reference Handler&quot; #2 daemon prio=10 os_prio=0 tid=0x00007f8b0407c000 nid=0x4b29 in Object.wait() [0x00007f8af13fa000]   java.lang.Thread.State: WAITING (on object monitor)    at java.lang.Object.wait(Native Method)    - waiting on &lt;0x00000006c6c08370&gt; (a java.lang.ref.Reference$Lock)    at java.lang.Object.wait(Object.java:502)    at java.lang.ref.Reference$ReferenceHandler.run(Reference.java:157)    - locked &lt;0x00000006c6c08370&gt; (a java.lang.ref.Reference$Lock)&quot;main&quot; #1 prio=5 os_prio=0 tid=0x00007f8b04009800 nid=0x4b22 runnable [0x00007f8b0a8ae000]   java.lang.Thread.State: RUNNABLE    at java.io.FileOutputStream.writeBytes(Native Method)    at java.io.FileOutputStream.write(FileOutputStream.java:326)    at java.io.BufferedOutputStream.flushBuffer(BufferedOutputStream.java:82)    at java.io.BufferedOutputStream.flush(BufferedOutputStream.java:140)    - locked &lt;0x00000006c6c17678&gt; (a java.io.BufferedOutputStream)    at java.io.PrintStream.write(PrintStream.java:482)    - locked &lt;0x00000006c6c06f30&gt; (a java.io.PrintStream)    at sun.nio.cs.StreamEncoder.writeBytes(StreamEncoder.java:221)    at sun.nio.cs.StreamEncoder.implFlushBuffer(StreamEncoder.java:291)    at sun.nio.cs.StreamEncoder.flushBuffer(StreamEncoder.java:104)    - locked &lt;0x00000006c6c06ee8&gt; (a java.io.OutputStreamWriter)    at java.io.OutputStreamWriter.flushBuffer(OutputStreamWriter.java:185)    at java.io.PrintStream.write(PrintStream.java:527)    - eliminated &lt;0x00000006c6c06f30&gt; (a java.io.PrintStream)    at java.io.PrintStream.print(PrintStream.java:669)    at java.io.PrintStream.println(PrintStream.java:806)    - locked &lt;0x00000006c6c06f30&gt; (a java.io.PrintStream)    at CpuLoadHighTest.main(CpuLoadHighTest.java:4)  &quot;VM Thread&quot; os_prio=0 tid=0x00007f8b04077000 nid=0x4b28 runnable  &quot;GC task thread#0 (ParallelGC)&quot; os_prio=0 tid=0x00007f8b0401e800 nid=0x4b24 runnable  &quot;GC task thread#1 (ParallelGC)&quot; os_prio=0 tid=0x00007f8b04020000 nid=0x4b25 runnable  &quot;GC task thread#2 (ParallelGC)&quot; os_prio=0 tid=0x00007f8b04022000 nid=0x4b26 runnable  &quot;GC task thread#3 (ParallelGC)&quot; os_prio=0 tid=0x00007f8b04023800 nid=0x4b27 runnable  &quot;VM Periodic Task Thread&quot; os_prio=0 tid=0x00007f8b040cc800 nid=0x4b30 waiting on condition  JNI global references: 9</code></pre><h4 id="得到线程号8566的十六进制表示"><a href="#得到线程号8566的十六进制表示" class="headerlink" title="得到线程号8566的十六进制表示"></a>得到线程号8566的十六进制表示</h4><p>  命令行执行如下命令：<br>    printf ‘%x\n’ 19234<br>  得到十六进制表示为4b22</p><h4 id="在堆栈日志中查找线程号十六进制表示为4b22的线程堆栈信息"><a href="#在堆栈日志中查找线程号十六进制表示为4b22的线程堆栈信息" class="headerlink" title="在堆栈日志中查找线程号十六进制表示为4b22的线程堆栈信息"></a>在堆栈日志中查找线程号十六进制表示为4b22的线程堆栈信息</h4><pre><code>在堆栈文件中查找4b22，得到如下线程堆栈信息：  &quot;main&quot; #1 prio=5 os_prio=0 tid=0x00007f8b04009800 nid=0x4b22 runnable [0x00007f8b0a8ae000] java.lang.Thread.State: RUNNABLE  at java.io.FileOutputStream.writeBytes(Native Method)  at java.io.FileOutputStream.write(FileOutputStream.java:326)  at java.io.BufferedOutputStream.flushBuffer(BufferedOutputStream.java:82)  at java.io.BufferedOutputStream.flush(BufferedOutputStream.java:140)  - locked &lt;0x00000006c6c17678&gt; (a java.io.BufferedOutputStream)  at java.io.PrintStream.write(PrintStream.java:482)  - locked &lt;0x00000006c6c06f30&gt; (a java.io.PrintStream)  at sun.nio.cs.StreamEncoder.writeBytes(StreamEncoder.java:221)  at sun.nio.cs.StreamEncoder.implFlushBuffer(StreamEncoder.java:291)  at sun.nio.cs.StreamEncoder.flushBuffer(StreamEncoder.java:104)  - locked &lt;0x00000006c6c06ee8&gt; (a java.io.OutputStreamWriter)  at java.io.OutputStreamWriter.flushBuffer(OutputStreamWriter.java:185)  at java.io.PrintStream.write(PrintStream.java:527)  - eliminated &lt;0x00000006c6c06f30&gt; (a java.io.PrintStream)  at java.io.PrintStream.print(PrintStream.java:669)  at java.io.PrintStream.println(PrintStream.java:806)  - locked &lt;0x00000006c6c06f30&gt; (a java.io.PrintStream)  at CpuLoadHighTest.main(CpuLoadHighTest.java:4)</code></pre><p>根绝CpuLoadHighTest.main(CpuLoadHighTest.java:4)可得到问题出现的具体位置，然后分析是否死循环死锁等问题即可解决。   </p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;排查CPU使用率始终100%的线上问题，现象，排查过程，解决方案。&lt;br&gt;
    
    </summary>
    
      <category term="problems" scheme="http://zhangxiangnan.com/categories/problems/"/>
    
    
      <category term="problems" scheme="http://zhangxiangnan.com/tags/problems/"/>
    
  </entry>
  
  <entry>
    <title>kafka的设计原理解析</title>
    <link href="http://zhangxiangnan.com/2017/02/16/kafka/kafka%E7%9A%84%E8%AE%BE%E8%AE%A1%E5%8E%9F%E7%90%86%E8%A7%A3%E6%9E%90/"/>
    <id>http://zhangxiangnan.com/2017/02/16/kafka/kafka的设计原理解析/</id>
    <published>2017-02-16T08:03:01.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>kafka的设计原理解析，其设计目标，及其为了这个目标作出的设计方案<br><a id="more"></a></p><h3 id="kafka的设计目标"><a href="#kafka的设计目标" class="headerlink" title="kafka的设计目标"></a>kafka的设计目标</h3><p>Kafka 的定位是作为一个统一的处理实时数据流的平台，其必须能够处理一个「大公司」每天可能产生的消息量级。因此，在设计之初 Kafka 就考虑要满足多样的应用场景：</p><ul><li>高吞吐量：必须具有高吞吐量以便支持大容量事件流，如实时日志聚合服务</li><li>加载离线数据：必须能够支持优雅地处理大数据量的backlog积压，以便能够支持来自离线系统的定期数据加载</li><li>低延迟：必须能够处理低延迟的消息传递应用以便支持更多的传统的消息系统的使用场景</li><li>最好能够支持对订阅源的分区、分布式、及实时处理，并产生新的派生订阅源，这促使产生了kafka的分区和消费者模型</li><li>高可用性：在流数据被馈送到其他数据系统用于服务的场景中，需要在机器故障时支持容错</li></ul><p>为了支持上述的场景，设计了一些独特的元素，类似于数据库日志，而非传统的消息系统。</p><h3 id="持久性"><a href="#持久性" class="headerlink" title="持久性"></a>持久性</h3><h4 id="文件系统性能并不差"><a href="#文件系统性能并不差" class="headerlink" title="文件系统性能并不差"></a>文件系统性能并不差</h4><p>kafka在存储和缓存消息上很大程度上依赖了文件系统。有个普遍的观念认为硬盘很慢，这让人觉着基于硬盘的持久性结构设计是不是能提供高性能。实际上恰当设计的硬盘结构通常可以和网络一样快。<br>影响硬盘性能的关键事实是随着过去十年的磁盘寻道耗费的延迟的大幅缩小，硬盘驱动器的吞吐量大幅提升，在具有六个7200rpm SATA RAID-5阵列的JBOD配置上线性写入的性能约为600MB/s，但随机写入性能仅为100k/s，相差超过6000倍。这些现行读取和写入是左右使用模式中最可预测的，操作系统也对此进行了大量优化。现代操作系统提供预读和后写技术，以大块多次预取数据并将较小的逻辑写入分组为大型物理写入。有些情况下的硬盘顺序访问可能比内存的随机访问更快。</p><p><img src="http://deliveryimages.acm.org/10.1145/1570000/1563874/jacobs3.jpg" alt="jacobs3"></p><h4 id="常量级别时间"><a href="#常量级别时间" class="headerlink" title="常量级别时间"></a>常量级别时间</h4><p>消息系统中使用的持久化数据结构通常是跟BTree或者其他通用用途的随机访问数据结构每个消费者一个队列来维护消息的元数据信息。Btrees是可用的最通用的数据结构，并且使得可以在消息队列中支持各种各样的事务和非事务的语义。同时也带来了相当高的成本，Btree操作时间复杂度为O(log N)。通常O(log N)被认为基本上等同于恒定时间，但是这不适用于磁盘操作。磁盘一次寻道需要10ms，并且一次只能寻一个，因此并行化受限。因此即使少量的磁盘也导致很高的开销。</p><p>Kafka 采用 log append 的方式写磁盘，可以为消息的读写操作提供 O(1)的复杂度，并且读写互相不会 block 其他操作。由于读写操作的复杂度和数据量无关，使得 Kafka 可以运行在普通机器组成的集群中，每台 server 所能存储的消息量也非常大。<br>这同时带来了另外一些便利，与其他消息系统在消息被消费后即删除的方式不同，kafka 可以将消息保存较长的一段时间，为 Consumer 带来很大的灵活性。</p><h3 id="IO优化提升效率"><a href="#IO优化提升效率" class="headerlink" title="IO优化提升效率"></a>IO优化提升效率</h3><p>我们已经付出了很大努力来提高效率。 我们的主要用例之一是处理网络活动数据，这是非常高的量：每个页面视图可能会生成几十个写入。 此外，我们假设每个发布的消息由至少一个消费者（通常很多）阅读，因此我们努力使消费尽可能廉价。</p><p>从以往构建和运行类似多个系统的经验看来，我们发现，对于有效地多租户操作效率是关键。如果下游基础设施服务在应用程序使用中由于小的突变很容易成为瓶颈，那么这样的变化总会产生问题。如果性能很快，便有助于确保应用程序不会由于基础设施的小问题而产生故障。这对于在中央集群上运行支持几十或上百的应用程序的集中式服务时特别重要，因为使用方式的变化几乎每天都发生。</p><p>上述讨论消除了不良的磁盘访问模式，对于这类系统仍有两个常见的效率低下的原因：太多的小I/O操作，太多的字节复制操作。<br>小的I/O问题在客户端和服务端以及服务端自身的持久化操作都有发生。<br>为了避免这个问题，Kafka使用的协议构建于一种自然地将消息分组到一起的“消息集”的抽象。这允许网络请求将消息分组到一起，而不是一次发送一条消息，缓冲了网络往返的开销。服务器则一次性将消息块追加到log文件中，客户端也一次性抓取大的连续消息块。</p><p>这个简单的优化产生数量级的速度。 批处理导致更大的网络包，更大的顺序磁盘操作，连续的内存块等等，所有这些都允许Kafka将随机消息写入的突发流转换为流向消费者的线性写入</p><p>另一个效率低下的地方是字节复制。消息比较少时不是问题，但是一旦负载升高，影响就很大。为了避免这个问题，Kafka采取生产者、代理broker服务器、消费者共享的标准二进制格式（这样数据块无需更改便可在三者间传递）。<br>代理服务器维护的消息日志本身就是一个文件组成的目录，每个文件由生产者和消费者共同使用的格式已经写入到磁盘的一系列消息集组成。维护这种通用格式有助于优化最重要的操作：持久日志块的网络传输。现代Unix操作系统提供了高度优化的代码用来将数据从页高速缓冲存储器传输到socket；Linux里则通过sendfile系统调用完成。</p><p>为了理解sendfile的影响，有必要理解数据从文件传递到socket几个常见步骤：</p><ul><li>操作系统将数据从磁盘读入到内核空间的页高速缓存存储器</li><li>应用程序将数据从内核空间读入到用户空间缓存区</li><li>应用程序将数据写会到内核空间中的sokcet缓冲区</li><li><p>操作系统从socket缓冲区复制数据到NIC缓冲区，并通过网络发送</p><p><img src="https://www.ibm.com/developerworks/linux/library/j-zerocopy/figure1.gif" alt="copy"><br>DMA copy:direct memory copy；直接内存访问</p></li></ul><p>很显然，这种方式效率低下，有四次复制操作，两次系统调用。使用sendfile后，通过允许操作系统直接发送页缓存的数据到网络而避免重复拷贝的操作，仅仅只需要最后拷贝到NIC缓冲区的操作。</p><p>对于一个主题多个消费者的常见使用情形，使用上述的零拷贝优化，数据只有一次被拷贝到页缓存，每次消费都重复使用，而不是加载到内存里当每次读取的时候被复制到内核空间。这使得消息被消费的速率取决于网络连接的限制。<br>页缓存和sendfile的结合意味着消费者大多出现的集群上，你可能观察不到任何磁盘活动，因为都是从缓存提供数据。</p><p>更多Java对零拷贝的支持和sendfile的背景，看<a href="http://www.ibm.com/developerworks/linux/library/j-zerocopy" target="_blank" rel="noopener">http://www.ibm.com/developerworks/linux/library/j-zerocopy</a></p><h3 id="端到端的批量消息压缩End-to-end-Batch-Compression"><a href="#端到端的批量消息压缩End-to-end-Batch-Compression" class="headerlink" title="端到端的批量消息压缩End-to-end Batch Compression"></a>端到端的批量消息压缩End-to-end Batch Compression</h3><p>I在其他场景下，CPU、硬盘其实不是瓶颈，而是网络带宽。很常见的就是需要通过广域网在各个数据中心之间发送消息的数据流水线。当然，用户也总是可以一次压缩信息，而不需要Kafka的帮助，但这样的压缩比通常很低，因为大多数的冗余来自于相同类型的消息间的重复而不是单条消息本身(如，JSON中的字段名称，web日志中的用户代理，公共的字符串值. 高效的压缩需要一次压缩多条消息而不是每次只压缩一条消息。</p><p>Kafka允许通过遍历消息集来实现消息批量压缩。多条信息可以放到一起压缩，然后发送压缩后的内容到服务器。这些消息也会按着压缩的格式来写入，并在日志文件中保持压缩格式，并且只会由消费者解压缩。Kafka支持GZIP、Snappy、LZ4压缩协议，更多压缩细节见<a href="https://cwiki.apache.org/confluence/display/KAFKA/Compression" target="_blank" rel="noopener">https://cwiki.apache.org/confluence/display/KAFKA/Compression</a></p><h3 id="生产者"><a href="#生产者" class="headerlink" title="生产者"></a>生产者</h3><h4 id="负载均衡"><a href="#负载均衡" class="headerlink" title="负载均衡"></a>负载均衡</h4><p>生产者直接发送数据到某分区的leader消息代理服务器，而不经过任何中间路由层。为了协助生产者实现这个功能，Kafka各个节点都能随时应答对于那些服务器存活、对于某个主题的所有分区leaders服务器地址的请求，协助生产者发送请求到恰当的服务器。</p><p>客户端控制着发布消息到哪一个分区，这可以通过随机、实现某种负载均衡的随机、某些语义层面的分区函数来实现。Kafka提供了语法上的分区接口，通过允许用户指定一个关键字分区，然后使用该接口hash到一个分区（如果有必要也可以选择重写分区函数）。例如，如果选择用户id作为关键字，这样对于指定用户的所有消息都被发送到相同分区中。 这反过来将允许消费者对他们的消费进行局部性假设。 这种分区风格被明确地设计为允许消费者进行对局部敏感的处理。</p><h4 id="异步发送"><a href="#异步发送" class="headerlink" title="异步发送"></a>异步发送</h4><p>批处理是效率的主要驱动因素之一，为了启用批处理，Kafka生产者将尝试在内存中累积数据，并在单个请求中发送更大的批次。 批处理可以配置为累积不超过固定数量的消息，并且等待不超过一定的固定等待时间（例如64k或10ms）。 这允许累积更多的字节发送，并且在服务器上几乎没有更大的I / O操作。 这种缓冲是可配置的，并且提供了一种机制来折衷少量的额外延迟以获得更好的吞吐量。</p><p>生产者配置见：<a href="https://kafka.apache.org/documentation/#producerconfigs" target="_blank" rel="noopener">https://kafka.apache.org/documentation/#producerconfigs</a><br>api细节：<a href="http://kafka.apache.org/082/javadoc/index.html?org/apache/kafka/clients/producer/KafkaProducer.html" target="_blank" rel="noopener">http://kafka.apache.org/082/javadoc/index.html?org/apache/kafka/clients/producer/KafkaProducer.html</a></p><h3 id="消费者"><a href="#消费者" class="headerlink" title="消费者"></a>消费者</h3><p>Kafka通过向Kafka服务器发送提取请求，来获取它想要消费的消息所在分区的leader地址。消费者在每次请求中指定日志中消息的偏移量，然后获取到一个从那个位置开始的日志块。这样消费者拥有对消费消息位置的控制权，如果有需要还能够重设其值来重复消费数据。</p><h4 id="推或者拉"><a href="#推或者拉" class="headerlink" title="推或者拉"></a>推或者拉</h4><p>kafka考虑的一个问题：是否消费者应该从服务器拉取数据还是服务器应该推送数据到消费者。在这个角度，Kafka遵循了一个更加传统的设计，也是大多数消息系统使用的 ，生产者发送数据到服务器，消费者从服务器拉取数据。一些集中式日志系统，如Scribe、Apache Flume，遵循的是一种非常不同的基于推的数据推送到下游的思路。两种设计各有利弊，基于推的系统不好应对多样化的消费者，因为服务器控制数据传输的速率。而通常的目标则是让消费者以自己最大的消费速率来消费数据；不幸的是，在推送的系统中，当消费速率跟不上生产速率时，消费者往往被压制（类似于消费者被攻击）。一个基于拉的系统有更好的性能，消费者只是简单的落后于生产者，在可能的时候赶上.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;kafka的设计原理解析，其设计目标，及其为了这个目标作出的设计方案&lt;br&gt;
    
    </summary>
    
      <category term="kafka" scheme="http://zhangxiangnan.com/categories/kafka/"/>
    
    
      <category term="kafka" scheme="http://zhangxiangnan.com/tags/kafka/"/>
    
  </entry>
  
  <entry>
    <title>kafka的适用场合</title>
    <link href="http://zhangxiangnan.com/2017/02/14/kafka/kafka%E7%9A%84%E9%80%82%E7%94%A8%E5%9C%BA%E5%90%88/"/>
    <id>http://zhangxiangnan.com/2017/02/14/kafka/kafka的适用场合/</id>
    <published>2017-02-14T08:06:02.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>kafka的适用场合<br><a id="more"></a></p><h3 id="Kafka的常见应用场合"><a href="#Kafka的常见应用场合" class="headerlink" title="Kafka的常见应用场合"></a>Kafka的常见应用场合</h3><h4 id="消息系统"><a href="#消息系统" class="headerlink" title="消息系统"></a>消息系统</h4><p>  Kafka完全可以替换传统消息服务器。使用消息系统有很多原因，解耦消息生产者和处理过程，缓存未处理的消息等。对比大多数消息系统，Kafka有更高的吞吐量，而且基于分片构建，有副本机制，有容错机制，对于构建大规模可扩展的消息处理程序是不错的选择。<br>  在我们的经验中，消息的使用往往相对低延迟，但需要很低的终端到终端的延迟，且经常依赖于Kafka提供强大的持久性保证。</p><p>  在消息系统领域，Kafka相较于传统消息系统如ActiveMq、RabbitMQ。</p><h4 id="网站活动追踪"><a href="#网站活动追踪" class="headerlink" title="网站活动追踪"></a>网站活动追踪</h4><p>  Kafka一开始是用来构建用户行为追踪流水线作为一系列实时的发布订阅模型的订阅源。这就是说网站活动（页面浏览、搜索、其他用户产生的行为动作）按着一个活动类型一个主题被发布到中央主题。这些订阅源可以用来消费用于一系列用途如实时处理、实时监控、加载数据到Hadoop或线下数据仓库系统以便于线下处理和报表。<br>  活动行为追踪常常数据量非常大，因为每一次用户浏览页面都产生许多活动信息。</p><h4 id="Metrics"><a href="#Metrics" class="headerlink" title="Metrics"></a>Metrics</h4><p>  Kafka经常用来分析运行时的监控数据。这涉及聚合来自分布式应用程序的统计数据来产生运行数据的几种馈送。</p><h4 id="日志聚合Log-Aggregation"><a href="#日志聚合Log-Aggregation" class="headerlink" title="日志聚合Log Aggregation"></a>日志聚合Log Aggregation</h4><p>  很多人使用Kafka进行日志聚合的方案。日志聚合通常从服务器收集物理日志文件，并将其集中起来（文件服务器或者可能HDFS）处理。Kafka抽象了文件的细节，并将日志或事件数据更清晰地抽象为消息流。这允许更低延迟地处理，更容易支持多数据源和分布式数据消费。相比于以日志为中心的系统如Scribe或者Flume，Kafka提供了同样出色的性能，更强的持久性保证及更低的端到端延迟。</p><h4 id="流处理Stream-Processing"><a href="#流处理Stream-Processing" class="headerlink" title="流处理Stream Processing"></a>流处理Stream Processing</h4><p>  很多Kafka用户在由多个阶段组成的处理流水线中处理数据，其中原始数据从Kafka主体消费，接着聚合，丰富，或转换到新主题以便于进一步消费或后续处理。如，一个推荐新闻文章的流水线可能先从RSS订阅源抓取文章内容，将其发布到名为“articles”的主题；进一步处理可能是规范化或者对内容去重，然后发布到清理后的内容到一个新主题；最后的处理阶段可能是尝试推荐内容给用户。这样的处理流水线就是基于不同的主题进行实时数据流的处理。从Kafka的0.10.0.0版本处理，一个轻量级但强大的流处理库为Kafka Streams可用来执行上述的数据处理。除了Kafka Streams，也可选择其他开源流处理工具如Apache Storm和Apache Samza。</p><h4 id="事件源Event-Sourcing"><a href="#事件源Event-Sourcing" class="headerlink" title="事件源Event Sourcing"></a>事件源Event Sourcing</h4><p>  事件源是一种应用程序设计风格，其中状态变化记录为按时间排序的记录序列。Kafka支持非常大的存储日志数据，是构建于这种风格的应用程序极好的后端。</p><h4 id="提交日志Commit-Log"><a href="#提交日志Commit-Log" class="headerlink" title="提交日志Commit Log"></a>提交日志Commit Log</h4><p>  Kafka可以作为分布式系统的一种外部提交日志。该日志有助于各节点间备份数据，以及故障节点重新恢复数据时充当重新同步机制。Kafka的日志压缩特性有助于支持该用途。在这点上，Kafka类似于Apache的Zookeeper项目的作用。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;kafka的适用场合&lt;br&gt;
    
    </summary>
    
      <category term="译" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/"/>
    
      <category term="kafka" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/kafka/"/>
    
    
      <category term="kafka" scheme="http://zhangxiangnan.com/tags/kafka/"/>
    
  </entry>
  
  <entry>
    <title>求2个单链表的交点</title>
    <link href="http://zhangxiangnan.com/2017/01/22/leetcode/%E6%B1%822%E4%B8%AA%E5%8D%95%E9%93%BE%E8%A1%A8%E7%9A%84%E4%BA%A4%E7%82%B9/"/>
    <id>http://zhangxiangnan.com/2017/01/22/leetcode/求2个单链表的交点/</id>
    <published>2017-01-22T07:21:01.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>求2个单链表的交点（思考过程及源码）<br><a id="more"></a></p><h3 id="求2个单链表的交点"><a href="#求2个单链表的交点" class="headerlink" title="求2个单链表的交点"></a>求2个单链表的交点</h3><p>160.Intersection of Two Linked Lists   </p><p><a href="https://leetcode.com/problems/intersection-of-two-linked-lists/?tab=Description" target="_blank" rel="noopener">https://leetcode.com/problems/intersection-of-two-linked-lists/?tab=Description</a></p><p>Write a program to find the node at which the intersection of two singly linked lists begins.</p><p>For example, the following two linked lists:</p><pre><code>A:          a1 → a2                   ↘                     c1 → c2 → c3                   ↗            B:     b1 → b2 → b3</code></pre><p>begin to intersect at node c1.</p><p>Notes:</p><ul><li>If the two linked lists have no intersection at all, return null.</li><li>The linked lists must retain their original structure after the function returns.</li><li>You may assume there are no cycles anywhere in the entire linked structure.</li><li>Your code should preferably run in O(n) time and use only O(1) memory.</li></ul><h4 id="理解题意"><a href="#理解题意" class="headerlink" title="理解题意"></a>理解题意</h4><p>  写一个程序来找到两个单链表相交开始的结点。<br>  例如，如下两个链表：    </p><pre><code>A:          a1 → a2                   ↘                     c1 → c2 → c3                   ↗            B:     b1 → b2 → b3</code></pre><p>  A、B两个链表相交在c1结点。   </p><p>  注意：</p><ul><li>如果两个链表没有交点，直接返回null</li><li>原有链表必须保持原始结构，即函数无副作用</li><li>可假设整个链表结构中没有环</li><li>代码的时间和空间复杂度应该为：O(n)，O(1)</li></ul><h4 id="实现思路一"><a href="#实现思路一" class="headerlink" title="实现思路一"></a>实现思路一</h4><p>  1、首先考虑特殊情况，A为null、或B为null，或者两者都为null，则应该返回null<br>  2、一般情况下，连个链表都不为空，假设相交于c1点，借用下图：</p><pre><code>A:          a1 → a2                   ↘                     c1 → c2 → c3                   ↗            B:     b1 → b2 → b3</code></pre><ul><li>梳理已知变量，我们只知道两个链表头结点a1、b1，不知道链表长度，对于链表的操作一般操作只有按顺序遍历，反转等；另外我们可以控制遍历时的速度，一次遍历一个结点还是2个结点，但我们对于A、B两个链表不清楚长度有倍数关系，所以两个链表都单步遍历，一次遍历一个结点（即两者速度一样，则相同时间里行驶的距离一样，即经过的结点数一样）。    </li><li>那我们就遍历，假设A、B的长度相等，因为2个链表已假设肯定相交，也即交点之前的两个链表的长度相等，两个链表同时进行遍历时判断遍历的节点是否相等就可以找到交点</li><li>那对于A、B交点之前的长度不相等的情况即A、B两个链表长度不相等的情况呢？没有思路，先假设c1之前A链表的长度为a,c1之前B链表的长度为b，两者相交后共同的部分长度为c，那么还是让两个指针pa、pb从A、B的头结点均单步遍历，看看会发生什么。pa遍历到末尾c3的时候，pb遍历到不知哪个结点，只知道pb走了pa+c距离，即pa走的长度，还有pb+c-(pa+c)=pb-pa的长度就走到末尾，此时两者行驶的距离一样。</li><li>但是我们想要的是pa、pb同时能相聚于c1点，现在pa已经到达c3点，pb还有pb-pa个点要走，想着此时pa等着pb，一直等到pb达到末尾，但貌似没啥用，又要重新开始吗难道？那就不让pa等，两个都接着走，这样pb到终点时，pa又走了pb-pa的距离（假设走的虚拟节点）。</li><li><p>此时pb到达终点c3，pa到终点c3后又多走了pb-pa距离，此时观察下子，发现pb-(pb-pa)=pa，即要是pa到终点后，从b1开始走，走了pb-pa距离，此时pb到终点，而此时pa在B链表上还差pa的距离即达到c1，pb刚好位于终点也是差了pa距离。这时，两个继续走，就能相交于c1.</p><p>以上思路是相交的，针对不相交的，同样逻辑判断，若最后没找到那个点即不想交。</p></li></ul><h4 id="实现思路二虚线延长法"><a href="#实现思路二虚线延长法" class="headerlink" title="实现思路二虚线延长法"></a>实现思路二虚线延长法</h4><pre><code>A:          a1 → a2                 a1 → a2                   ↘              ↗                     c1 → c2 → c3                   ↗              ↘         B:     b1 → b2 → b3                b1 → b2 → b3</code></pre><ul><li>单步遍历两个链表（想不到理由会不同速率遍历，因为不知道长度差异，所以就单步试试），速率相同，时间可以自由控制，也就是我们可以决定两个遍历遍历节点数相同还是差异一定倍数。</li><li>那假设时间一样吧，因为也想不到为啥时间弄成不一样。指针pa从从a1出发经过c1到c3后，接着走b1到b3；pb从b1出发，经过c1到c3后，继续走a1到a2.<br>此时，pa、pb是同时到达b3、a2的，因为两者速率一样，时间一样，最后经过节点数肯定一样。</li><li><p>c3之后的两个分支是虚拟的，但是我们知道a1和b1两个头结点，若pa、pb上面不是走的虚拟分支，而是交换头结点后继续走，那么会同时到达b3和a2结点，也会同时到c1点，解决。</p><p>考虑两个链表没有相交，仍按照上述逻辑交换，执行到最后时，pa、pb的next结点都为null。</p></li></ul><h4 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h4><pre><code>`/*** Definition for singly-linked list.* public class ListNode {*     int val;*     ListNode next;*     ListNode(int x) { val = x; }* }*/`public ListNode getIntersectionNode(ListNode headA, ListNode headB) {    if (headA == null || headB == null) return null;    ListNode pa = headA;    ListNode pb = headB;    boolean swapa = false;// 记录pa是否交换执行到pb    boolean swapb = false;// 记录pb是否交换执行到pa    while (pa != null &amp;&amp; pb != null) {        if (pa == pb) {            return pa;        } else {            pa = pa.next;            pb = pb.next;            if (pa == null &amp;&amp; !swapa) {                pa = headB;                swapa = true;            }            if (pb == null &amp;&amp; !swapb) {                pb = headA;                swapb = true;            }        }    }    return null;}</code></pre><p>  改进：可以省去标记变量，对于不相交的情况最后两者都会等于null，从而退出循环   </p><pre><code>public class Solution {    public ListNode getIntersectionNode(ListNode headA, ListNode headB) {        if (headA == null || headB == null) return null;        ListNode pa = headA;        ListNode pb = headB;        while (a != b) {            a = a != null ? a.next : headB;            b = b != null ? b.next : headA;        }        return a;    }}</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;求2个单链表的交点（思考过程及源码）&lt;br&gt;
    
    </summary>
    
      <category term="algorithm" scheme="http://zhangxiangnan.com/categories/algorithm/"/>
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/"/>
    
      <category term="linked list" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/linked-list/"/>
    
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>求2个数字之和</title>
    <link href="http://zhangxiangnan.com/2017/01/18/leetcode/%E6%B1%822%E4%B8%AA%E6%95%B0%E5%AD%97%E4%B9%8B%E5%92%8C/"/>
    <id>http://zhangxiangnan.com/2017/01/18/leetcode/求2个数字之和/</id>
    <published>2017-01-18T09:48:01.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>求2个由单链表表示的数字之和<br><a id="more"></a></p><h3 id="Add-Two-Numbers-两数之和"><a href="#Add-Two-Numbers-两数之和" class="headerlink" title="Add Two Numbers-两数之和"></a>Add Two Numbers-两数之和</h3><p>链接：<a href="https://leetcode.com/problems/add-two-numbers/" target="_blank" rel="noopener">https://leetcode.com/problems/add-two-numbers/</a><br>原题：<br>You are given two non-empty linked lists representing two non-negative integers.<br>The digits are stored in reverse order and each of their nodes contain a single digit.<br>Add the two numbers and return it as a linked list.</p><p>You may assume the two numbers do not contain any leading zero, except the number 0 itself.</p><p>Input: (2 -&gt; 4 -&gt; 3) + (5 -&gt; 6 -&gt; 4)<br>Output: 7 -&gt; 0 -&gt; 8</p><p>单链表定义:</p><pre><code>`/*** Definition for singly-linked list.* public class ListNode {*     int val;*     ListNode next;*     ListNode(int x) { val = x; }* }*/`</code></pre><h4 id="翻译"><a href="#翻译" class="headerlink" title="翻译"></a>翻译</h4><p>给定2个非空单链表来表示2个非负整数，每个数字的每一位在链表中以倒序存储（即链表头结点表示个位数，以此类推），每一个结点包含一个单独的数字。<br>求出2数之和，并以链表形式返回（倒序存储，即链表头结点表示个位数，依次类推）。<br>示例：<br>输入： (2 -&gt; 4 -&gt; 3) + (5 -&gt; 6 -&gt; 4)<br>输出：7 -&gt; 0 -&gt; 8</p><h4 id="题意分析"><a href="#题意分析" class="headerlink" title="题意分析"></a>题意分析</h4><p>1、和一般2数求和稍微不同，无非就是2个数字用链表表示，链表头结点表示个位数，依次类推。<br>2、正常数字求和无非就是2个数的个位数与个数数相加，得到数字大于9，则需要往十位进位（进位的值为个位数的和/10,整除）。<br>和的个位数值则为个数为和 % 10（对10取余），依次类推。使用链表表示的数字求和原理类似。<br>3、考虑几个特殊情况，若一个数字有2位，一个有3位，此时3位数字的百位数字仍然要和0相加，并加上来自十分位的进位值；<br>若2-&gt;9与8-&gt;1相加呢，和为三位数，其百位值为十位进位得到，需要考虑到进位值所产生的新的结点。</p><h4 id="实现思路"><a href="#实现思路" class="headerlink" title="实现思路"></a>实现思路</h4><p>  1、初始：</p><pre><code>public class Solution {public ListNode addTwoNumbers(ListNode l1, ListNode l2) {  ....}</code></pre><p>  2、题意里说2个非空链表，不用判断链表是否为null；<br>    接着自然想到在求和过程中需要定义一个int变量保存进位值，需要定义一个结点变量表示求和后新链表的头结点ListNode newHead；<br>    在上面分析的过程中，遍历2个链表求和时，需要一个临时变量来保存当前遍历过程中的新结点的值ListNode sumNode:</p><pre><code>public class Solution {public ListNode addTwoNumbers(ListNode l1, ListNode l2) {    int carry = 0;// 存储进位值，初始化为0    ListNode newHead= null;//  新链表的头结点值，初始化为null    while(l1 != null || l2 != null){// 只要2个链表不同时为null，都需要继续遍历求和        ListNode sumNode = null;        int sum = carry;// 和，加上进位值        if(l1 != null) {//         sum += l1.val;// sum加上l链表当前结点的值         l1 = l1.next;// 修改l1指向下个结点        }        if(l2 != null) {         sum += l2.val;         l2 = l2.next;        }        // 求进位值和当前位的值        carry = sum / 10;        sum = sum %10;        // 遍历第一次时，设置新链表头结点的值        sumNode = new ListNode(sum);        if(newHead ==null) {            newHead = sumNode;        }        .....    }    // 进位不等于0时    if(carry!=0) {      ....    }     return newHead;   }}</code></pre><p>  3、上述过程将sumNode在while循环里作为临时变量，每次while循环后sumNode重新初始化为null，缺少了将每次循环产生的sumNode<br>  连接起来的步骤，所以需要将sumNode移动到while循环外作为临时变量，这样就能记住上次循环的sumNode值；<br>  另外就是进位产生的新结点不在while循环中，需要单独设置循环最后一次的sumNode的next为carry所生成的node：</p><pre><code>public class Solution {public ListNode addTwoNumbers(ListNode l1, ListNode l2) {    int carry = 0;// 存储进位值，初始化为0    ListNode newHead= null;//  新链表的头结点值，初始化为null    ListNode preNode= null;// 每次while循环时上次循环的和结点的值，第一次循环的上一次的和结点值可以理解为null    while(l1 != null || l2 != null){// 只要2个链表不同时为null，都需要继续遍历求和        int sum = carry;// 和，加上进位值        if(l1 != null) {//         sum += l1.val;// sum加上l链表当前结点的值         l1 = l1.next;// 修改l1指向下个结点        }        if(l2 != null) {         sum += l2.val;         l2 = l2.next;        }        // 求进位值和当前位的值        carry = sum / 10;        sum = sum %10;        // 遍历第一次时，设置新链表头结点的值        ListNode sumNode = new ListNode(sum);        if(newHead ==null) {            newHead = sumNode;            preNode = sumNode;// 更新preNode的值为新值        }else {            preNode.next = sumNode;// 将上次结点和本次结点连接起来            preNode = sumNode;//更新preNode的值为本次和产生的结点值        }    }    // 进位不等于0时    if(carry!=0) {        preNode.next = new ListNode(carry);// 将上次结点值指向进位产生的结点    }     return newHead;   }}</code></pre><h4 id="进一步优化"><a href="#进一步优化" class="headerlink" title="进一步优化"></a>进一步优化</h4><p>上个步骤的代码基本可以当做答案，不过可以进一步稍微优化：</p><ul><li>求进位值和当前位值可以添加限制条件，不用每次都执行，因为只有当sum&lt;=9时执行和不执行carry和sum值一样</li><li>可以使用sum变量提取到while外，作为上一步循环的2个结点的值加上上上一次循环的产生的进位值，这样carry变量就可以省略</li><li>while里每次都要判断newHead == null，啰嗦，判断这个条件无非就是newHead初始值为null，那假如这个结点第一次不为null，而是个类似哨兵结点sentinel，<br>这样中间便不用每次判空，直接往后追加，最后直接返回sentinel.next即可。<br>最后结果：</li></ul><pre><code>public class Solution {  public ListNode addTwoNumbers(ListNode l1, ListNode l2) {    ListNode sentinel= new ListNode(0);// 哨兵结点    ListNode preNode= sentinel;// preNode默认为哨兵结点    int sum = 0; //上一步循环的2个结点的值加上上上一次循环的产生的进位值    while(l1 != null || l2 != null){        sum = sum / 10;// 取上次循环sum的进位值        if(l1 != null) {         sum += l1.val;         l1 = l1.next;        }        if(l2 != null) {         sum += l2.val;         l2 = l2.next;        }        preNode.next = new ListNode(sum%10);;        preNode = preNode.next;    }    // 2个结点的进位值最多为1    if(sum /10 == 1) {        preNode.next = new ListNode(1);    }     return sentinel.next;// 哨兵结点的下一个即为新链表头结点   }}</code></pre><h4 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h4><p>可以通过几个简单case，自行验证：<br>1-&gt;2 2-&gt;3;<br>1-&gt;9 9-&gt;1;<br>1-&gt;9 1;   </p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;求2个由单链表表示的数字之和&lt;br&gt;
    
    </summary>
    
      <category term="algorithm" scheme="http://zhangxiangnan.com/categories/algorithm/"/>
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/"/>
    
      <category term="linked list" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/linked-list/"/>
    
      <category term="math" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/linked-list/math/"/>
    
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>反转单链表的某部分</title>
    <link href="http://zhangxiangnan.com/2017/01/17/leetcode/%E5%8F%8D%E8%BD%AC%E5%8D%95%E9%93%BE%E8%A1%A8%E7%9A%84%E6%9F%90%E9%83%A8%E5%88%86-reverse-part-singly-linked-list/"/>
    <id>http://zhangxiangnan.com/2017/01/17/leetcode/反转单链表的某部分-reverse-part-singly-linked-list/</id>
    <published>2017-01-17T09:07:01.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>反转单链表的某部分（思考过程及java源码）<br><a id="more"></a></p><h3 id="反转单链表的某部分"><a href="#反转单链表的某部分" class="headerlink" title="反转单链表的某部分"></a>反转单链表的某部分</h3><p>Reverse Linked List II(reverse part)<br><a href="https://leetcode.com/problems/reverse-linked-list-ii/" target="_blank" rel="noopener">https://leetcode.com/problems/reverse-linked-list-ii/</a></p><h4 id="原题"><a href="#原题" class="headerlink" title="原题"></a>原题</h4><pre><code>Reverse a linked list from position m to n. Do it in-place and in one-pass.For example:Given 1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;NULL, m = 2 and n = 4,return 1-&gt;4-&gt;3-&gt;2-&gt;5-&gt;NULL.Note:Given m, n satisfy the following condition:1 ≤ m ≤ n ≤ length of list.Subscribe to see which companies asked this question`/** * Definition for singly-linked list. * public class ListNode { *     int val; *     ListNode next; *     ListNode(int x) { val = x; } * } */`</code></pre><h4 id="题意"><a href="#题意" class="headerlink" title="题意"></a>题意</h4><p>大概就是给我们一个单链表，然后让我们反转单链表的一部分（2个参数表示反转的第m个结点到第n个结点，m、n从1开始），反转单链表是要求反转全部，要求必须是一次遍历完成，且必须使用原地算法（使用变量是固定数量的、小的空间，不能现行增长，本地意思是不能复制链表）<br>原地算法：<a href="https://zh.wikipedia.org/wiki/%E5%8E%9F%E5%9C%B0%E7%AE%97%E6%B3%95" target="_blank" rel="noopener">https://zh.wikipedia.org/wiki/%E5%8E%9F%E5%9C%B0%E7%AE%97%E6%B3%95</a></p><h4 id="大致思路"><a href="#大致思路" class="headerlink" title="大致思路"></a>大致思路</h4><p>不考虑特殊情况，就考虑大众情况，比方说让我反转1-&gt;2-&gt;3-&gt;4-&gt;5-&gt;NULL的结点2到结点4的部分（m=2，n=4），那我很容易想到就是遍历单链表，遍历的时候定义一个变量i，i==m到i==n中间结点就是我要反转的部分结点，这部分结点反转的逻辑就是反转某个单链表；接着就是这部分反转后的部分结点，需要和原链表按顺序连一块，本例就是1和4连，2和5连，那意思是我遍历的时需要记住待反转部分结点的前面一个结点和后一个结点，还需记住待反转部分反转后的首尾结点，然后返回结果是原链表的头结点，所以还需要记住原链表的头结点。<br>接着，我可以遍历一遍找到m、n对应结点，然后再从m遍历到n进行链表反转，这样子遍历不止一次，不符合题目要求，只能在一次遍历的时候同时反转</p><h4 id="具体过程"><a href="#具体过程" class="headerlink" title="具体过程"></a>具体过程</h4><p>  1、首先我先对head或者head.next == null的情况做下检查，head.next == null情况可能不太好想一开始，先排除：   </p><pre><code>public ListNode reverseBetween(ListNode head, int m, int n) {    if(head == null || head.next == null) return head;    ...}</code></pre><p>  2、接着我按着刚才的思路，定义几个相关变量，并初始化下值：</p><pre><code>public ListNode reverseBetween(ListNode head, int m, int n) {    if(head == null || head.next == null) return head;    int i = 1;// 因为m、n从1开始，所以统一i也从1开始    ListNode oldHead = head;// 原链表的头结点，赋值应该没啥异议    ListNode reversePreNode = null;// 反转部分链表反转前其头结点的前一个结点    ListNode reversedTail = null;// 反转部分链表反转后的尾结点，一开始不知道，为null    ListNode reverseNextNode = null;// 反转部分链表反转前尾结点的下一个结点    ListNode reversedNewHead = null;// 反转部分链表反转后的头结点，一开始不知道，为null    ...}</code></pre><p>  3、开始遍历，遍历时，我可以用while、for都行，此处用while，所以上步定义了变量i表示迭代的次数，遍历得有结束遍历的条件吧，毕竟只反转一部分，不需要遍历所有结点，所以我需要加个结束条件if(i&gt;n)break;接着就是寻找第2步中定义的几个变量的值：</p><ul><li>reversePreNode的含义是反转部分反转前头结点的前一个结点，反转部分反转前头结点其索引顺序为m，m的前一个结点索引为m－1，所以遍历时，若i=m-1，则该结点就是reversePreNode的值（反转部分反转前头结点的前一个结点）；</li><li><p>reversedTail含义为反转部门链表反转后的尾结点，那就是找反转前的头结点，头结点索引顺序为m，则若i＝＝m，则当前结点就是reversedTail的值<br>因为要在一次循环里搞定，所以朦胧感觉可能需要在while里有一段代码做反转操作：      </p><pre><code>public ListNode reverseBetween(ListNode head, int m, int n) {   if (head == null) return null;   if (head.next == null) return head;   int i = 1;   ListNode reversedNewHead = null;// 反转部分链表反转后的头结点   ListNode reversedTail = null;// 反转部分链表反转后的尾结点   ListNode oldHead = head;// 原链表的头结点   ListNode reversePreNode = null;// 反转部分链表反转前其头结点的前一个结点   ListNode reverseNextNode = null;   while (head != null) {       if (i &gt; n) {           break;       }       if (i == m - 1) {           reversePreNode = head;       }       if (i == m) {           reversedTail = head;       }       // 反转结点逻辑...       i++;// 更新i，没啥异议吧   }   return xx;// 暂定，未知}</code></pre></li></ul><p>4、接着就是反转结点部分的逻辑:</p><ul><li>首先反转结点逻辑可参考反转单链表的几句关键逻辑（临时变量next保存当前结点的next结点，修改当前结点的next指向已️反转部分链表头结点，然后更新已反转链表头结点指向，更新待反转链表的头结点指向)，几个变量对应到步骤2定义的变量即可</li><li>反转的结点是在m和n之间，所以反转逻辑应该在if(i&gt;=m &amp;&amp; i&lt;=n) 条件内执行</li><li>遍历时需要修改head指向为下一个结点，所以要在各个if else分支里都要执行head＝head.next</li></ul><pre><code>public ListNode reverseBetween(ListNode head, int m, int n) {   if (head == null) return null;   if (head.next == null) return head;   int i = 1;   ListNode reversedNewHead = null;// 反转部分链表反转后的头结点   ListNode reversedTail = null;// 反转部分链表反转后的尾结点   ListNode oldHead = head;// 原链表的头结点   ListNode reversePreNode = null;// 反转部分链表反转前其头结点的前一个结点   ListNode reverseNextNode = null;   while (head != null) {       if (i &gt; n) {           break;       }       if (i == m - 1) {           reversePreNode = head;       }       if (i &gt;= m &amp;&amp; i &lt;= n) {          if (i == m) {// 这部分移到if条件里              reversedTail = head;          }          reverseNextNode = head.next;// 反转前保存当前结点的next结点，反转结束时该值刚好是reverseNextNode代表的含义          head.next = reversedNewHead;// 修改当前结点（待反转链表部分的头结点）指向已反转链表部分的头结点          reversedNewHead = head;// 更改已反转链表的头结点的指向          head = reverseNextNode;// 更新head指向为待反转链表的头结点        } else {            head = head.next;//  更新head指向为其下一个结点        }       i++;// 更新i，没啥异议吧   }   return xx;// 暂定，未知}</code></pre><p>5、收尾与返回逻辑：</p><ul><li>在循环里执行反转后，紧接着需要在循环外执行更新reversedTail的next指向，根据几个变量含义，让其指向reverseNextNode即可。</li><li><p>返回结果就需要考虑几种情况了，因为需要返回反转后新链表的头结点，但是m有可能&gt;=2，此时reversePreNode肯定不为空，需要将reversePreNode的next指向反转部分新的头结点，然后直接返回oldHead即可；若m==1,则不能这么做，需要返回反转部分链表的新的头结点，即reversedNewHead：</p><pre><code>public ListNode reverseBetween(ListNode head, int m, int n) {    if (head == null) return null;    if (head.next == null) return head;    int i = 1;    ListNode reversedNewHead = null;// 反转部分链表反转后的头结点    ListNode reversedTail = null;// 反转部分链表反转后的尾结点    ListNode oldHead = head;// 原链表的头结点    ListNode reversePreNode = null;// 反转部分链表反转前其头结点的前一个结点    ListNode reverseNextNode = null;    while (head != null) {        if (i &gt; n) {            break;        }        if (i == m - 1) {            reversePreNode = head;        }        if (i &gt;= m &amp;&amp; i &lt;= n) {            if (i == m) {                reversedTail = head;            }            reverseNextNode = head.next;            head.next = reversedNewHead;            reversedNewHead = head;            head = reverseNextNode;        } else {            head = head.next;        }        i++;    }    reversedTail.next = reverseNextNode;    if (reversePreNode != null) {        reversePreNode.next = reversedNewHead;        return oldHead;    } else {        return reversedNewHead;    }}</code></pre></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;反转单链表的某部分（思考过程及java源码）&lt;br&gt;
    
    </summary>
    
      <category term="algorithm" scheme="http://zhangxiangnan.com/categories/algorithm/"/>
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/"/>
    
      <category term="linked list" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/linked-list/"/>
    
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>反转单链表</title>
    <link href="http://zhangxiangnan.com/2017/01/12/leetcode/%E5%8F%8D%E8%BD%AC%E5%8D%95%E9%93%BE%E8%A1%A8-reverse-singly-linked-list/"/>
    <id>http://zhangxiangnan.com/2017/01/12/leetcode/反转单链表-reverse-singly-linked-list/</id>
    <published>2017-01-12T09:22:08.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>反转单链表（思考过程及java源码）<br><a id="more"></a></p><h3 id="反转单链表"><a href="#反转单链表" class="headerlink" title="反转单链表"></a>反转单链表</h3><p>Reverse a singly linked list.<br><a href="https://leetcode.com/problems/reverse-linked-list/" target="_blank" rel="noopener">https://leetcode.com/problems/reverse-linked-list/</a></p><h4 id="迭代方式思路"><a href="#迭代方式思路" class="headerlink" title="迭代方式思路"></a>迭代方式思路</h4><p>  已知条件：假定给定一个正常单链表a-&gt;b-&gt;c-&gt;d，ListNode l1 = a;<br>  推导：反转无非是把已知的链表转化为d-&gt;c-&gt;b-&gt;a，即将原链表的结点指向全部反向。那么，开始思考：   </p><p>  1、首先我定义一个变量指向a，表示待反转链表的头结点ListNode oldHead=a，接着第一步很自然想到把a的next指向null，但是一旦我这么改的话，我就没法得到b结点，进而没法修改b的next指向。故我需要在修改a的指向前，提前定义一个额外的变量来临时保存a的next指向，那么我定义ListNode nextNode = oldHead.next，此时nextNode指向b结点，链表还未做任何改动，即a(oldHead)-&gt;b(nextNode)-&gt;c-&gt;d;  </p><p>  2、定义了nextNode变量，那么我修改a的指向便无后顾之忧，所以我执行oldHead.next = null,即使a的next指向null，此时原链表变为了2段，一段是a(oldHead)-&gt;null（称作已反转链表部分），另一段是b(nextNode)-&gt;c-&gt;d（称作未反转链表部分），因为oldHead的含义理解为当前需要反转的结点，因为需要将oldHead指向nextNode(b)，并将nextNode指向nextNode.next(c)，修改oldHead指向的话，需要记住已分割部分的头结点，只能再定义个结点newHead=oldHead，执行顺序为newHead=oldHead，oldHead=nextNode，nextNode=nextNode.next，此时链表的2段为a(newHead)-&gt;null, b(oldHead)-&gt;c(nextNode)-&gt;d-&gt;null。</p><p>  3、此时我接着要把未反转链表部分接着反转，把头结点b的next指向a（即把未反转链表的头结点指向已反转链表的头结点），此时链表分两段为b（oldHead）-&gt;a（newHead）-&gt;null，c(nextNode)-&gt;d，接着我需要更新几个变量的指向（使其oldHead指向c，newHead就需要指向b，nextNode指向d，2段链表分别为b(newHead)-&gt;a-&gt;null,c(oldHead)-&gt;d(nextNode)-&gt;null。</p><p>  4、继续重复过程3，修改c的next使其指向b，那么我得到c（oldHead）—&gt;b（newHead）-&gt;a-&gt;null，d（nextNode）-&gt;null，更新几个变量指向后，2段链表为c(newHead)-&gt;b-&gt;a-&gt;null，d(oldHead)-null(nextNode)</p><p>  5、再重复过程3，修改d的next=newHead,2段链表分别为d(oldHead)-&gt;c(newHead)-&gt;b-&gt;a-null，null(nextNode)，此时已经反转完成，然后迭代结束条件就是nextNode==null</p><p>  6、总的来说，需要newHead、oldHead、nextNode3个变量，在原链表还是一段的时候，我们的oldHead指向a，nextNode=a.next，newHead则等于null,三个变量的表示意思为：oldHead表示待反转链表的头结点，newHead表示已反转链表的头结点（所以一开始初始值为null），nextNode表示带反转链表的头结点的下一个结点（所以初始值为oldHead.next)。</p><h4 id="迭代（循环）实现"><a href="#迭代（循环）实现" class="headerlink" title="迭代（循环）实现"></a>迭代（循环）实现</h4><pre><code>`/** * Definition for singly-linked list. * public class ListNode { *     int val; *     ListNode next; *     ListNode(int x) { val = x; } * } */`public class Solution {    public ListNode reverseList(ListNode head) {       if(head == null) return null;       ListNode newHead = null;       ListNode oldHead = head;       ListNode nextNode = head.next;// nextNode定义为待反转链表头结点的next结点，初始化为head的next结点       while(nextNode != null){//  待反转链表头结点的next结点不为null作为条件          oldHead.next = newHead;          newHead =oldHead;          oldHead = nextNode;          nextNode = nextNode.next;       }       oldHead.next = newHead;// 需要手动更改待反转链表的最后一个头结点的指向       newHead = oldHead;// 该步可以省略，直接返回oldHead，为了更清晰。       return newHeadNode;    }}为什么while循环要以nextNode！= null作为结束条件？考虑以下实现，只是不同的初始化方式：`/** * Definition for singly-linked list. * public class ListNode { *     int val; *     ListNode next; *     ListNode(int x) { val = x; } * } */`public class Solution {    public ListNode reverseList(ListNode head) {        if(head == null) return null;        ListNode newHead = null;// 定义为已反转链表头结点        ListNode oldHead = head;// 定义为待反转链表头结点        ListNode next = null;// 定义为待反转链表的头结点的next结点，初始化为null        while(oldHead != null){//迭代结束条件是待反转链表的头结点不为null，为null说明反转结束            next = oldHead.next;// 保存next指向            oldHead.next = newHead;            newHead = oldHead;            oldHead = next;        }        return newHead;// newHead结点即为新链表的头结点    }}</code></pre><h4 id="递归方式思路"><a href="#递归方式思路" class="headerlink" title="递归方式思路"></a>递归方式思路</h4><p>前面分析的过程可以概括为一个已反转链表（初始为null）部分，一个未反转链表部分（初始为待反转链表），每次都是从待反转链表拿出头结点，作为已反转链表部分的头结点，不断重复这个过程，直到待反转链表部分只剩下null结点。这个过程和递归很类似，递归就是在函数里调用函数本身，重复调用函数本身说明解决的问题结构很类似，刚才的分析过程中一直存在的结构或变量有2个，一个是已反转链表、一个未反转链表，那递归函数是不是可以这么定义？</p><pre><code>private ListNode reverse(ListNode oldHead, ListNode newHead)</code></pre><p>  我在这个函数里要调用函数自身进行递归，所以：    </p><pre><code>private ListNode reverse(ListNode oldHead, ListNode newHead){  return reverse(oldHead, newHead);}</code></pre><p>  貌似需要在递归里做些处理，回想我们在每一次的从待反转链表结点里拿出头结点oldHead（拿出前需要先记住oldHead的next结点，否则修改oldHead的next指向后，便找不到原next结点了），将其next指向已反转链表的头结点，这样未反转链表和已反转链表的头结点都发生了变化，我们需要更新oldHead和newHead的指向。</p><pre><code>private ListNode reverse(ListNode oldHead, ListNode newHead){  ListNode next = oldHead.next;// 记住待反转链表头结点的next结点  oldHead.next = newHead;// 将oldHead结点从待反转链表结点拿出，使其成为已反转链表结点的头结点  newHead = oldHead;// 更新newHead结点指向  oldHead = next;// 更新oldHead结点指向，指向此时待反转链表结点的头结点（为先前保存的next指向的结点）  return reverse(oldHead, newHead);}</code></pre><p>   貌似递归还缺少递归出口，拿什么作为出口？回想刚才分析过程，最终是以待反转链表的头结点未null，结束，此时已反转链表的头结点就是反转后新链表的头结点。</p><pre><code>private ListNode reverse(ListNode oldHead, ListNode newHead){  if(oldHead == null) return newHead;  ListNode next = oldHead.next;// 记住待反转链表头结点的next结点  oldHead.next = newHead;// 将oldHead结点从待反转链表结点拿出，使其成为已反转链表结点的头结点  newHead = oldHead;// 更新newHead结点指向  oldHead = next;// 更新oldHead结点指向，指向此时待反转链表结点的头结点（为先前保存的next指向的结点）  return reverse(oldHead, newHead);}</code></pre><h4 id="递归方式实现"><a href="#递归方式实现" class="headerlink" title="递归方式实现"></a>递归方式实现</h4><pre><code>public ListNode reverseList(ListNode head) {    if(head == null) return null;    return reverse(head, null);// 一开始已反转链表为null，因此头结点未null}private ListNode reverse(ListNode oldHead, ListNode newHead){  if(oldHead == null) return newHead;  ListNode next = oldHead.next;// 记住待反转链表头结点的next结点  oldHead.next = newHead;// 将oldHead结点从待反转链表结点拿出，使其成为已反转链表结点的头结点  // 以下2个更新可以省略，为了清晰  newHead = oldHead;// 更新newHead结点指向  oldHead = next;// 更新oldHead结点指向，指向此时待反转链表结点的头结点（为先前保存的next指向的结点）  return reverse(oldHead, newHead);}</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;反转单链表（思考过程及java源码）&lt;br&gt;
    
    </summary>
    
      <category term="algorithm" scheme="http://zhangxiangnan.com/categories/algorithm/"/>
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/"/>
    
      <category term="linked list" scheme="http://zhangxiangnan.com/categories/algorithm/leetcode/linked-list/"/>
    
    
      <category term="leetcode" scheme="http://zhangxiangnan.com/tags/leetcode/"/>
    
  </entry>
  
  <entry>
    <title>kafka的入门知识</title>
    <link href="http://zhangxiangnan.com/2017/01/09/kafka/kafka%E7%9A%84%E5%85%A5%E9%97%A8%E7%9F%A5%E8%AF%86/"/>
    <id>http://zhangxiangnan.com/2017/01/09/kafka/kafka的入门知识/</id>
    <published>2017-01-09T09:17:02.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>kafka的入门知识<br><a id="more"></a></p><h3 id="kafka的背景"><a href="#kafka的背景" class="headerlink" title="kafka的背景"></a>kafka的背景</h3><p>Kafka是LinkedIn开发的一个分布式消息系统，使用Scala编写，现成为Apache项目。它以可水平扩展和高吞吐率而被广泛使用。目前越来越多的开源分布式处理系统如Cloudera、Apache Storm、Spark都支持与Kafka集成。现在很多不同类型公司用来作为多种类型的数据管道和消息系统使用。</p><h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><ul><li>日志收集：收集各种服务的log，通过kafka以统一接口方式开放给consumer，例如：Hadoop、Hbase等</li><li>消息系统：解耦producer和consumer，缓存消息</li><li>用户活动跟踪：记录用户各种活动，如page view、搜索、点击等，这些消息被发送到topic中，然后consumer做实时的监控分析</li><li>运营指标：记录运营监控指标，服务器的性能数据，如：CPU,IO,响应时间等</li><li>事件源</li><li>实时流处理</li><li>提交日志，用于备份还原</li></ul><h3 id="kafka的拓扑结构"><a href="#kafka的拓扑结构" class="headerlink" title="kafka的拓扑结构"></a>kafka的拓扑结构</h3><h4 id="拓扑图"><a href="#拓扑图" class="headerlink" title="拓扑图"></a>拓扑图</h4><p><img src="http://cdn.infoqstatic.com/statics_s1_20161108-0613/resource/articles/kafka-analysis-part-1/zh/resources/0310020.png" alt="tuopu"></p><h4 id="几个概念"><a href="#几个概念" class="headerlink" title="几个概念"></a>几个概念</h4><h5 id="Broker"><a href="#Broker" class="headerlink" title="Broker"></a>Broker</h5><p>Kafka集群包含一个或多个服务器，这种服务器被称为broker（代理服务器）。一台kafka服务器就是一个broker，一个集群由多个broker组成，一个broker可以容纳多个topic。</p><h5 id="Topic"><a href="#Topic" class="headerlink" title="Topic"></a>Topic</h5><p>每条发布到Kafka集群的消息都有一个类别或者叫做主题，这个类别被称为Topic，Kafka中Topic可以理解为一个存储消息的队列。物理上不同Topic的消息分开存储，逻辑上一个Topic的消息虽然保存在一个或多个broker上，但用户只需指定消息的Topic即可生产或消费数据而不必关心数据存于何处。</p><h5 id="Partition"><a href="#Partition" class="headerlink" title="Partition"></a>Partition</h5><p>分区，Partition是物理上的概念，Kafka物理上把Topic分成一个或多个Partition，每个Partition在物理上对应一个文件夹，该文件夹下存储这个Partition的所有消息和索引文件。如创建topic1和topic2两个topic，且分别有13个和19个Partition分区，则整个集群上相应会生成32个文件夹。为了实现扩展性，一个非常大的topic可以分布到多个broker上，但kafka只保证按一个partition中的顺序将消息发给consumer，不保证一个topic的整体（多个partition间）的顺序。</p><h5 id="Producer"><a href="#Producer" class="headerlink" title="Producer"></a>Producer</h5><p>消息生产者，负责发布消息到Kafka broker</p><h5 id="Consumer"><a href="#Consumer" class="headerlink" title="Consumer"></a>Consumer</h5><p>消息消费者，向Kafka broker读取消息的客户端。</p><h5 id="Consumer-Group（CG）"><a href="#Consumer-Group（CG）" class="headerlink" title="Consumer Group（CG）"></a>Consumer Group（CG）</h5><p>kafka用来实现一个topic消息的广播（发给所有的consumer）和单播（发给任意一个consumer）的手段。一个topic可以属于多个CG，即多个CG可以无差别地消费一个topic的信息。topic的消息会复制（不是真的复制，是概念上的）到所有的CG，但每个CG只会把消息发给该CG中的一个consumer。如果需要实现广播，只要每个consumer有一个独立的CG就可以了。要实现单播只要所有的consumer在同一个CG。用CG还可以将consumer进行自由的分组而不需要多次发送消息到不同的topic（不懂）。每个consumer属于一个特定的Consumer Group, Kafka允许为每个consumer指定group name，若不指定group name则属于默认的group。</p><h5 id="Offset偏移量"><a href="#Offset偏移量" class="headerlink" title="Offset偏移量"></a>Offset偏移量</h5><p>kafka的存储文件都是按照offset.kafka来命名，用offset做名字的好处是方便查找。例如你想找位于2049的位置，只要找到2048.kafka的文件即可。当然the first offset就是00000000000.kafka   </p><p>一个Kafka集群中会包含若干个Producer（可能是端上的PV数据，或者服务器日志，CPU、Memory监控信息等），若干个Broker（Broker越多，集群的吞吐量越高），若干个Consumer Group，以及一个zookeeper集群。Kafka通过zookeeper管理broker和consumer的动态加入和离开。Producer通过push方式将消息发送到Broker，Consumer通过pull方式从Broker订阅消费消息。</p><h3 id="kafka是一个分布式的流式传输平台"><a href="#kafka是一个分布式的流式传输平台" class="headerlink" title="kafka是一个分布式的流式传输平台"></a>kafka是一个分布式的流式传输平台</h3><p>分布式流平台应该具有的三个特征：</p><ul><li>可以发布和订阅流记录流，这点类似于消息队列或者企业级消息系统</li><li>可以以容错方式存储记录流</li><li>可以当产生记录流时进行处理.</li></ul><p>kafka的优势<br>kafka用于2大类应用程序：</p><ul><li>构建可靠地在系统或应用程序之间获取数据的实时流数据流水线</li><li>构建对数据流进行变换或反应的实时流应用程序</li></ul><p>相关概念：<br>Kafka作为集群运行在一个或者多个机器上<br>Kafka集群以称为主题的类别存储记录流。<br>每个记录由一个键，一个值和一个时间戳组成。</p><p>Kafka有四个核心API：</p><ul><li>Producer API允许应用程序发布记录流到一个或多个Kafka主题</li><li>Consumer API允许应用程序订阅一个或多个主题，并处理为其生成的记录流.</li><li>Steams API允许应用程序充当流处理器，从一个或多个主题消费输入流，然后产生输出流到一个或多个输出主题，高效地转换输入流到输出流。</li><li><p>Connector API允许构建和运行可重用的生产者或消费者，将Kafka主题连接到现有应用程序或数据系统。例如，关系数据库的Connector连接器可能捕获对表的每个更改。</p><p><img src="http://kafka.apache.org/0101/images/kafka-apis.png" alt="kafka-apis"></p></li></ul><p>Kafka中，客户端和服务端的通讯使用一个简单的，高性能的，语言无关的TCP协议。该协议有版本控制，并保持与旧版本的向后兼容性。同时，提供了很多种语言的Kafka客户端。</p><h3 id="Kafka文件存储机制"><a href="#Kafka文件存储机制" class="headerlink" title="Kafka文件存储机制"></a>Kafka文件存储机制</h3><h4 id="Topic-amp-Partition"><a href="#Topic-amp-Partition" class="headerlink" title="Topic &amp; Partition"></a>Topic &amp; Partition</h4><p>Kafka为记录流提供的核心抽象便是主题（topic）.<br>主题是发布记录的类别或者feed名称。Kafka的主题总是支持多个消费者的，即一个主题可以拥有0个、1个或者多个订阅者来订阅发送到该主题的数据。</p><p>对于每个主题，Kafka集群维护了一个分区日志如下图：<br><img src="http://kafka.apache.org/0101/images/log_anatomy.png" alt="log_anatomy"></p><p>逻辑上Topic可以看为一个Queue，每个消息必须指定它所属的Topic，可理解为每条消息必须指明放到哪个Queue里。<br>物理上把Topic分成一个或多个Partition，每个Partition对应一个文件夹，文件夹存储Partition所有消息和索引文件。每个Partition有自己的副本，每个副本分布在不同的Broker节点上。通过多个Partition可使得吞吐率能线性提升。<br>消息append到Partition中，属于顺序写磁盘，顺序写磁盘效率远大于随机写内存，因此效率高。</p><p>每一个分区是一个有序的、不可变的记录序列，不断地追加到结构化提交日志。分区中的每条记录都被赋予了一个顺序的id号，称作offset偏移量，偏移量在某个分区内唯一标示每条记录。</p><p>Kafka集群保留所有已发布的记录，无论是否已经被消费。当然保留所有数据也不现实，kafka提供两种方式来删除数据，一种通过配置来定义数据的保留时间，另一种通过配置分区文件大小限制。例如，如果设置保留策略为2天，则一条记录在被发布后的2天之内，是可以被消费的，一旦2天过后则被丢弃来达到释放空间的目的。Kafka的性能和数据的大小密切相关，和将数据存储多长时间无太大关系.</p><p><img src="http://kafka.apache.org/0101/images/log_consumer.png" alt="log_consumer"></p><p>事实上，每个消费者保留的唯一元数据是该消费者在日志中的偏移或位置。这种偏移由消费者控制，通常消费者在读取记录时线性地提前其偏移，但是实际上，由于消费者控制着位置的值，所以它可以以喜欢的任何顺序来消费数据。例如，一个消费者可以将偏移量的值减小来重复处理以前的数据，或者跳过最近的记录从“现在”开始消费数据。</p><p>这些功能的组合使kafka的消费者非常轻量级，消费者来来去去，对集群和其他消费者却没什么太多影响。如，你可以通过kafka命令行工具的tail命令来获取任何主题的记录，却无需更改任何现有用户使用的内容。</p><p>日志的分区设计有几个目的。首先分区使日志可以增加到超出单台服务器的容量。首先，它们允许日志扩展到适合单个服务器的大小。每个单独的分区必须适合托管它的服务器，但主题可能有许多分区，因此它可以处理任意数量的数据。第二，为了并行性</p><h4 id="partition存储结构"><a href="#partition存储结构" class="headerlink" title="partition存储结构"></a>partition存储结构</h4><p>Partition命名规则：Topic+有序序号(从0开始)，例如topic_name-0, topic_name-1, topic_name-2 …   </p><p>Partition和副本分配方法。例如一个Kafka集群有2个Broker，创建一个Topic包含2个Partition，1个副本，则broker-0存放partition-0、partition-1，broker-1存放partition-0，partition-1即可 。</p><p>Partition中文件存储方式：每个Partition平均分配到多个大小相等的Segment数据文件中，Segment文件生命周期由server端配置参数决定。<br>Kafka有2种策略删除旧数据：</p><ul><li>基于时间，例如：删除1周以前的数据</li><li>基于partition文件大小，例如：Partition文件大于1GB时候删除旧数据</li></ul><h3 id="分布式"><a href="#分布式" class="headerlink" title="分布式"></a>分布式</h3><p>日志的分区分布在kafka集群中的服务器上，每个服务器处理数据并请求共享分区。每个分区都使用可配置数量的服务器进行复制，以实现容错。</p><p>每个分区都有一台机器充当leader，0或者多台机器充当followers。leader处理所有的针对该分区的读和写请求，followers则被动同步leader的数据。若leader宕机，则kafka自动选取一个followers来充当leader。每个服务器都充当某个分区的leader，同时，又充当剩下分区的follower，这样实现集群的负载均衡。</p><h3 id="生产者Producers"><a href="#生产者Producers" class="headerlink" title="生产者Producers"></a>生产者Producers</h3><p>生产者发布数据到他们选择发布的主题上。生产者决定哪条记录发布到主题内的哪个分区上，实现这个功能可以通过简单轮询或者通过语法层面的分区函数（如根据记录里的某个关键字段）来实现负载均衡。</p><h3 id="消费者Consumers"><a href="#消费者Consumers" class="headerlink" title="消费者Consumers"></a>消费者Consumers</h3><p>消费者使用一个消费者组名称consumer group name来标记自己，发布到主题的每个记录针对订阅该主题的每个消费组只会发送给其中一个消费者实例（消费者实例可以是在单独的进程或者单独的机器上）。<br>如果所有的消费者实例有相同的消费组，这样数据将在这些消费者实例中很好地负载均衡。<br>如果所有的消费者实例都有着不同的消费组，这样每条数据记录都会被广播到所有的消费者实例中。    </p><p><img src="http://kafka.apache.org/0101/images/consumer-groups.png" alt="consumer-groups"></p><p>如图，两台服务器组成的kafka集群，托管有四个分区(p0~p3)，与2个消费组。消费组A有2个消费者实例，消费组B有4个消费者实例。</p><p>通常情况下，一个主题有少量的消费组，可以理解为“逻辑上的订阅者”。为了扩展和容错，每个消费组里包含多个消费者实例。这就和发布-订阅模型很像，只不过kafka的一个订阅者是一个由很多实例组成的集群而不是单个进程。</p><p>在Kafka中实现消费的方式是通过在消费者实例上划分日志中的分区，使得每个实例在任何时间点是分区的“公平共享”的独占消费者。维持消费组内的消费实例与分区关系的过程是通过kafka协议动态完成的。如果新的消费实例加入消费组，他们会从组内的其他消费实例的手中接管一些分区；如果一个消费者实例死掉，它负责的分区会被分派到其他尚存的消费者实例中。</p><p>Kafka只提供一个分区内记录的总顺序，而不是主题中的不同分区之间，即不保证一个主题内不同分区的数据间的顺序关系。每个分区的顺序性结合按键分割数据的特性已经能满足大部分的程序需要。然而，如果你需要对记录进行总排序，可以使用只有一个分区的主题来实现，这意味着每个消费组内也只能有一个消费者实例。</p><h3 id="保证"><a href="#保证" class="headerlink" title="保证"></a>保证</h3><p>Kafka提供了如下保证：</p><ul><li>生产者发送到特定主题分区的消息将按照发送时的顺序进行追加。即，如果记录M1和M2都是被同一个生产者发送，且M1先发送，这样M1拥有比M2小的偏移量，而且在日志文件里也出现的比M2早。</li><li>一个消费者实例按照记录在日志文件里存储的顺序消费数据。</li><li>针对副本个数设置为N的主题，kafka可以容忍N-1个服务器故障而不丢失任何提交到日志文件的任何记录。</li></ul><h3 id="Kafka作为消息系统Messaging-System"><a href="#Kafka作为消息系统Messaging-System" class="headerlink" title="Kafka作为消息系统Messaging System"></a>Kafka作为消息系统Messaging System</h3><p>Kafka流的概念 VS 传统企业级消息系统：</p><p>1、传统消息系统有2个模型：队列和发布-订阅模型。针对队列，一组消费者从一个服务器读取数据，每一条记录只被其中的一个消费者消费；针对发布-订阅模型，每条记录被广播到所有消费者。这2个模型都有其优势和劣势。队列允许你分割数据的处理到多个消费者实例，让你可以扩展处理过程，但不支持多个消费者组,因为一旦一个进程消费了数据数据便不可再被消费。发布订阅允许你广播数据到多个订阅者，但是不能够扩展处理过程因为每条记录都被发送到每一个订阅者。</p><p>由此，kafka产生了消费组的概念，和队列一样，消费组让你可以分割处理过程到一系列进程去处理（消费组的成员）。和订阅发布类似，消费组让你可以广播信息到多个消费组。</p><p>Kafka模型的优势是每个主题都有着两个特性，可以扩展处理过程同时支持多订阅者，没有必要非得像传统消息系统一样非得选择队列或者订阅发布模型。</p><p>2、Kafka相比传统消息系统也有着更强的顺序保证：<br>传统队列在服务器端按顺序保留记录，如果多个消费者从队列里消费数据，服务端按照他们存储的顺序分发记录，记录被异步传递到消费者，所以记录可能到达不同的消费者时丢失了顺序。这意味着存在并行消费时，记录的顺序在被多个消费者消费时的顺序和其存储顺序则不一致。消息系统通常使用一个叫做独占消费者的概念来解决这个问题，独占消费者只允许一个进程来从队列里消费数据，但这意味着处理过程没有并行性。</p><p>Kafka做得更好。通过在主题内部具有并行性的概念（分区），kafka可以在多个消费者进程间保证顺序和负载均衡。通过将主题内的分区分派到消费组内的消费者来实现，这样每个分区仅仅被该消费组内的恰好一个消费者消费。这样，kafka确保指定消费者是指定分区的唯一消费者，并且按顺序消费数据。由于有多个分区，这样做仍然可以在多个消费者实例上负载均衡。注意一个消费组里的消费者实例不能超过主题的分区数。</p><h3 id="kafka作为存储系统"><a href="#kafka作为存储系统" class="headerlink" title="kafka作为存储系统"></a>kafka作为存储系统</h3><p>任何允许发布消息和消费消息两个过程解耦的消息队列实际上都充当正在传输的消息的存储系统。Kafka就是一个非常优秀的存储系统。<br>写到Kafka的信息被写到了磁盘，为了容错进行了复制。Kafka为生产者提供确认机制，这样直到数据被全部复制成功后，一个写操作才被认为成功，并且保证即时服务器写入失败也会持续运行。<br>Kafka使用的磁盘结构很容易扩展-无论服务器上有50KB还是50TB的持久化数据，Kafka做的事情是一样的。</p><p>由于对很好地利用存储，并且允许客户端来控制自身的读取位置，所以将Kafka当做某种特殊用途的分布式文件系统，致力于解决高性能、低延迟的提交日志存储，复制和传播特性。</p><p>###kafka作为流处理系统<br>Kafka不仅仅可以用来读、写、存储数据流、还是可以当做实时流处理系统。<br>Kafka中，流处理器是从输入主题中获取连续数据流，然后对输入数据做一些处理，接着产生连续数据流到输出主题的任何东西。<br>如，一个零售程序接收销售和货物的输入数据流，然后计算后产生排序和价格调整的输出流。</p><p>如果是直接做一些简单的处理过程可以通过producer和consumer的API，对于更复杂的转换，Kafka提供了一套完整集成的流API，允许对流进行计算聚合或者合并流数据。</p><p>这套API帮助解决了这些难题：处理无序数据，重复处理数据，执行状态有关的计算等。<br>这套API构建与Kafka提供的核心原是愈发：生产者消费者API，Kafka的有状态存储机制，在流处理器实例之间使用相同的group组机制进行容错</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Kafka包含了消息、存储、流处理这些特性看似不寻常，但是Kafka作为一个流平台这些都是必须的。<br>分布式文件系统如HDFS允许存储静态文件用来批处理。这样的系统实际上可以存储并处理过去的历史数据。</p><p>传统企业级消息系统让你可以处理在订阅之后产生的数据，而构建于分布式文件系统的系统则可以处理订阅之前的数据。<br>Kafka拥有这两种特性，这对于Kafka作为流程序平台和流数据流水线至关重要</p><p>通过结合了存储和低延迟订阅的特性，流式应用程序以同样的方式处理过去和未来的数据。这个单一的程序可以处理历史的、已存储的数据；当消费到最后一条数据时，并没有结束，可以继续处理未来产生的数据。这便是流处理的一个广义概念，包括了批处理和消息驱动程序。</p><p>同样，对于流水局流水线而言，订阅和实时事件相结合使借助Kafka作为非常低延迟的流水线成为可能；可靠存储数据的特性使其应用于传送关键数据的场合，这些场合对于数据的传递必须得到保证，或者用于和仅仅周期性加载数据或可以长时间进行维护的离线系统进行集成的场景。Kafka流处理设施使得可以在数据到达时变换数据。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;kafka的入门知识&lt;br&gt;
    
    </summary>
    
      <category term="kafka" scheme="http://zhangxiangnan.com/categories/kafka/"/>
    
    
      <category term="kafka" scheme="http://zhangxiangnan.com/tags/kafka/"/>
    
  </entry>
  
  <entry>
    <title>go的GOPATH概念</title>
    <link href="http://zhangxiangnan.com/2017/01/06/go/go%E7%9A%84GOPATH%E6%A6%82%E5%BF%B5/"/>
    <id>http://zhangxiangnan.com/2017/01/06/go/go的GOPATH概念/</id>
    <published>2017-01-06T08:27:12.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>go的GOPATH概念</p><a id="more"></a><p>Go的路径概念用来解析引入的声明<br>The Go path is used to resolve import statements.<br>It is implemented by and documented in the go/build package.</p><p>The GOPATH environment variable lists places to look for Go code.<br>On Unix, the value is a colon-separated string.<br>On Windows, the value is a semicolon-separated string.<br>On Plan 9, the value is a list.</p><p>GOPATH must be set to get, build and install packages outside the<br>standard Go tree.</p><p>Each directory listed in GOPATH must have a prescribed structure:</p><p>The src directory holds source code.  The path below src<br>determines the import path or executable name.</p><p>The pkg directory holds installed package objects.<br>As in the Go tree, each target operating system and<br>architecture pair has its own subdirectory of pkg<br>(pkg/GOOS_GOARCH).</p><p>If DIR is a directory listed in the GOPATH, a package with<br>source in DIR/src/foo/bar can be imported as “foo/bar” and<br>has its compiled form installed to “DIR/pkg/GOOS_GOARCH/foo/bar.a”.</p><p>The bin directory holds compiled commands.<br>Each command is named for its source directory, but only<br>the final element, not the entire path.  That is, the<br>command with source in DIR/src/foo/quux is installed into<br>DIR/bin/quux, not DIR/bin/foo/quux.  The “foo/“ prefix is stripped<br>so that you can add DIR/bin to your PATH to get at the<br>installed commands.  If the GOBIN environment variable is<br>set, commands are installed to the directory it names instead<br>of DIR/bin. GOBIN must be an absolute path.</p><p>Here’s an example directory layout:</p><pre><code>GOPATH=/home/user/gocode/home/user/gocode/    src/        foo/            bar/               (go code in package bar)                x.go            quux/              (go code in package main)                y.go    bin/        quux                   (installed command)    pkg/        linux_amd64/            foo/                bar.a          (installed package object)</code></pre><p>Go searches each directory listed in GOPATH to find source code,<br>but new packages are always downloaded into the first directory<br>in the list.</p><p>See <a href="https://golang.org/doc/code.html" target="_blank" rel="noopener">https://golang.org/doc/code.html</a> for an example.</p><p>Internal Directories</p><p>Code in or below a directory named “internal” is importable only<br>by code in the directory tree rooted at the parent of “internal”.<br>Here’s an extended version of the directory layout above:</p><pre><code>/home/user/gocode/    src/        crash/            bang/              (go code in package bang)                b.go        foo/                   (go code in package foo)            f.go            bar/               (go code in package bar)                x.go            internal/                baz/           (go code in package baz)                    z.go            quux/              (go code in package main)                y.go</code></pre><p>The code in z.go is imported as “foo/internal/baz”, but that<br>import statement can only appear in source files in the subtree<br>rooted at foo. The source files foo/f.go, foo/bar/x.go, and<br>foo/quux/y.go can all import “foo/internal/baz”, but the source file<br>crash/bang/b.go cannot.</p><p>See <a href="https://golang.org/s/go14internal" target="_blank" rel="noopener">https://golang.org/s/go14internal</a> for details.</p><p>Vendor Directories</p><p>Go 1.6 includes support for using local copies of external dependencies<br>to satisfy imports of those dependencies, often referred to as vendoring.</p><p>Code below a directory named “vendor” is importable only<br>by code in the directory tree rooted at the parent of “vendor”,<br>and only using an import path that omits the prefix up to and<br>including the vendor element.</p><p>Here’s the example from the previous section,<br>but with the “internal” directory renamed to “vendor”<br>and a new foo/vendor/crash/bang directory added:</p><pre><code>/home/user/gocode/    src/        crash/            bang/              (go code in package bang)                b.go        foo/                   (go code in package foo)            f.go            bar/               (go code in package bar)                x.go            vendor/                crash/                    bang/      (go code in package bang)                        b.go                baz/           (go code in package baz)                    z.go            quux/              (go code in package main)                y.go</code></pre><p>The same visibility rules apply as for internal, but the code<br>in z.go is imported as “baz”, not as “foo/vendor/baz”.</p><p>Code in vendor directories deeper in the source tree shadows<br>code in higher directories. Within the subtree rooted at foo, an import<br>of “crash/bang” resolves to “foo/vendor/crash/bang”, not the<br>top-level “crash/bang”.</p><p>Code in vendor directories is not subject to import path<br>checking (see ‘go help importpath’).</p><p>When ‘go get’ checks out or updates a git repository, it now also<br>updates submodules.</p><p>Vendor directories do not affect the placement of new repositories<br>being checked out for the first time by ‘go get’: those are always<br>placed in the main GOPATH, never in a vendor subtree.</p><p>See <a href="https://golang.org/s/go15vendor" target="_blank" rel="noopener">https://golang.org/s/go15vendor</a> for details.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;go的GOPATH概念&lt;/p&gt;
    
    </summary>
    
      <category term="译" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/"/>
    
      <category term="go" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/go/"/>
    
      <category term="GOPATH" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/go/GOPATH/"/>
    
    
      <category term="guava" scheme="http://zhangxiangnan.com/tags/guava/"/>
    
      <category term="immutableCollections" scheme="http://zhangxiangnan.com/tags/immutableCollections/"/>
    
  </entry>
  
  <entry>
    <title>guava的集合工具类详解</title>
    <link href="http://zhangxiangnan.com/2016/11/22/guava/collections/guava%E7%9A%84%E9%9B%86%E5%90%88%E5%B7%A5%E5%85%B7%E7%B1%BB%E8%AF%A6%E8%A7%A3/"/>
    <id>http://zhangxiangnan.com/2016/11/22/guava/collections/guava的集合工具类详解/</id>
    <published>2016-11-22T07:18:15.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>JDK集合框架提供了许多工具，方便实用，Guava相比提供了更多的工具，适用于所有集合，这也是guava更流行和成熟的部分。<br><a id="more"></a></p><p>guava针对一个特定的接口将其方法以一种相对直观的方式进行分组，如下:</p><table><thead><tr><th>接口</th><th>JDK还是Guava的接口</th><th>对应的Guava工具类</th></tr></thead><tbody><tr><td>Collection</td><td>JDK</td><td>Collections2 (避免和 java.util.Collections冲突)</td></tr><tr><td>List</td><td>JDK</td><td>Lists</td></tr><tr><td>Set</td><td>JDK</td><td>Sets</td></tr><tr><td>SortedSet</td><td>JDK</td><td>Sets</td></tr><tr><td>Map</td><td>JDK</td><td>Maps</td></tr><tr><td>SortedMap</td><td>JDK</td><td>Maps</td></tr><tr><td>Queue</td><td>JDK</td><td>Queues</td></tr><tr><td>Multiset</td><td>Guava</td><td>Multisets</td></tr><tr><td>Multimap</td><td>Guava</td><td>Multimaps</td></tr><tr><td>BiMap</td><td>Guava</td><td>Maps</td></tr><tr><td>Table</td><td>Guava</td><td>Tables</td></tr></tbody></table><h3 id="静态构造方式"><a href="#静态构造方式" class="headerlink" title="静态构造方式"></a>静态构造方式</h3><p>JDK7之前，构造新的泛型集合需要讨厌的代码重复:</p><pre><code>List&lt;TypeThatsTooLongForItsOwnGood&gt; list = new ArrayList&lt;TypeThatsTooLongForItsOwnGood&gt;();</code></pre><p>Guava则借助于泛型推断来决定右侧类型：</p><pre><code>List&lt;TypeThatsTooLongForItsOwnGood&gt; list = Lists.newArrayList();Map&lt;KeyType, LongishValueType&gt; map = Maps.newLinkedHashMap();</code></pre><p>JDK7的菱形操作符也去除了右侧类型：</p><pre><code>List&lt;TypeThatsTooLongForItsOwnGood&gt; list = new ArrayList&lt;&gt;();</code></pre><p>但是guava优化的更好，使用工厂模式，可以很方便的初始化集合。</p><pre><code>Set&lt;Type&gt; copySet = Sets.newHashSet(elements);List&lt;String&gt; theseElements = Lists.newArrayList(&quot;alpha&quot;, &quot;beta&quot;, &quot;gamma&quot;);</code></pre><p>此外，可以根据工厂方法的名称来提高初始化集合到指定大小的可读性：</p><pre><code>List&lt;Type&gt; exactly100 = Lists.newArrayListWithCapacity(100);List&lt;Type&gt; approx100 = Lists.newArrayListWithExpectedSize(100);Set&lt;Type&gt; approx100Set = Sets.newHashSetWithExpectedSize(100);</code></pre><p>注意: Guava自己的新集合类型没有暴露原始构造器或初始化工具方法。相反，Guava暴露了静态工厂方法，如：</p><pre><code>Multiset&lt;String&gt; multiset = HashMultiset.create();</code></pre><h3 id="Iterables"><a href="#Iterables" class="headerlink" title="Iterables"></a>Iterables</h3><p>Guava的工具方法总是接受Iterable而不是Collection，这是因为遇到一个不是存在在内存中的“集合”是很常见的，如从数据库，从数据中心；如果没有抓取到所有的元素，是不支持如size方法的。</p><p>因此，你希望的支持所有Collections的许多操作都可以在Iterables中找到。此外，大多数Iterables的方法在Iterables中有访问原始iterator的对应版本。</p><p>Iterables中的绝大多数方法都是很简便的: 只在有绝对需要的时候才增强底层迭代。返回Iterables的方法返回的是简单计算的视图，而不是内存中显示构造的新集合。</p><p>Guava12中，Iterables通过FlumentIterable支持，对大多数操作进行流式语法包装。</p><h4 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法"></a>常用方法</h4><table><thead><tr><th>方法</th><th>描述</th><th>等同于</th></tr></thead><tbody><tr><td>concat(<code>Iterable&lt;Iterable&gt;</code>)</td><td>返回几个Iterables对象的原始级联视图</td><td>concat(Iterable…)</td></tr><tr><td>frequency(Iterable, Object)</td><td>返回指定object在Iterable中出现的次数</td><td>Collections.frequency(Collection, Object); 参考Multiset</td></tr><tr><td>partition(Iterable, int)</td><td>把Iterable分割为每块含有指定的数目，并返回不可修改的视图</td><td>Lists.partition(List, int), paddedPartition(Iterable, int)</td></tr><tr><td>getFirst(Iterable, T default)</td><td>返回Iterable的第一个元素，若为空返回指定默认元素.</td><td>Iterable.iterator().next() FluentIterable.first()</td></tr><tr><td>getLast(Iterable)</td><td>返回iterable的最后元素，或者当为空时快速失败</td><td>getLast(Iterable, T default) FluentIterable.last()</td></tr><tr><td> elementsEqual(Iterable, Iterable)</td><td>当Iterables相同的顺序上拥有相同的元素才返回true</td><td>List.equals(Object)</td></tr><tr><td>unmodifiableIterable(Iterable)</td><td>返回iterable的不可修改视图</td><td>Collections.unmodifiableCollection(Collection)</td></tr><tr><td>limit(Iterable, int)</td><td>返回元素个数最多为指定数字的Iterable</td><td>FluentIterable.limit(int)</td></tr><tr><td>getOnlyElement(Iterable)</td><td>返回Iterable中的仅有的一个元素，若Iterable为空或者含有多个元素则快速失败。</td><td>getOnlyElement(Iterable, T default)</td></tr></tbody></table><pre><code>Iterable&lt;Integer&gt; concatenated = Iterables.concat(  Ints.asList(1, 2, 3),  Ints.asList(4, 5, 6));// concatenated has elements 1, 2, 3, 4, 5, 6String lastAdded = Iterables.getLast(myLinkedHashSet);String theElement = Iterables.getOnlyElement(thisSetIsDefinitelyASingleton);  // if this set isn&apos;t a singleton, something is wrong!</code></pre><h4 id="Iterables类似Collection的方法"><a href="#Iterables类似Collection的方法" class="headerlink" title="Iterables类似Collection的方法"></a>Iterables类似Collection的方法</h4><p>通常，Collections天然支持如下这些操作，但是Iterables不是这样。<br>这些操作当参数实际上是Collection时，每个都都代表了对应的集合接口方法。如，如果Iterables.size传入的参数实际上是一个Collection，方法内部调用Collection.size替代而不是遍历Iterator。</p><table><thead><tr><th>方法</th><th>类似集合方法</th><th>FluentIterable等价方法</th></tr></thead><tbody><tr><td>addAll(Collection addTo, Iterable toAdd)</td><td>Collection.addAll(Collection)</td><td></td></tr><tr><td>contains(Iterable, Object)</td><td>Collection.contains(Object)</td><td>FluentIterable.contains(Object)</td></tr><tr><td>removeAll(Iterable removeFrom, Collection toRemove)</td><td>Collection.removeAll(Collection)</td><td></td></tr><tr><td>retainAll(Iterable removeFrom, Collection toRetain)</td><td>Collection.retainAll(Collection)</td><td></td></tr><tr><td>size(Iterable)</td><td>Collection.size()</td><td>FluentIterable.size()</td></tr><tr><td>toArray(Iterable, Class)</td><td>Collection.toArray(T[])</td><td>FluentIterable.toArray(Class)</td></tr><tr><td>isEmpty(Iterable)</td><td>Collection.isEmpty()</td><td>FluentIterable.isEmpty()</td></tr><tr><td>get(Iterable, int)</td><td>List.get(int)</td><td>FluentIterable.get(int)</td></tr><tr><td>toString(Iterable)</td><td>Collection.toString()</td><td>FluentIterable.toString()</td></tr></tbody></table><h4 id="FluentIterable"><a href="#FluentIterable" class="headerlink" title="FluentIterable"></a>FluentIterable</h4><p>FluentIterable也有几个拷贝到不可变集合的简洁的方法:</p><p>ImmutableSet toImmutableSet()<br>ImmutableSortedSet toImmutableSortedSet(Comparator)</p><h3 id="Lists"><a href="#Lists" class="headerlink" title="Lists"></a>Lists</h3><p>除了静态构造方法和函数式编程方法，Lists提供了很多实用的工具方法。</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td>partition(List, int)</td><td>返回一个list视图，将原List分为指定大小的块</td></tr><tr><td>reverse(List)</td><td>返回指定List的反转（倒序）视图，若果list不可变，请使用 ImmutableList.reverse()</td></tr></tbody></table><pre><code>List&lt;Integer&gt; countUp = Ints.asList(1, 2, 3, 4, 5);List&lt;Integer&gt; countDown = Lists.reverse(theList); // {5, 4, 3, 2, 1}List&lt;List&lt;Integer&gt;&gt; parts = Lists.partition(countUp, 2); // {{1, 2}, {3, 4}, {5}}</code></pre><h3 id="Sets"><a href="#Sets" class="headerlink" title="Sets"></a>Sets</h3><h4 id="Set理论上的操作"><a href="#Set理论上的操作" class="headerlink" title="Set理论上的操作"></a>Set理论上的操作</h4><p>We provide a number of standard set-theoretic operations, implemented as views over the argument sets. These return a SetView, which can be used:<br>Guava支持set理论上的操作，均返回SetView，其支持：</p><ul><li>直接作为一个Set，因为SetView实现了Set接口</li><li>拷贝到另一个不可变的集合通过copyInto(set)</li><li>通过immutableCopy()得到一个不可变的拷贝</li></ul><table><thead><tr><th></th><th>方法</th></tr></thead><tbody><tr><td></td><td>union(Set, Set)</td></tr><tr><td></td><td>intersection(Set, Set)</td></tr><tr><td></td><td>difference(Set, Set)</td></tr><tr><td></td><td>symmetricDifference(Set, Set)</td></tr></tbody></table><p>如：</p><pre><code>Set&lt;String&gt; wordsWithPrimeLength = ImmutableSet.of(&quot;one&quot;, &quot;two&quot;, &quot;three&quot;, &quot;six&quot;, &quot;seven&quot;, &quot;eight&quot;);Set&lt;String&gt; primes = ImmutableSet.of(&quot;two&quot;, &quot;three&quot;, &quot;five&quot;, &quot;seven&quot;);SetView&lt;String&gt; intersection = Sets.intersection(primes, wordsWithPrimeLength); // contains &quot;two&quot;, &quot;three&quot;, &quot;seven&quot;// 可以直接使用intersection作为Set，但是如果使用很多次则拷贝更高效return intersection.immutableCopy();</code></pre><p>其他Set工具方法</p><table><thead><tr><th>方法</th><th>描述</th><th>参照</th></tr></thead><tbody><tr><td>cartesianProduct(List<set>)</set></td><td>笛卡尔乘积，返回从每个Set选取的每个元素可能组成的list</td><td>cartesianProduct(Set…)</td></tr><tr><td>powerSet(Set)</td><td>返回指定set的子set的set集合</td><td></td></tr></tbody></table><pre><code>Set&lt;String&gt; animals = ImmutableSet.of(&quot;gerbil&quot;, &quot;hamster&quot;);Set&lt;String&gt; fruits = ImmutableSet.of(&quot;apple&quot;, &quot;orange&quot;, &quot;banana&quot;);Set&lt;List&lt;String&gt;&gt; product = Sets.cartesianProduct(animals, fruits);// {{"gerbil", "apple"}, {"gerbil", "orange"}, {"gerbil", "banana"},    //  {"hamster", "apple"}, {"hamster", "orange"}, {"hamster", "banana"}}Set&lt;Set&lt;String&gt;&gt; animalSets = Sets.powerSet(animals);// {{}, {"gerbil"}, {"hamster"}, {"gerbil", "hamster"}}</code></pre><h3 id="Maps"><a href="#Maps" class="headerlink" title="Maps"></a>Maps</h3><h4 id="uniqueIndex方法"><a href="#uniqueIndex方法" class="headerlink" title="uniqueIndex方法"></a>uniqueIndex方法</h4><p>Maps.uniqueIndex(Iterable, Function) 解决的是拥有一组对象，每个对象的某个属性是唯一的，我们想通过这个唯一的属性来找到这个对象。</p><p>如我们有一组string，每个string长度不一样，想通过指定的长度来找到某个对象：</p><pre><code>ImmutableMap&lt;Integer, String&gt; stringsByIndex = Maps.uniqueIndex(strings, new Function&lt;String, Integer&gt; () {    public Integer apply(String string) {      return string.length();    }  });</code></pre><h4 id="difference方法"><a href="#difference方法" class="headerlink" title="difference方法"></a>difference方法</h4><p>Maps.difference(Map, Map)比较的是2个map的所有不同点，返回一个MapDifference对象:</p><ul><li>entriesInCommon() 在2个map里都有的entry，key和values都相等。</li><li>entriesDiffering()    key相同，但是有不同的values，可以通过MapDifference.ValueDifference来得到左边和右边的values</li><li>entriesOnlyOnLeft()    key在左边的map里存在但是在右边的map里没有</li><li><p>entriesOnlyOnRight()    key在右边的map存在，左边没有</p><p>  Map&lt;String, Integer&gt; left = ImmutableMap.of(“a”, 1, “b”, 2, “c”, 3);<br>  Map&lt;String, Integer&gt; right = ImmutableMap.of(“b”, 2, “c”, 4, “d”, 5);<br>  MapDifference&lt;String, Integer&gt; diff = Maps.difference(left, right);</p><p>  diff.entriesInCommon(); // {“b” =&gt; 2}<br>  diff.entriesDiffering(); // {“c” =&gt; (3, 4)}<br>  diff.entriesOnlyOnLeft(); // {“a” =&gt; 1}<br>  diff.entriesOnlyOnRight(); // {“d” =&gt; 5}</p></li></ul><h3 id="BiMap的工具方法"><a href="#BiMap的工具方法" class="headerlink" title="BiMap的工具方法"></a>BiMap的工具方法</h3><p>The Guava针对BiMap的工具方法在Maps里，因为BiMap也是一个Map</p><table><thead><tr><th>BiMap utility</th><th>Corresponding Map utility</th></tr></thead><tbody><tr><td>synchronizedBiMap(BiMap)</td><td>Collections.synchronizedMap(Map)</td></tr><tr><td>unmodifiableBiMap(BiMap)</td><td>Collections.unmodifiableMap(Map)</td></tr></tbody></table><h3 id="Multisets"><a href="#Multisets" class="headerlink" title="Multisets"></a>Multisets</h3><p>标准的集合操作，如containsAll忽略了multiset中元素出现的次数，只关心元素是否在multiset中存在与否。Multisets则提供了许多重视元素多样性的操作：</p><table><thead><tr><th>方法</th><th>描述</th><th>和Collection方法的区别</th></tr></thead><tbody><tr><td>containsOccurrences(Multiset sup, Multiset sub)</td><td>如果sub.count(o)&lt;=super.count(o)对于所有的o成立，则返回true，否则false</td><td>Collection.containsAll忽略了出现次数，仅仅测试元素是否被包含</td></tr><tr><td>removeOccurrences(Multiset removeFrom, Multiset toRemove)</td><td>移除removeFrom中的一次出现针对toRemove元素的每一次出现</td><td>Collection.removeAll移除了所有元素的所有出现次数，虽然toremove中仅仅出现一次</td></tr><tr><td>retainOccurrences(Multiset removeFrom, Multiset toRetain)</td><td>确保对于所有的o，removeFrom.count(o) &lt;= toReatin.count(o)</td><td>Collection.retainAll保留了所有元素的所有出现次数，虽然toReatin中仅仅出现一次。</td></tr><tr><td>intersection(Multiset, Multiset)</td><td>返回2个multisets的交集视图，retainOccurrences的一种非破坏性方法</td><td></td></tr></tbody></table><pre><code>Multiset&lt;String&gt; multiset1 = HashMultiset.create();multiset1.add(&quot;a&quot;, 2);Multiset&lt;String&gt; multiset2 = HashMultiset.create();multiset2.add(&quot;a&quot;, 5);multiset1.containsAll(multiset2); // returns true: all unique elements are contained,  // even though multiset1.count(&quot;a&quot;) == 2 &lt; multiset2.count(&quot;a&quot;) == 5Multisets.containsOccurrences(multiset1, multiset2); // returns falsemultiset2.removeOccurrences(multiset1); // multiset2 now contains 3 occurrences of &quot;a&quot;multiset2.removeAll(multiset1); // removes all occurrences of &quot;a&quot; from multiset2, even though multiset1.count(&quot;a&quot;) == 2multiset2.isEmpty(); // returns true</code></pre><p>其他Multisets中的工具方法包括：</p><table><thead><tr><th>方法</th><th>说明</th></tr></thead><tbody><tr><td>copyHighestCountFirst(Multiset)</td><td>返回出现频次倒序的Multiset的不可变拷贝</td></tr><tr><td>unmodifiableMultiset(Multiset)</td><td>返回Multiset的不可变视图</td></tr><tr><td>unmodifiableSortedMultiset(SortedMultiset)</td><td>返回排序Multiset的不可变视图</td></tr></tbody></table><pre><code>Multiset&lt;String&gt; multiset = HashMultiset.create();multiset.add(&quot;a&quot;, 3);multiset.add(&quot;b&quot;, 5);multiset.add(&quot;c&quot;, 1);ImmutableMultiset&lt;String&gt; highestCountFirst = Multisets.copyHighestCountFirst(multiset);// highestCountFirst, 类似它的entrySet和elementSet, 以顺序{&quot;b&quot;, &quot;a&quot;, &quot;c&quot;}遍历元素</code></pre><h3 id="Multimaps"><a href="#Multimaps" class="headerlink" title="Multimaps"></a>Multimaps</h3><h4 id="index"><a href="#index" class="headerlink" title="index"></a>index</h4><p>对比Maps.uniqueIndex, Multimaps.index(Iterable, Function)解决的是当你想要找到某个属性上相同的所有对象时，也有可能是唯一的单独对象.<br>假如想通过长度来进行分组：<br>    ImmutableSet<string> digits = ImmutableSet.of(“zero”, “one”, “two”, “three”, “four”,<br>      “five”, “six”, “seven”, “eight”, “nine”);<br>    Function&lt;String, Integer&gt; lengthFunction = new Function&lt;String, Integer&gt;() {<br>      public Integer apply(String string) {<br>        return string.length();<br>      }<br>    };<br>    ImmutableListMultimap&lt;Integer, String&gt; digitsByLength = Multimaps.index(digits, lengthFunction);<br>    `/*</string></p><pre><code>* digitsByLength maps:*  3 =&gt; {&quot;one&quot;, &quot;two&quot;, &quot;six&quot;}*  4 =&gt; {&quot;zero&quot;, &quot;four&quot;, &quot;five&quot;, &quot;nine&quot;}*  5 =&gt; {&quot;three&quot;, &quot;seven&quot;, &quot;eight&quot;}*/`</code></pre><h4 id="invertFrom"><a href="#invertFrom" class="headerlink" title="invertFrom"></a>invertFrom</h4><p>由于Multimap可以映射许多key到某个value，以及一个key到多个values，所有反转一个multimap会很有用。Guava提供invertFrom(Multimap toInvert, Multimap dest)来实现该功能。</p><p>NOTE: 如果使用ImmutableMultimap，则考虑使用ImmutableMultimap.inverse()替换.</p><pre><code>ArrayListMultimap&lt;String, Integer&gt; multimap = ArrayListMultimap.create();multimap.putAll(&quot;b&quot;, Ints.asList(2, 4, 6));multimap.putAll(&quot;a&quot;, Ints.asList(4, 2, 1));multimap.putAll(&quot;c&quot;, Ints.asList(2, 5, 3));TreeMultimap&lt;Integer, String&gt; inverse = Multimaps.invertFrom(multimap, TreeMultimap.&lt;String, Integer&gt; create());// 注意我们自己选择实现，所有如果用TreeMultimap会得到如下有序的结果：`/* * inverse maps: *  1 =&gt; {&quot;a&quot;} *  2 =&gt; {&quot;a&quot;, &quot;b&quot;, &quot;c&quot;} *  3 =&gt; {&quot;c&quot;} *  4 =&gt; {&quot;a&quot;, &quot;b&quot;} *  5 =&gt; {&quot;c&quot;} *  6 =&gt; {&quot;b&quot;} */`</code></pre><h4 id="forMap方法"><a href="#forMap方法" class="headerlink" title="forMap方法"></a>forMap方法</h4><p>假如想对一个Map使用Multimap的方法该怎么办？forMap(map)视图将一个Map当做SetMultiMap。这会很有用，如，和Multimaps.invertFrom结合使用：</p><pre><code>Map&lt;String, Integer&gt; map = ImmutableMap.of(&quot;a&quot;, 1, &quot;b&quot;, 1, &quot;c&quot;, 2);SetMultimap&lt;String, Integer&gt; multimap = Multimaps.forMap(map);// multimap maps [&quot;a&quot; =&gt; {1}, &quot;b&quot; =&gt; {1}, &quot;c&quot; =&gt; {2}]Multimap&lt;Integer, String&gt; inverse = Multimaps.invertFrom(multimap, HashMultimap.&lt;Integer, String&gt; create());// inverse maps [1 =&gt; {&quot;a&quot;, &quot;b&quot;}, 2 =&gt; {&quot;c&quot;}]</code></pre><h4 id="Wrappers包装"><a href="#Wrappers包装" class="headerlink" title="Wrappers包装"></a>Wrappers包装</h4><h3 id="Tables"><a href="#Tables" class="headerlink" title="Tables"></a>Tables</h3><p>与Multimaps.newXXXMultimap(Map, Supplier)工具方法类似， Tables.newCustomTable(Map, Supplier<map>)允许你指定一个你喜欢的row或者column map的Table实现。</map></p><pre><code>// use LinkedHashMaps instead of HashMapsTable&lt;String, Character, Integer&gt; table = Tables.newCustomTable(  Maps.&lt;String, Map&lt;Character, Integer&gt;&gt;newLinkedHashMap(),  new Supplier&lt;Map&lt;Character, Integer&gt;&gt; () {    public Map&lt;Character, Integer&gt; get() {      return Maps.newLinkedHashMap();    }  });</code></pre><h4 id="transpose变换"><a href="#transpose变换" class="headerlink" title="transpose变换"></a>transpose变换</h4><p>The transpose(Table&lt;R, C, V&gt;) 方法允许你将Table&lt;R, C, V&gt;变换成Table&lt;C, R, V&gt;.</p><h4 id="Wrappers"><a href="#Wrappers" class="headerlink" title="Wrappers"></a>Wrappers</h4><p>大多数情形使用ImmutableTable</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;JDK集合框架提供了许多工具，方便实用，Guava相比提供了更多的工具，适用于所有集合，这也是guava更流行和成熟的部分。&lt;br&gt;
    
    </summary>
    
      <category term="译" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/"/>
    
      <category term="guava" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/guava/"/>
    
    
      <category term="guava" scheme="http://zhangxiangnan.com/tags/guava/"/>
    
      <category term="CollectionUtilities" scheme="http://zhangxiangnan.com/tags/CollectionUtilities/"/>
    
  </entry>
  
  <entry>
    <title>guava的新集合类型</title>
    <link href="http://zhangxiangnan.com/2016/10/21/guava/collections/guava%E7%9A%84%E6%96%B0%E9%9B%86%E5%90%88%E7%B1%BB%E5%9E%8B/"/>
    <id>http://zhangxiangnan.com/2016/10/21/guava/collections/guava的新集合类型/</id>
    <published>2016-10-21T05:20:15.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>guava的新集合类型Multiset、Multimap、BiMap、Table、ClassToInstanceMap、RangeSet<br><a id="more"></a><br>Guava介绍了许多jdk中没有的但是很有用处的新集合类型，这些新集合类型和JDK集合框架和平共处，并且没有硬塞进任何东西到JDK集合抽象类中。<br>一般来说，Guava的集合实现严格遵循了JDK接口契约。</p><h3 id="Multiset"><a href="#Multiset" class="headerlink" title="Multiset"></a>Multiset</h3><p>传统的JAVA惯例手法来统计一文档中的单词出现频率，类似如下：</p><pre><code>Map&lt;String, Integer&gt; counts = new HashMap&lt;String, Integer&gt;();for (String word : words) {  Integer count = counts.get(word);  if (count == null) {    counts.put(word, 1);  } else {    counts.put(word, count + 1);  }}</code></pre><p>这种方式笨拙，容易出错，且不支持收集各种有用的统计信息，如单词总数，而Guava做的更好。<br>Guava提供一个新集合类型，Multiset，支持元素的多次添加，数学中的multiset定义为元素可以出现不止一次的set。在multiset中，类似sets和tuples，元素是无序的，如multisets {a, a, b} 和 {a, b, a} 是equal的。</p><p>有2种方式理解这个概念：</p><ul><li>类似一个没元素顺序的约束的ArrayList<e></e></li><li>类似一个Map&lt;E, Integer&gt;,key、value分别表示元素和元素出现的次数</li></ul><p>Guava的Multiset API结合了这2种方式，如下：</p><ul><li>当以正常集合对待它，Multiset表现的更像是一个无序的ArrayList：<ul><li>调用add(E),则该元素出现的次数加1.</li><li>Multiset的iterator()方法迭代每一个元素的每一次出现。</li><li>size()方法是所有元素的所有出现次数的总和。</li></ul></li><li>额外的query操作，及性能特性，比较像一个Map&lt;E, Integer&gt;.<ul><li>count(Object)方法返回指定元素的出现次数。HashMultiset的count方法效率是O(1)，TreeMultiset的count方法效率是O(log n)等。</li><li>entrySet() 类似Map的entrySet。</li><li>elementSet()返回multiset的一个去重的元素集合<code>Set&lt;E&gt;</code>，类似于Map的keySet()</li><li>针对不重复的元素来说，Multiset实现类内存消耗（空间复杂度）随着不重复元素的数目线性增长,即重复存储相同对象。</li></ul></li></ul><p>尤其需要注意，Multiset是和JDK集合Collection的接口规范完全一致的，除了JDK自身的早期版本的极少见情形 – 特别的，TreeMultiset，类似TreeSet，使用comparison来比较是否相等，而不是Object的equals方法. 特别地，Multiset.addAll(Collection)在集合中的每个元素每出现一次，该元素的出现次数加一，该方式比上面的使用Map+for循环更简便。</p><p>其他方法说明如下：</p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td>count(E)</td><td>对添加到multiset的元素出现次数进行计数</td></tr><tr><td>elementSet()</td><td>返回Multiset<e>的去重元素的集合(一个<code>Set&lt;E&gt;</code>)</e></td></tr><tr><td>entrySet()</td><td>类似于Map.entrySet(),返回<code>Set&lt;Multiset.Entry&lt;E&gt;&gt;</code>，一个包含支持getElement()和getCount()方法的entries的set集合</td></tr><tr><td>add(E, int)</td><td>给指定元素添加指定数目的出现次数</td></tr><tr><td>remove(E, int)</td><td>移除指定元素的指定数目的出现次数</td></tr><tr><td>setCount(E, int)</td><td>设置指定元素的出现次数为非负值</td></tr><tr><td>size()</td><td>返回Multiset中所有元素的总共的出现次数和。</td></tr></tbody></table><h4 id="Multiset不是一个Map"><a href="#Multiset不是一个Map" class="headerlink" title="Multiset不是一个Map"></a>Multiset不是一个Map</h4><p>注意Multiset<e>不是一个Map&lt;E, Integer&gt;，尽管Multiset实现有这部分的功能。Multiset是一个真正的集合类型， 符合所有的相关的接口协议，其他值得注意的区别如下：</e></p><ul><li>一个Multiset<e>的元素只能有正数的count计数，不能是负数。计数0不认为在multiset中，在elementSet()、entrySet视图中计数为0的元素会被过滤。</e></li><li>multiset.size() 返回集合的大小，就是集合中所有元素的计数的总和。如果想要得到去重后元素的数目（不重复元素的数目），使用elementSet().size()得到。(所以，例如，add(E)方法会使multiset.size()的数目加一)</li><li>multiset.iterator() 会遍历每一个元素的每一次出现，所以迭代的次数等于multiset.size()</li><li><code>Multiset&lt;E&gt;</code> 支持添加元素，移除元素，或者直接设置元素的计数count。setCount(elem, 0)等价于移除该元素的所有次数的出现。</li><li>multiset.count(elem) 对于不在multiset中的元素始终返回0</li></ul><h4 id="实现类"><a href="#实现类" class="headerlink" title="实现类"></a>实现类</h4><p>Guava提供许多Multiset的实现，大体上对应于JDK的map实现类。</p><table><thead><tr><th>Map</th><th>Corresponding Multiset</th><th>Supports null elements</th></tr></thead><tbody><tr><td>HashMap</td><td>HashMultiset</td><td>Yes</td></tr><tr><td>TreeMap</td><td>TreeMultiset</td><td>Yes (if the comparator does)</td></tr><tr><td>LinkedHashMap</td><td>LinkedHashMultiset</td><td>Yes</td></tr><tr><td>ConcurrentHashMap</td><td>ConcurrentHashMultiset</td><td>No</td></tr><tr><td>ImmutableMap</td><td>ImmutableMultiset</td><td>No</td></tr></tbody></table><h3 id="SortedMultiset"><a href="#SortedMultiset" class="headerlink" title="SortedMultiset"></a>SortedMultiset</h3><p>SortedMultiset<br>SortedMultiset是Multiset接口的一个变种，支持高效地获取指定范围的子multisets。例如你可以使用latencies.subMultiset(0, BoundType.OPEN, 100, BoundType.OPEN).size()来获取你的网站100ms下的延迟的请求的子集，然后和latecies.size()比较来获取百分比。</p><p>TreeMultiset实现了SortedMultiset接口。ImmutableSortedMultiset目前正在测试GWT的兼容性。</p><h3 id="Multimap"><a href="#Multimap" class="headerlink" title="Multimap"></a>Multimap</h3><p>每一个有经验的java程序员很可能自己实现了<code>Map&lt;K, List&lt;V&gt;&gt;</code>或者<code>Map&lt;K, Set&lt;V&gt;&gt;</code>，或者直接使用那种笨拙的结构。Guava的Multimap框架使处理从keys到多个值得映射关系变得简单。一个Multimap是一个普遍的方式来关联keys和任意多values。</p><p>从概念上有2种方式来理解Multimap：单个key到单个value的映射集合：</p><pre><code>a -&gt; 1 a -&gt; 2 a -&gt; 4 b -&gt; 3 c -&gt; 5</code></pre><p>或者是唯一的单个key到values集合的映射关系：</p><pre><code>a -&gt; [1, 2, 4] b -&gt; [3] c -&gt; [5]</code></pre><p>一般来说, Multimap接口是理解这种观念最好的视图，但是也允许你使用asMap()以另一种方式来看，asMap()返回<code>Map&lt;K, Collection&lt;V&gt;&gt;</code>。<br>最重要的是， 不会出现一个key对应一个空集合的情况：一个key要么映射到至少一个value，要么在Multimap中该key不存在。<br>如果你想区分key存在但是没对应的values和key就不存在这2种情况，更适合的数据结构很可能是Graph图（支持孤立点）。</p><p>我们很少直接使用Multimap接口，更多的是，我们会使用ListMultimap或者SetMultimap，相应地keys映射到List和Set。</p><h4 id="Modifying-修改API"><a href="#Modifying-修改API" class="headerlink" title="Modifying 修改API"></a>Modifying 修改API</h4><p>Multimap.get(key)返回和给定key相关联的values视图，即时目前什么也没有。对于ListMultiMap返回一个list，SetMultimap返回一个set。</p><p>修改操作直接对底层的Multimap进行写。如：    </p><pre><code>Set&lt;Person&gt; aliceChildren = childrenMultimap.get(alice);aliceChildren.clear();aliceChildren.add(bob);aliceChildren.add(carol);</code></pre><p>对底层的MultiMap直接进行写。</p><p>其他修改multimap的方式（更直接）包括：</p><table><thead><tr><th>方法</th><th>描述</th><th>等同于</th></tr></thead><tbody><tr><td>put(K, V)</td><td>添加key到value的映射</td><td>multimap.get(key).add(value)</td></tr><tr><td><code>putAll(K, Iterable&lt;V&gt;</code>)</td><td>依次添加key到每一个value的映射</td><td>Iterables.addAll(multimap.get(key), values)</td></tr><tr><td>remove(K, V)</td><td>移除key到value的映射，若multimap改变了则返回true，否则false</td><td>multimap.get(key).remove(value)</td></tr><tr><td>removeAll(K)</td><td>移除所有与key映射的所有value，返回的集合可以修改，但是修改它不会影响multimap (返回适合的集合类型)</td><td>multimap.get(key).clear()</td></tr><tr><td><code>replaceValues(K, Iterable&lt;V&gt;</code>)</td><td>清空key到所有value的映射，然后设置key到每个给定value的映射。返回被清空的key关联的vlaues集合</td><td>multimap.get(key).clear(); Iterables.addAll(multimap.get(key), values)</td></tr></tbody></table><h4 id="views视图"><a href="#views视图" class="headerlink" title="views视图"></a>views视图</h4><p>MultiMap支持一系列强大的视图：</p><ul><li>asMap方法将所有的<code>Multimap&lt;K,V&gt;当做一个Map&lt;K,Collection&gt;&gt;</code>，其返回值支持remove、并且对返回集合的改变会影响到multimap，但是其不支持put或者putall。更关键的是，当你想要对于不存在的key返回null时你可以使用asMap().get(key)，而不是返回一个干净的空集合。(你应当强转asMap.get(key)的返回值到合适的集合类型– 对应SetMultiMap的是set，ListMultiMap的List – 但是JDK的类型系统不允许ListMultiMap返回<code>Map&lt;K, List&lt;V&gt;&gt;</code>)</li><li>entries方法返回MultiMap所有的entries为一个<code>Collection&lt;Map.Entry&lt;K, V&gt;&gt;</code>（对SetMultiMap来说，Collection实际上是个Set)</li><li>keySet将MultiMap中所有的key去重，返回一个set。</li><li>keys方法将MultiMap中的所有keys看成一个MultiSet，并且和key相关联的values有着相同的多样性。 元素可以从MultiSet移除，但是不能添加；改变会导致原MultiMap改变。</li><li>values()方法返回MultiMap里所有的values为一个多重集的<code>Collection&lt;V&gt;</code>，所有的value都在这一个Collection。类似于Iterables.concat(multimap.asMap.values())</li></ul><h4 id="Multimap不仅仅是map"><a href="#Multimap不仅仅是map" class="headerlink" title="Multimap不仅仅是map"></a>Multimap不仅仅是map</h4><p>A <code>Multimap&lt;K, V&gt;</code> 不仅仅是一个<code>Map&lt;K, Colleciton&lt;V&gt;&gt;</code>，尽管这样的一个map可能被Multimap的某个实现使用，显著区别如下：</p><ul><li>Multimap.get(key) 始终返回一个非null的，可能的空集合. 这并不意味着multimap花费了任何内存关联那个key，相反，返回的集合是一个允许你添加key映射的视图。</li><li>如果更倾向于类似Map的行为，如对于multimap中不存在的key返回null，可以使用asMap()视图来得到一个<code>Map&lt;K,Collection&lt;V&gt;&gt;</code>。（或者，从ListMultiMap得到一个<code>Map&lt;K,List&lt;v&gt;&gt;</code>，使用静态Multimaps.asMap()方法，类似方法适用于SetMultimap、SetMultimap、SortedSetMultimap)</li><li>Multimap.containsKey(key)仅仅当有任何元素和key相关联时才返回true。特别地，如果一个k以前在multimap存在映射关系，但是后来移除映射关系了，该方法返回false</li><li>Multimap.entries()返回multimap中的所有key的所有entries。如果想要得到所有key-collection entires，使用asMap.entrySet().</li><li>Multimap.size()返回MultiMap中所有的entries数目，不是对key去重后的数目。使用 Multimap.keySet().size() 可以得到去重后的key数目</li></ul><h4 id="实现类-1"><a href="#实现类-1" class="headerlink" title="实现类"></a>实现类</h4><p>Multimap提供很多实现，可以在大多数想使用<code>Map&lt;K, Colleciton&lt;V&gt;&gt;</code>的场合使用。</p><table><thead><tr><th>实现</th><th>Key行为类似</th><th>Values行为类似</th></tr></thead><tbody><tr><td>ArrayListMultimap</td><td>HashMap</td><td>ArrayList，同key对应values有顺序可重复</td></tr><tr><td>HashMultimap</td><td>HashMap</td><td>HashSet,同key对应values不可重复</td></tr><tr><td>LinkedListMultimap</td><td>LinkedHashMap</td><td>LinkedList 同key对应value有顺序可重复</td></tr><tr><td>LinkedHashMultimap</td><td>LinkedHashMap</td><td>LinkedHashSet 同key对应value有顺序不重复</td></tr><tr><td>TreeMultimap</td><td>TreeMap</td><td>TreeSet，同key对应的value有顺序</td></tr><tr><td>ImmutableListMultimap</td><td>ImmutableMap</td><td>ImmutableList</td></tr><tr><td>ImmutableSetMultimap</td><td>ImmutableMap</td><td>ImmutableSet</td></tr></tbody></table><p>每一个实现，除了不可变的那些，都支持key和value为null。</p><ul><li><p>LinkedListMultimap.entries()，对于不重复的key value保持了迭代时的顺序。</p></li><li><p>LinkedHashMultimap，保持了映射项entries的插入顺序，包括键keys的插入顺序、和任意一个key关联的values所有值的插入顺序。</p></li></ul><p>要知道以上的所有实现中并不是所有的实现都通过<code>Map&lt;K, Collection&lt;V&gt;&gt;</code>（特别的，几个MultiMap实现使用了自定义hash表来最小化开销）</p><p>如果你想要更多自定义，使用MultiMaps.newMultimap(Map, Supplier&lt;Collection)或者list、set自定义实现来支撑自定义multimap。</p><h3 id="BiMap-双向Map"><a href="#BiMap-双向Map" class="headerlink" title="BiMap 双向Map"></a>BiMap 双向Map</h3><p>传统映射values到key的方式是维护2个独立的map，保持他们同步，但是这种方式当一个value已经在map里存在时令人极度困惑且感觉像是bug。如：   </p><pre><code>Map&lt;String, Integer&gt; nameToId = Maps.newHashMap();Map&lt;Integer, String&gt; idToName = Maps.newHashMap();nameToId.put(&quot;Bob&quot;, 42);idToName.put(42, &quot;Bob&quot;);// 如果“Bob”或42已经存在，当我们刚好又忘记保持他们同步的时候，就会发生奇怪的错误。</code></pre><p>A <code>BiMap&lt;K, V&gt;</code> 是一个<code>Map&lt;K,V&gt;</code>：</p><ul><li>允许你使用inverse()方法来得到反转视图BiMap&lt;V,K&gt;</li><li>确保values是不重复的，使values类似一个Set</li></ul><p>所有，BiMap的特点是key、value都不能重复。<br>若尝试添加一个key到已经存在的value映射会报参数异常。若想删除预先存在的entry（指定的value），则使用BiMap.forcePut(key, value)。</p><pre><code>public static void main(String[] args) {       BiMap&lt;String, String&gt; biMap = HashBiMap.create();       biMap.put(&quot;1&quot;, &quot;2&quot;);       biMap.put(&quot;2&quot;, &quot;2&quot;);       System.out.println(biMap);   }   #报参数异常   public static void main(String[] args) {           BiMap&lt;String, String&gt; biMap = HashBiMap.create();           biMap.put(&quot;1&quot;, &quot;2&quot;);           String s = biMap.inverse().get(&quot;1&quot;);           System.out.println(s);           System.out.println(biMap.inverse().get(&quot;2&quot;));       }       #null       #1</code></pre><h4 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h4><table><thead><tr><th>Key-Value Map实现类</th><th>Value-Key Map 实现类</th><th>对应BiMap</th></tr></thead><tbody><tr><td>HashMap</td><td>HashMap</td><td>HashBiMap</td></tr><tr><td>ImmutableMap</td><td>ImmutableMap</td><td>ImmutableBiMap</td></tr><tr><td>EnumMap</td><td>EnumMap</td><td>EnumBiMap</td></tr><tr><td>EnumMap</td><td>HashMap</td><td>EnumHashBiMap</td></tr></tbody></table><p>Note: BiMap的工具方法如synchronizedBiMap在Maps里实现。</p><h3 id="表格Table"><a href="#表格Table" class="headerlink" title="表格Table"></a>表格Table</h3><pre><code>Table&lt;DateOfBirth, LastName, PersonalRecord&gt; records = HashBasedTable.create();records.put(someBirthday, &quot;Schmo&quot;, recordA);records.put(someBirthday, &quot;Doe&quot;, recordB);records.put(otherBirthday, &quot;Doe&quot;, recordC);records.row(someBirthday); // returns a Map mapping &quot;Schmo&quot; to recordA, &quot;Doe&quot; to recordBrecords.column(&quot;Doe&quot;); // returns a Map mapping someBirthday to recordB, otherBirthday to recordC</code></pre><p>通常，当你想同一时间对不止一个的key检索的时候，你会想出使用类似<code>Map&lt;FirstName, Map&lt;LastName, Person&gt;&gt;</code>，这种方式丑陋难以使用。 Guava提供一个新的集合类型，表格Table，支持“行row”类型，和“column”列类型。表格Table支持很多种视图来让你可以使用从任何角度的数据，包括：</p><ul><li>rowMap(), 该方法将<code>Table&lt;R,C,V&gt;</code>视作<code>Table&lt;R,Map&lt;C,V&gt;&gt;</code>。类似rowKeySet返回一个Set<r>.</r></li><li>row(r)返回一个非null的Map&lt;C,V&gt;，对这个Map的写操作将穿透到底层Table。</li><li>相似的列方法提供如下： columnMap(), columnKeySet(), column(c). (基于column的访问稍微比基于行的访问效率低)</li><li>cellSet() 返回一个Table.Cell&lt;R,C,V&gt;的set集合，Cell很像Map.Entry，但是区分row和column key。</li></ul><p>几种Table实现如下：</p><ul><li>HashBasedTable, 底层使用<code>HashMap&lt;R,HashMap&lt;C,V&gt;&gt;</code>（Guava20.0使用LinkedHashMap&lt;R, LinkedHashMap&lt;C,V&gt;&gt;实现）。</li><li>TreeBasedTable,底层使用<code>TreeMap&lt;R, TreeMap&lt;C, V&gt;&gt;</code>.</li><li>ImmutableTable,底层使用<code>ImmutableMap&lt;R, ImmutableMap&lt;C, V&gt;&gt;</code>. (Note: ImmutableTable针对稀疏和稠密的数据集有单独的优化实现)</li><li>ArrayTable, 需要在构建的时候传递全部的row和column数据，当数据是紧密的数据时通过2维数组来提高速度、内存利用率。ArrayTable和其他实现原理有稍微不同.</li></ul><h3 id="ClassToInstanceMap-类型到实例的映射Map"><a href="#ClassToInstanceMap-类型到实例的映射Map" class="headerlink" title="ClassToInstanceMap 类型到实例的映射Map"></a>ClassToInstanceMap 类型到实例的映射Map</h3><p>有时，map的key并不都是相同的类型，可能有多个类型，你可能想映射类型到values，可以使用ClassToInstanceMap.<br>除了扩展Map接口，ClassToInstanceMap提供方法T <code>getInstance(Class&lt;T&gt;</code>) 和T putInstance(Class<t>, T)，省去了手动进行类型转换。</t></p><p>ClassToInstanceMap只有一个名称为B的类型参数，代表map管理的类型的上层限制，如：</p><pre><code>ClassToInstanceMap&lt;Number&gt; numberDefaults = MutableClassToInstanceMap.create();numberDefaults.putInstance(Integer.class,Integer.valueOf(0);</code></pre><p>严格说，ClassToInstanceMap<b>实现了接口<code>Map&lt;Class&lt;? extends B&gt;, B&gt;</code> – 或者是从B的子类型class到B的映射的一个map，这会令ClassToInstanceMap的反省类型有些令人困惑，但记住B始终是map中类型的上层限制（上界）–通常，B就是Object类。</b></p><p>Guava提供MutableClassToInstanceMap、ImmutableClassToInstanceMap。</p><p>注意: 像其他Map&lt;Class, Object&gt;，一个ClassToInstanceMap可能包含基本类型，基本类型和对应包装类型可能对应不同的values。</p><h3 id="RangeSet"><a href="#RangeSet" class="headerlink" title="RangeSet"></a>RangeSet</h3><p>A RangeSet描述了一个不连续的、非空的范围集合，当往一个可变的RangeSet添加一个范围的时候，任何可连续的范围都被合并到一起，空范围被忽略，如下：</p><pre><code>RangeSet&lt;Integer&gt; rangeSet = TreeRangeSet.create(); rangeSet.add(Range.closed(1, 10)); // {[1, 10]} rangeSet.add(Range.closedOpen(11, 15)); // disconnected range: {[1, 10], [11, 15)} rangeSet.add(Range.closedOpen(15, 20)); // connected range; {[1, 10], [11, 20)} rangeSet.add(Range.openClosed(0, 0)); // empty range; {[1, 10], [11, 20)} rangeSet.remove(Range.open(5, 10)); // splits [1, 10]; {[1, 5], [10, 10], [11, 20)}</code></pre><p>记住想要合并范围如Range.closed(1, 10) and Range.closedOpen(11, 15), 必须首先使用Range.canonical(DiscreteDomain)预处理范围，如使用DiscreteDomain.integers().</p><pre><code>RangeSet&lt;Integer&gt; rangeSet = TreeRangeSet.create();    rangeSet.add(Range.closed(1, 10).canonical(DiscreteDomain.integers())); // {[1, 10]}    rangeSet.add(Range.closedOpen(11, 15)); // disconnected range: {[1, 10], [11, 15)}    System.out.println(rangeSet);//[[1‥15)]    // canonical可以理解为规范化，效果如下：    System.out.println(Range.closed(1, 10).canonical(DiscreteDomain.integers()));[1‥11)    System.out.println(Range.closed(1, 10));[1‥10]</code></pre><p>注意: RangeSet 在GWT下和JDK1.5之下都不被支持；RangeSet需要全部NavigableMap的特征在JDK1.6中。</p><h4 id="视图"><a href="#视图" class="headerlink" title="视图"></a>视图</h4><p>RangeSet实现支持很多视图：</p><ul><li>complement(): 返回补集视图，也是一个RangeSet，它包含不连续的、非空范围.</li><li>subRangeSet(<code>Range&lt;C&gt;</code>): 返回RangeSet和指定范围的range的交叉集合，这会返回惯例的排序的headSet、subSet、tailSet视图。</li><li>asRanges():将RangeSet视作<code>Set&lt;Range&lt;C&gt;&gt;</code>，可以迭代遍历。</li><li>asSet(<code>DiscreteDomain&lt;C&gt;</code>) (仅仅对ImmutableRangeSet而言): 将<code>RangeSet&lt;C&gt;</code>当做<code>ImmutableSortedSet&lt;C&gt;</code>看待ranges中的元素而非range自己. (这个操作如果DiscreteDomain和RangeSet同时是无穷大或无穷小时不支持)</li></ul><h4 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h4><p>除了视图，RangeSet还直接支持几种查询操作 ，最常用的：</p><ul><li>contains(C): RangeSet的最基本的方法，查询RangeSet中的任意一个range是否包含指定元素。</li><li>rangeContaining(C): 返回包含指定元素的Range，无返回null。</li><li>encloses(<code>Range&lt;C&gt;</code>): 测试RangeSet中的任意Range是否包围指定Range</li><li>span(): 返回包围RangeSet中任意的Range的最小Range。</li></ul><h3 id="RangeMap"><a href="#RangeMap" class="headerlink" title="RangeMap"></a>RangeMap</h3><p>RangeMap是描述不相交、非空范围到values的映射关系。不同于RangeSet，RangeMap不合并相邻映射，即使相邻的Ranges映射到相同的values。如：</p><pre><code>RangeMap&lt;Integer, String&gt; rangeMap = TreeRangeMap.create();rangeMap.put(Range.closed(1, 10), &quot;foo&quot;); // {[1, 10] =&gt; &quot;foo&quot;}rangeMap.put(Range.open(3, 6), &quot;bar&quot;); // {[1, 3] =&gt; &quot;foo&quot;, (3, 6) =&gt; &quot;bar&quot;, [6, 10] =&gt; &quot;foo&quot;}rangeMap.put(Range.open(10, 20), &quot;foo&quot;); // {[1, 3] =&gt; &quot;foo&quot;, (3, 6) =&gt; &quot;bar&quot;, [6, 10] =&gt; &quot;foo&quot;, (10, 20) =&gt; &quot;foo&quot;}rangeMap.remove(Range.closed(5, 11)); // {[1, 3] =&gt; &quot;foo&quot;, (3, 5) =&gt; &quot;bar&quot;, (11, 20) =&gt; &quot;foo&quot;}Views</code></pre><p>RangeMap提供2中视图：</p><ul><li>asMapOfRanges(): 将RangeMap当做一个<code>Map&lt;Range&lt;K&gt;,V&gt;</code>，当想要迭代RangeMap时可以使用</li><li>subRangeMap(<code>Range&lt;K&gt;</code>) 将RangeMap和指定Range的交集当做一个RangeMap，惯例返回headMap, subMap, and tailMap.</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;guava的新集合类型Multiset、Multimap、BiMap、Table、ClassToInstanceMap、RangeSet&lt;br&gt;
    
    </summary>
    
      <category term="译" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/"/>
    
      <category term="guava" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/guava/"/>
    
    
      <category term="guava" scheme="http://zhangxiangnan.com/tags/guava/"/>
    
      <category term="newCollectionsTypes" scheme="http://zhangxiangnan.com/tags/newCollectionsTypes/"/>
    
  </entry>
  
  <entry>
    <title>guava的不可变集合</title>
    <link href="http://zhangxiangnan.com/2016/10/20/guava/collections/guava%E7%9A%84%E4%B8%8D%E5%8F%AF%E5%8F%98%E9%9B%86%E5%90%88/"/>
    <id>http://zhangxiangnan.com/2016/10/20/guava/collections/guava的不可变集合/</id>
    <published>2016-10-20T05:10:12.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>guava的不可变集合讲解-Immutable*<br><a id="more"></a></p><h3 id="jdk的Collections-ImmutableXXX例子"><a href="#jdk的Collections-ImmutableXXX例子" class="headerlink" title="jdk的Collections.ImmutableXXX例子"></a>jdk的Collections.ImmutableXXX例子</h3><pre><code>package cn.zxn.guava.collections;import java.util.ArrayList;import java.util.Collections;import java.util.List;// jdk的方法修改原始集合会导致不可变集合发生变化public class ImmutableCollectionsExplained {public static void main(String[] args) {    List&lt;String&gt; originalList = new ArrayList&lt;String&gt;();    originalList.add(&quot;S&quot;);    originalList.add(&quot;Q&quot;);    List&lt;String&gt; unmodifiableList = Collections.unmodifiableList(originalList);    System.out.println(unmodifiableList.size());// 输出2    // 原始集合新增一个item    originalList.add(&quot;T&quot;);    // 不可变集合的大小变化,变为3    System.out.println(unmodifiableList.size());// 输出3}}</code></pre><h3 id="guava例子"><a href="#guava例子" class="headerlink" title="guava例子:"></a>guava例子:</h3><pre><code>// 不可变set声明示例public static final ImmutableSet&lt;String&gt; COLOR_NAMES = ImmutableSet.of(  &quot;red&quot;,  &quot;orange&quot;,  &quot;yellow&quot;,  &quot;green&quot;,  &quot;blue&quot;,  &quot;purple&quot;); class Foo {    final ImmutableSet&lt;Bar&gt; immutableBars;    Foo(Set&lt;Bar&gt; bars) {      this.immutableBars = ImmutableSet.copyOf(bars);      // 保护性拷贝，使集合immutableBars不可被删除增加元素，但是可以调用元素自身的方法进行元素的修改    }  }</code></pre><h3 id="使用不可变对象的原因"><a href="#使用不可变对象的原因" class="headerlink" title="使用不可变对象的原因"></a>使用不可变对象的原因</h3><p>不可变对象有许多好处，包括：</p><ul><li>安全地被不受信任的类库使用</li><li>线程安全：可以在多线程中使用而不用考虑(静态条件)加锁同步</li><li>不需要支持可变性（自动扩充等机制），可以更好的节省空间、时间。所有的不可变集合实现都比对应的可变集合更能高效的使用内存。</li><li>可以当做常量使用，其会保持不变</li></ul><p>对对象objects做不可变的拷贝使一项很好的保护性编程技巧。Guava为每一个标准的集合类型提供简单、易用及不可变的版本，包含Guava自己的集合。</p><p>JDK提供Collections.unmodifiableXXX类似的方法，但在我们看来:</p><ul><li>笨拙且冗长：在你想做保护性拷贝的地方使用起来不方便</li><li>不安全：返回的集合仅仅在其他地方没有持有对原始集合的引用前提下才真正不可变，guava则对原始集合的操作（删除、增加）不影响不可变集合。</li><li><p>效率低下：内部数据结构仍然有可变集合会有的开销，包括并发修改检查，hash表中额外的空间等。</p><p><strong>当你不想改变一个集合，或者希望一个集合保持不变，进行保护性拷贝原集合到一个不可变集合是一个不错的实践</strong></p><p>重点：每一个Guava不可变集合实现类不能存放null值。我们做了一个彻底的对Google内部代码库调查，发现null元素仅仅在5%的情形里允许，其余95%则是若是null则会快速失败。如果你需要使用null值，考虑使用Collections.unmodifiableList及它的相关方法，可以允许null。更多细节见：<a href="https://github.com/google/guava/wiki/UsingAndAvoidingNullExplained" target="_blank" rel="noopener">here</a></p></li></ul><h3 id="如何使用"><a href="#如何使用" class="headerlink" title="如何使用"></a>如何使用</h3><p>一个InmutableXXX集合可以通过以下几种方式创建：</p><ul><li>使用copyOf方法，例如，ImmutableSet.copyOf(set)；</li><li>使用Of方法，如，ImmutableSet.of(“a”, “b”, “c”)或者ImmutableMap.of(“a”, “1”, “b”, 2);</li><li><p>使用一个创建器Builder，如，   </p><pre><code>public static final ImmutableSet&lt;Color&gt; GOOGLE_COLORS =   ImmutableSet.&lt;Color&gt;builder()       .addAll(WEBSAFE_COLORS)       .add(new Color(0, 191, 255))       .build();</code></pre><p>除了排序集合，元素顺序在构建时被保持。<br>如，ImmutableSet.of(“a”, “b”, “c”, “a”, “d”, “b”) 迭代输出会得到结果： “a”, “b”, “c”, “d”.</p></li></ul><h4 id="智能拷贝"><a href="#智能拷贝" class="headerlink" title="智能拷贝"></a>智能拷贝</h4><p>  有必要记住ImmutableXXX.copyOf尝试避免拷贝数据（如果这么做安全地话）– 确切的细节未指定，但是实现通常是智能的，如，</p><pre><code>ImmutableSet&lt;String&gt; foobar = ImmutableSet.of(&quot;foo&quot;, &quot;bar&quot;, &quot;baz&quot;);thingamajig(foobar);void thingamajig(Collection&lt;String&gt; collection) {  ImmutableList&lt;String&gt; defensiveCopy = ImmutableList.copyOf(collection);  ...}</code></pre><p>以上代码中，ImmutableList.copyOf(foobar) 将足够智能仅仅返回foobar.asList()，这是ImmutableSet的一个O(1)时间复杂度的方法。</p><p>ImmutableXXX.copyOf(ImmutableCollection)通常会在符合以下条件下来尝试来避免线性时间的拷贝：</p><ul><li>在常数时间内使用底层数据结果是可能的话：如, ImmutableSet.copyOf(ImmutableList) 不能在常数时间内完成.</li><li>不会导致内存泄漏 – 如，如果你有ImmutableList<string> hugeList（超大list）,如果执行ImmutableList.copyOf(hugeList.subList(0, 10)), 就会执行一个显示的拷贝, 以便避免意外地持有hugeList中不必要的引用</string></li><li>不会改变语义 – 所以ImmutableSet.copyOf(myImmutableSortedSet) 将会执行显示的拷贝, 因为ImmutableSet使用的hashCode()和equals拥有跟ImmutableSortedSet的基于comparator的行为不同的语义。<br>这有助于减少良好的防御性编程风格的性能开销。</li></ul><h4 id="asList"><a href="#asList" class="headerlink" title="asList"></a>asList</h4><p>所有的不可变集合通过asList()返回一个不可变的ImmutableList视图，所以 – 如 – 即使你有使用ImmutableSortedSet排序的数据，你也可以使用sortedSet.asList().get(k).来得到第k个最小的元素。</p><p>返回ImmutableList是普遍的 – 不总是，但是经常 – 一个常数级开销的视图，而不是执行显示拷贝。即，它比平常的List通常更智能 – 如，它底层使用更高效的contains方法。</p><h3 id="jdk可变集合与guava不可变集合对应关系？"><a href="#jdk可变集合与guava不可变集合对应关系？" class="headerlink" title="jdk可变集合与guava不可变集合对应关系？"></a>jdk可变集合与guava不可变集合对应关系？</h3><table><thead><tr><th>接口</th><th>接口属于jdk或guava</th><th>不可变版本</th></tr></thead><tbody><tr><td>Collection</td><td>JDK</td><td>ImmutableCollection</td></tr><tr><td>List</td><td>JDK</td><td>ImmutableList</td></tr><tr><td>Set</td><td>JDK</td><td>ImmutableSet</td></tr><tr><td>SortedSet/NavigableSet</td><td>JDK</td><td>ImmutableSortedSet</td></tr><tr><td>Map</td><td>JDK</td><td>ImmutableMap</td></tr><tr><td>SortedMap</td><td>JDK</td><td>ImmutableSortedMap</td></tr><tr><td>Multiset</td><td>Guava</td><td>ImmutableMultiset</td></tr><tr><td>SortedMultiset</td><td>Guava</td><td>ImmutableSortedMultiset</td></tr><tr><td>Multimap</td><td>Guava</td><td>ImmutableMultimap</td></tr><tr><td>ListMultimap</td><td>Guava</td><td>ImmutableListMultimap</td></tr><tr><td>SetMultimap</td><td>Guava</td><td>ImmutableSetMultimap</td></tr><tr><td>BiMap</td><td>Guava</td><td>ImmutableBiMap</td></tr><tr><td>ClassToInstanceMap</td><td>Guava</td><td>ImmutableClassToInstanceMap</td></tr><tr><td>Table</td><td>Guava</td><td>ImmutableTable</td></tr></tbody></table>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;guava的不可变集合讲解-Immutable*&lt;br&gt;
    
    </summary>
    
      <category term="译" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/"/>
    
      <category term="guava" scheme="http://zhangxiangnan.com/categories/%E8%AF%91/guava/"/>
    
    
      <category term="guava" scheme="http://zhangxiangnan.com/tags/guava/"/>
    
      <category term="immutableCollections" scheme="http://zhangxiangnan.com/tags/immutableCollections/"/>
    
  </entry>
  
  <entry>
    <title>okhttp之常见使用方法（一）</title>
    <link href="http://zhangxiangnan.com/2016/09/25/http/okhttp/okhttp%E4%B9%8B%E5%B8%B8%E8%A7%81%E4%BD%BF%E7%94%A8%E6%96%B9%E6%B3%95%EF%BC%88%E4%B8%80%EF%BC%89/"/>
    <id>http://zhangxiangnan.com/2016/09/25/http/okhttp/okhttp之常见使用方法（一）/</id>
    <published>2016-09-25T13:18:48.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<p>常见的GET同步、异步调用示例，POST提交字符串、提交流示例。<br><a id="more"></a></p><h3 id="GET方式同步调用"><a href="#GET方式同步调用" class="headerlink" title="GET方式同步调用"></a>GET方式同步调用</h3><pre><code>package cn.xiangnan.okhttpdemo;/** * Created by zhangxiangnan on 16/9/25. */import okhttp3.Headers;import okhttp3.OkHttpClient;import okhttp3.Request;import okhttp3.Response;import java.io.IOException;/** * 使用GET方式同步掉调用,输出响应的header及返回内容。 * string方法适合于&lt;=1M的响应,再大的话最好用stream流方式。 */public class SynchronousGet {    private final OkHttpClient client = new OkHttpClient();    public void run() throws Exception {        Request request = new Request.Builder()                .url(&quot;http://www.baidu.com&quot;)// 可以是需要下载的文件地址                .build();        Response response = null;        try {            response = client.newCall(request).execute();            if (!response.isSuccessful()) throw new IOException(&quot;Unexpected code &quot; + response);            Headers responseHeaders = response.headers();            for (int i = 0; i &lt; responseHeaders.size(); i++) {                System.out.println(responseHeaders.name(i) + &quot;: &quot; + responseHeaders.value(i));            }            System.out.println(response.body().string());        } catch (Exception e) {            e.printStackTrace();        } finally {            if (response != null) {                response.close();            }        }    }    public static void main(String... args) throws Exception {        new SynchronousGet().run();    }}</code></pre><h3 id="GET方式异步调用"><a href="#GET方式异步调用" class="headerlink" title="GET方式异步调用"></a>GET方式异步调用</h3><pre><code>package cn.xiangnan.okhttpdemo;/** * Created by zhangxiangnan on 16/9/25. */import okhttp3.*;import java.io.IOException;/** * 异步GET调用,在一个工作线程中进行文件或者普通页面调用,当响应可读时在另一个线程中执行回调函数; * 在响应头部信息准备好后,就执行回调函数;但是读响应体可能仍然阻塞。 * Okhttp目前不提供对响应体异步分开读取。 */public final class AsynchronousGet {    private final OkHttpClient client = new OkHttpClient();    public void run() throws Exception {        Request request = new Request.Builder()                .url(&quot;http://www.baidu.com&quot;)                .build();        // 异步请求是通过执行入队        client.newCall(request).enqueue(new Callback() {            // 失败            public void onFailure(Call call, IOException e) {                e.printStackTrace();            }            //正常            public void onResponse(Call call, Response response) throws IOException {                ResponseBody responseBody = null;                try {                    responseBody = response.body();                    if (!response.isSuccessful()) throw new IOException(&quot;Unexpected code &quot; + response);                    Headers responseHeaders = response.headers();                    for (int i = 0, size = responseHeaders.size(); i &lt; size; i++) {                        System.out.println(responseHeaders.name(i) + &quot;: &quot; + responseHeaders.value(i));                    }                    System.out.println(responseBody.string());                } catch (Exception e) {                } finally {                    responseBody.close();                    response.close();                }            }        });    }    public static void main(String... args) throws Exception {        new AsynchronousGet().run();    }}</code></pre><h3 id="访问头部信息"><a href="#访问头部信息" class="headerlink" title="访问头部信息"></a>访问头部信息</h3><p>通常HTTP头部类似Map&lt;String,String&gt;工作：每一个字段有一个值或更多。但是一些头部允许多个值，如Guava的MultiMap。例如，HTTP响应体提供多个变化的headers是合法和常见的。OKHttp的API尝试优雅的处理2中情形。</p><p>当写入请求体时，使用header(name, value)来设置name和value的首次设置，如果已经存在values，values会被移除，新值会被添加。可以使用addHeader(name,value)来追加header，而不用移除已经存在的headers.</p><p>当读取响应的头部时，使用header(name)来获取最近出现的命名的value值。通常这也是仅有的一个值。如果值不存在，header(name)返回null。想要读取一个字段的所有值返回一个list，可以使用headers(name).</p><p>想要访问所有的头部，使用Headers类，支持按索引访问。</p><pre><code>package cn.xiangnan.okhttpdemo;/** * Created by zhangxiangnan on 16/9/25. */import java.io.IOException;import okhttp3.OkHttpClient;import okhttp3.Request;import okhttp3.Response;/** * 访问头部,header(name)访问最近的头部,headers(name)返回多个值 */public final class AccessHeaders {    private final OkHttpClient client = new OkHttpClient();    public void run() throws Exception {        Request request = new Request.Builder()                .url(&quot;https://api.github.com/repos/square/okhttp/issues&quot;)                .header(&quot;User-Agent&quot;, &quot;OkHttp Headers.java&quot;)                .addHeader(&quot;Accept&quot;, &quot;application/json; q=0.5&quot;)                .addHeader(&quot;Accept&quot;, &quot;application/vnd.github.v3+json&quot;)                .build();        Response response = null;        try {            response = client.newCall(request).execute();            if (!response.isSuccessful()) throw new IOException(&quot;Unexpected code &quot; + response);            System.out.println(&quot;Server: &quot; + response.header(&quot;Server&quot;));            System.out.println(&quot;Date: &quot; + response.header(&quot;Date&quot;));            System.out.println(&quot;Vary: &quot; + response.headers(&quot;Vary&quot;));        } catch (Exception e) {            e.printStackTrace();        } finally {            response.close();        }    }    public static void main(String... args) throws Exception {        new AccessHeaders().run();    }}</code></pre><h3 id="POST提交String字符串"><a href="#POST提交String字符串" class="headerlink" title="POST提交String字符串"></a>POST提交String字符串</h3><p>使用一个HTTP POST来发送一个请求体到一个服务，因为该API的整个请求体全部在内存里，所以最好&lt;=1m。</p><pre><code>package cn.xiangnan.okhttpdemo;/** * Created by zhangxiangnan on 16/9/25. */import okhttp3.*;import java.io.IOException;/** * 使用一个HTTP POST来发送一个请求体到一个服务，因为该API的整个请求体全部在内存里，所以最好&lt;=1m。 */public final class PostString {    public static final MediaType MEDIA_TYPE_MARKDOWN            = MediaType.parse(&quot;text/x-markdown; charset=utf-8&quot;);    private final OkHttpClient client = new OkHttpClient();    public void run() throws Exception {        String postBody = &quot;&quot;                + &quot;Releases\n&quot;                + &quot;--------\n&quot;                + &quot;\n&quot;                + &quot; * _1.0_ May 6, 2013\n&quot;                + &quot; * _1.1_ June 15, 2013\n&quot;                + &quot; * _1.2_ August 11, 2013\n&quot;;        Request request = new Request.Builder()                .url(&quot;https://api.github.com/markdown/raw&quot;)                .post(RequestBody.create(MEDIA_TYPE_MARKDOWN, postBody))                .build();        Response response = null;        try {            response = client.newCall(request).execute();            if (!response.isSuccessful()) throw new IOException(&quot;Unexpected code &quot; + response);            System.out.println(response.body().string());        } catch (Exception e) {            e.printStackTrace();        } finally {            response.body();        }    }    public static void main(String... args) throws Exception {        new PostString().run();    }}</code></pre><h3 id="POST提交流"><a href="#POST提交流" class="headerlink" title="POST提交流"></a>POST提交流</h3><pre><code>package cn.xiangnan.okhttpdemo;/** * Created by zhangxiangnan on 16/9/25. *  POST方式提交流，请求提写入的同时产生；该例直接将流写入到okio * 的buffered sink,可以优先使用OutputStream，并从BufferedSink.outputStream()得到。 * */import java.io.IOException;import okhttp3.MediaType;import okhttp3.OkHttpClient;import okhttp3.Request;import okhttp3.RequestBody;import okhttp3.Response;import okio.BufferedSink;public final class PostStreaming {    public static final MediaType MEDIA_TYPE_MARKDOWN            = MediaType.parse(&quot;text/x-markdown; charset=utf-8&quot;);    private final OkHttpClient client = new OkHttpClient();    public void run() throws Exception {        RequestBody requestBody = new RequestBody() {            @Override            public MediaType contentType() {                return MEDIA_TYPE_MARKDOWN;            }            @Override            public void writeTo(BufferedSink sink) throws IOException {                sink.writeUtf8(&quot;Numbers\n&quot;);                sink.writeUtf8(&quot;-------\n&quot;);                for (int i = 2; i &lt;= 997; i++) {                    sink.writeUtf8(String.format(&quot; * %s = %s\n&quot;, i, factor(i)));                }            }            private String factor(int n) {                for (int i = 2; i &lt; n; i++) {                    int x = n / i;                    if (x * i == n) return factor(x) + &quot; × &quot; + i;                }                return Integer.toString(n);            }        };        Request request = new Request.Builder()                .url(&quot;https://api.github.com/markdown/raw&quot;)                .post(requestBody)                .build();        Response response = null;        try {            response = client.newCall(request).execute();            if (!response.isSuccessful()) throw new IOException(&quot;Unexpected code &quot; + response);            System.out.println(response.body().string());        } catch (Exception e) {            e.printStackTrace();        } finally {            response.close();        }    }    public static void main(String... args) throws Exception {        new PostStreaming().run();    }}</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;常见的GET同步、异步调用示例，POST提交字符串、提交流示例。&lt;br&gt;
    
    </summary>
    
      <category term="http" scheme="http://zhangxiangnan.com/categories/http/"/>
    
    
      <category term="okhttp" scheme="http://zhangxiangnan.com/tags/okhttp/"/>
    
      <category term="http" scheme="http://zhangxiangnan.com/tags/http/"/>
    
      <category term="译" scheme="http://zhangxiangnan.com/tags/%E8%AF%91/"/>
    
  </entry>
  
  <entry>
    <title>okhttp之连接Connections</title>
    <link href="http://zhangxiangnan.com/2016/09/25/http/okhttp/okhttp%E4%B9%8B%E8%BF%9E%E6%8E%A5Connections/"/>
    <id>http://zhangxiangnan.com/2016/09/25/http/okhttp/okhttp之连接Connections/</id>
    <published>2016-09-25T04:51:16.000Z</published>
    <updated>2019-02-27T05:39:45.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="连接"><a href="#连接" class="headerlink" title="连接"></a>连接</h3><p>在使用Okhttp发送请求时，尽管你仅仅提供URL，OkHttp使用三种方式来设计连接：URL、Address、和路有route。<br><a id="more"></a></p><h4 id="URLs链接"><a href="#URLs链接" class="headerlink" title="URLs链接"></a>URLs链接</h4><p>URLs（如 <a href="https://github.com/square/okhttp" target="_blank" rel="noopener">https://github.com/square/okhttp</a> ）是HTTP和Internet互联网的基石。为了成为一个为web上任何东西的通用的、去中心化分散的的命名方案，他们也指出了如何访问web资源。</p><p>URLs是抽象的：</p><ul><li>URLS指出调用可能是明文的（http）或者加密的（https），但没有指出哪一种加密算法应该使用，也未指定该如何验证访问者的身份或者何种的凭证应该被信任，</li><li>也未指出是否应该使用一个特定的代理服务器或者如何和代理服务器认证。</li></ul><p>URLS也是具体的：每一个URL标示一个特定的路径（如/square/okhttp)和查询(如?=q=xx&amp;a=xx)。每一个web服务器管理许多URLS。</p><h4 id="Addresses地址"><a href="#Addresses地址" class="headerlink" title="Addresses地址"></a>Addresses地址</h4><p>地址指定一个web服务器（如github.com），以及连接到服务器所需的静态配置：端口、Https设置、优先的网络协议（如HTTP/2或者SPDY）。</p><p>共享相同address地址的URLs可能共享相同的底层的TCP socket连接。共享一个连接对性能有重大提升：低延迟、高吞吐（因为TCP启动较慢，三次握手）以及节省用电.Okhttp使用一个自动重用HTTP/1.x连接的连接池、支持多路复用技术的HTTP/2、SPDY连接。<br>在Okhttp中，address地址的一些字段来自URL（协议，主机，端口），剩余来自OkHttpClient。</p><h4 id="路由Routes"><a href="#路由Routes" class="headerlink" title="路由Routes"></a>路由Routes</h4><p>路由提供动态必需的动态信息来实际连接到一个web服务器。通过特定的ip地址尝试（通过DNS查询发现），使用准确的代理服务器（如果使用了一个ProxySelector），以及TLS协商的版本（针对HTTPS连接）。<br>一个地址可能有许多路由，例如，在多数据中心的一个web服务器可能在DNS应答中返回多个ip地址。</p><h4 id="连接-1"><a href="#连接-1" class="headerlink" title="连接"></a>连接</h4><p>当你使用Okhttp请求一个URL，如下是Okhttp请求的内部过程：   </p><p>  1、它使用URL和配置好的OkHttpClient来创建一个address地址，这个address地址指出了我们如何连接到web服务器。<br>  2、它尝试在连接池中检索来获取那个地址的连接<br>  3、若在连接池中没有找到对应连接，它选择一个路由来尝试。这通常意味着发出一个DNS请求来得到服务器的IP地址。如果必须的话，它还会选择一个TLS版本和代理服务器<br>  4、如果是一个新路由，它通过构建一个直接socket连接、一个TLS隧道（针对一个HTTP代理上的HTTPS）、或者一个直接的TLS连接来连接。必要时它会进行TLS握手。<br>  5、最后发送HTTP请求然后读取响应。</p><p>如果连接异常，Okhttp将再选择一个路有，再次重试。这允许Okhttp在当一个服务端的多个地址有一部分不可达时，进行恢复。这当一个连接池中的连接过时失效时，或者已经尝试的TLS版本不支持的话，也是有用处的。</p><p>一旦响应被接受，连接就会被返回到连接池，以便新的请求可以重用该连接。在一个连接不活跃一段时间后，连接将被从连接池回收。</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;连接&quot;&gt;&lt;a href=&quot;#连接&quot; class=&quot;headerlink&quot; title=&quot;连接&quot;&gt;&lt;/a&gt;连接&lt;/h3&gt;&lt;p&gt;在使用Okhttp发送请求时，尽管你仅仅提供URL，OkHttp使用三种方式来设计连接：URL、Address、和路有route。&lt;br&gt;
    
    </summary>
    
      <category term="http" scheme="http://zhangxiangnan.com/categories/http/"/>
    
    
      <category term="okhttp" scheme="http://zhangxiangnan.com/tags/okhttp/"/>
    
      <category term="http" scheme="http://zhangxiangnan.com/tags/http/"/>
    
      <category term="译" scheme="http://zhangxiangnan.com/tags/%E8%AF%91/"/>
    
      <category term="Connections" scheme="http://zhangxiangnan.com/tags/Connections/"/>
    
  </entry>
  
</feed>
